# ng-agent — Project State

Last updated: 2026-05-26

## Recent changes

- **2026-05-26 — Call-prep brief: iClosed intake fallback when GHL has no contact ([PR #27](https://github.com/neurogrowth-cr/ng-agent/pull/27), squash `ee12298`).** Ron flagged that Max's `📞 CALL PREP` Slack DMs were still rendering the misleading `❓ Source: setter unknown (no appointment-setting pipeline opportunity)` + `*GHL CONVERSATION HISTORY:* No GHL contact / conversation found for this prospect (likely iClosed-only booking).` for iClosed self-bookings — zero context for the closer. **Diagnosis (read-only against live GHL + portal Supabase):** for Manuel Leon Urtuzuastegui (`mlu.exed@gmail.com` / `+523339529885`), GHL returns **empty** for email-duplicate, phone-duplicate, fuzzy-email, fuzzy-phone, and fuzzy-name lookups — all HTTP 200, so the `searchGHLContact` helper at [index.js:5175](../index.js) was working correctly; the contact genuinely does not exist in GHL. The iClosed payload confirms why: `call_booked_from: "PUBLIC_SCHEDULER"` with empty `event.setter`. iClosed's native integration does not create a GHL contact for public-scheduler self-bookings, and `dash.neurogrowth.io` ingestion only writes `revops_*` (never calls GHL). With the VSL funnel paused, this is now systemic — every public-scheduler booking hits this path. The `funnel_architecture.md` memory note ("iClosed creates a GHL contact") is stale on this point. **Fix:** new helper `fetchIClosedIntakeForProspect(prospectId)` at [index.js:5310](../index.js) pulls the booking payload from `iclosed_webhook_deliveries` (portal Supabase, keyed by `prospect_id`), prefers the `Call booked` delivery row, extracts `questions_and_answers` (array form on Call booked, object form on Contact updated), `event_type.name`, `call_booked_from`, `event.setter.name`, timezone, phone — drops boilerplate Q&A (`Full Name` / `Phone Number` / `Email Address` / `*_question` / `*_response`). In `runSalesCallPrep` at [index.js:5471](../index.js): added scalar `prospect_id` to the appointment select, restructured the `vsl`-source branch so when `setterInfo.source === 'vsl'` the brief now calls the new helper and resolves `sourceLine` to `🟢 Source: self-booked via iClosed (no setter)` and `convoSection` to a deterministic intake block (event name + the prospect's free-text "anything to help prepare" answer + booked-via / timezone / phone). Setter-booked / Form-funnel calls (which do have GHL contacts) are unchanged — the GHL conversation block still renders for them. Brief style chosen: deterministic render, no LLM (matches the existing static call-prep pattern; the prospect's own free-text answer is the gold). **Verification:** read-only repro confirmed the helper output for Manuel produces `Event: Internal Partner Consulting | 1:1 Session` + `Please share anything that will help prepare for our meeting: "nueva campaña Leon Sanador"` + `Booked via: public scheduler · Timezone: America/Mexico_City · Phone: +523339529885`. Railway deployment `4158eafb-8058-456f-ad0b-5a9fe8046569` from commit `ee12298` confirmed **SUCCESS** at 23:14 UTC. Next hourly call-prep cron exercises the new path end-to-end.
- **2026-05-26 — User-scoped MCP servers for Railway + Meta (mirrors GHL pattern).** Two new local MCP servers installed at `~/.claude/mcp-servers/` and registered at user scope so every Claude Code project sees them on launch (verified `✓ Connected` from `/tmp`). **Railway (`railway_mcp.py`)** — stdlib-only stdio JSON-RPC wrapper over Railway's Public GraphQL API at `https://backboard.railway.com/graphql/v2`. Auth via `RAILWAY_TOKEN` in `.env` using `Project-Access-Token` header (token is project-scoped to `confident-analysis` / project ID `36c952a1-6e0b-46b1-a4e5-b9fa1ebc1c47`, env ID `09745209-2fc3-4ad8-a356-b1a042bdb3e5`, the only Railway project — fine since there's just one). Token scope is auto-resolved at startup and used as defaults so most calls don't need IDs. 13 tools: `railway_token_info`, `railway_project_get`, `railway_services_list`, `railway_deployments_list`, `railway_deployment_get`, `railway_deployment_logs`, `railway_build_logs`, `railway_variables_list`, `railway_variable_upsert`, `railway_variable_delete`, `railway_redeploy`, `railway_deployment_trigger`, `railway_graphql_raw`. **Meta (`meta_mcp.py`)** — stdlib-only wrapper over Graph API v21.0 + Marketing API + Conversions API. Auth via `META_ACCESS_TOKEN` (long-lived system-user token tied to "ng-agent" user `122107474424879627`) with default `META_AD_ACCOUNT_ID=act_703538698969328` baked into tool defaults. 14 tools: `meta_me`, `meta_token_debug`, `meta_adaccount_get`, `meta_campaigns_list`, `meta_adsets_list`, `meta_ads_list`, `meta_insights`, `meta_pages_list`, `meta_lead_forms_list`, `meta_lead_form_leads`, `meta_capi_send` (server-side conversion events; defaults to `META_PIXEL_ID=2758787564470503` "Ron-Duarte-Pixel-02/11/26", the active pixel — second pixel `4123134261242482` "Ron Duarte" hasn't fired since 2026-04-27 and was skipped), `meta_pixel_get`, `meta_business_pixels_list`, `meta_graph_raw`. Both wrappers follow the existing `ghl_mcp.py` pattern: subprocess-curl, GraphQL/HTTP error → `isError: true`, raw escape-hatch tool. Registered via `claude mcp add --scope user railway/meta python3 <path>` — config lives in `~/.claude.json` user scope (not project `.mcp.json`). Smoke-tested end-to-end (initialize → tools/list → token info + ad account fetch). After today's setup, the `~/.claude/mcp-servers/` user-scope inventory is **3 servers**: ghl, railway, meta — all available from any project on launch.
- **2026-05-26 — Sales EOD Report v3: truncation fix + booking-track priority filter + 5-message conversation context.** Three iterative fixes after observing two real reports in the wild. **(1) Truncation fix ([PR #21](https://github.com/neurogrowth-cr/ng-agent/pull/21), squash `c15d43d`)** — the May 25 report cut off mid-name ("ERICK VILLALOB"). Root cause: the tool-use follow-up call in `callClaude` at [index.js:4318](../index.js) — where the final report text is generated *after* Claude finishes its tool calls — was capped at `max_tokens: 1024` (~3000 chars). Bumped to 4096 (~12000 chars). The initial call at line 4259 stays at 2048 since that round just plans tool calls. **(2) Booking-track priority filter (Supabase prompt only, no code)** — May 26 report still listed 24 unanswered conversations including every inbound message, many of which were prospects who already had booked calls (noise) or cold/dead-end replies. Rewrote `TOMORROW'S PRIORITY` in the Sales EOD prompt (Supabase `scheduled_tasks` id `8fa09ed3-0922-45c7-a2f0-8ad1b5cdee21`) to surface ONLY two buckets: (a) "ABOUT TO BOOK — PENDING ON OUR SIDE" — inbound + no booked call (today or future) + positive booking-track intent in the message tail; (b) "STALLED POSITIVE CONVERSATION — 2+ DAYS" — last exchange 2+ days old, was substantive, no booked call. Booked-call confirmations dropped from priority (handled at call-prep time elsewhere). `WORKED VS UNWORKED` collapsed to 3 counts that must match the priority list sizes. **(3) Richer conversation context ([PR #24](https://github.com/neurogrowth-cr/ng-agent/pull/24), squash `096a475`)** — judging "positive booking-track" from a single 120-char `lastMessageBody` snippet was borderline for replies like "ok dale" or 👍 (context decides). Extended `getGHLConversations` at [index.js:3017](../index.js) to fetch the last 5 messages per conversation in parallel via the existing `ghlGetConversationMessages` helper, inlined oldest-first with direction (`in`/`out`) + CR timestamp + 150-char body slice. Per-convo fetch failures degrade silently to the previous single-snippet line so one bad request never breaks the tool result. Adds ~50 GHL API calls per Sales EOD run (well under the 100/min limit; runs once daily) and grows tool output from ~5KB to ~25KB — fine for the model. Prompt updated in the same pass to read "Last 5 messages" block instead of the single snippet. **Also shipped today:** authoritative LEADS TODAY attribution from `lead_posts` + `setter_claims` (replacing the broken Slack-text inference that was undercounting Oscar 9→1 on May 18) — new `"leads today"` branch in `getSalesIntelligence` at [index.js:2540](../index.js) buckets by CR day, dedupes FB→WA dupes by `slack_message_ts`, joins claims by `ghl_contact_id`. Tool description updated so Claude routes the call. Verified pre-deploy against May 18 data: authoritative 19 leads / William 10 / Oscar 9 / 0 unclaimed vs the broken report's 14 / 13 / 1. Railway deployment `1576b12a-7b9b-42c9-992f-c6e4ec1b0064` from commit `096a475` confirmed RUNNING at 15:38 UTC — all 11 dynamic crons re-registered at boot, so the new prompt is loaded. Tonight's 6:30 PM CR run is the first to exercise the full stack end-to-end.
- **2026-05-25 — Full sales-report audit: flywheel-only filter applied across all 10 query sites.** Ron flagged that sales reports were still surfacing partner-consulting 1:1s in places the May 19 fix didn't touch (the May 19 patch only covered 3 call sites). Replaced upstream's narrow `getPartnerConsultingCallIds()` (slug == 'partner-consulting-session') with a stricter positive allowlist helper: `getNonFlywheelCallIds()` + `filterFlywheelAppts()` defined at [index.js:2535](../index.js). The helper returns the Set of `iclosed_call_id`s whose `payload[0].event_type.slug` is NOT one of `linkedin-flywheel`, `linkedin-flywheel-vsl`, `linkedin-flywheel-doc` (auto-handles any future non-flywheel calendar iClosed adds). 10-min in-process cache, 120-day window, negative filter (rows with no webhook match still pass through so we never silently drop a real flywheel call). Applied to all 10 sales-report query sites: `getSalesIntelligence`, `getCloserWeeklyStats`, `getSetterWeeklyStats`, `runMondayGapDetection` (no-shows + unlogged outcomes), `_scrapeMetaCostPerBookingToday`, `_scrapeIclosedCallsBookedYesterday`, `_scrapeIclosedCallsHeldYesterday`, `runSalesCallPrep`, `runSalesStandup` (closer today-on-deck), `runUnloggedOutcomeReminders`. The redundant upstream `getPartnerConsultingCallIds()` helper and its 3 in-line filter blocks were removed (replaced by the new helper one layer up). EOD-table scrapers untouched — they read self-reported pod data which is already sales-scoped. Production check: in the last 90 days, 30 partner-consulting calls had been polluting these reports vs 147 legitimate flywheel calls.
- **2026-05-19 — Daily Call Roster / call-prep / closer brief: exclude Internal Partner Consulting 1:1s.** Bug: iClosed hosts two distinct calendars under the same account — `linkedin-flywheel*` (sales strategy calls from flywheel events, what the sales pod actually closes against) and `partner-consulting-session` ("Internal Partner Consulting | 1:1 Session", paid-client meetings). Both flow into `revops_appointments` indistinguishable by any stored column (`meeting_type` is 100% null in prod). On May 19 the Daily Call Roster post in `#ng-sales-goats` had 5 of 6 calls as partner consulting (Alejandro Cross, Natalia Carvajal, James Taylor, Carlos Pérez confirmed by event_type.slug; only Jacobo Lau and Diego Hidalgo were genuine flywheel). **Fix:** new helper `getPartnerConsultingCallIds()` at [index.js:5333](../index.js) reads `iclosed_webhook_deliveries` (90-day window, `normalized_event_type='Call booked'`), collects every `payload[0].event.callPreviewId` whose `payload[0].event_type.slug === 'partner-consulting-session'` into a Set, and three call sites filter against it via `iclosed_call_id`: (i) `getSalesIntelligence` "today calls" branch (line ~2671) — drives the Daily Call Roster scheduled task `7873fe73-9c92-4865-964f-5fc1efadf7e9`; (ii) `runSalesCallPrep` (line ~5340) — added `iclosed_call_id` to the select; (iii) `runSalesStandup` closer brief (line ~5828) — added `iclosed_call_id` to the select. Filter is **negative** (drop known partner-consulting): appointments without a matched webhook row still appear, so we never silently hide a real flywheel call. No prompt change needed — the Supabase scheduled_tasks row already reads from `get_sales_intelligence("today calls")`. No migration. Verified the slug taxonomy live: 91× `linkedin-flywheel`, 29× `partner-consulting-session`, 10× `linkedin-flywheel-vsl`, 7× alt VSL, 2× `linkedin-flywheel-doc` (Apr 1+ window).
- **2026-05-19 — Unlogged-outcome closer reminders + root-cause fix: iClosed outcomes never reached `revops_sales_outcomes`.** Two PRs across two repos. **ng-agent ([PR #15](https://github.com/neurogrowth-cr/ng-agent/pull/15) — merged to main):** new `runUnloggedOutcomeReminders()` in [index.js](../index.js), cron `0 16 * * * America/Costa_Rica` (4 PM CR daily, every day incl. weekends). Past calls 24h–14d old, excludes cancelled (`qualification_snapshot.iclosed.cancelled`) and null `iclosed_call_id`; "logged?" is derived from raw `iclosed_webhook_deliveries` rows where `normalized_event_type='Outcome added'` (matching `payload.callPreviewId === revops_appointments.iclosed_call_id`, 30-day lookback) — chosen because `revops_sales_outcomes` was empty at build time. DMs each closer (`CLOSER_SLACK`) their unlogged list, re-nudges daily de-duped via `agent_knowledge` key `outcome-reminder:<apptId>` (value `firstRemindedISO|count`), escalates to Ron after 3 days. Removed the dead `attended=true` "Outcome not logged" block from `runSalesStandup` (it never fired — `attended` is ~always null), wired `inferReportId` + `getReportLessons('unlogged-outcome-reminder')`. Validated: 48 due / 42 logged / 6 unlogged (all recent weekend), matches reality. **dash.neurogrowth.io ([PR #3](https://github.com/neurogrowth-cr/dash.neurogrowth.io/pull/3) `3a119d6` — merged + live on Vercel prod):** root cause of the empty table — iClosed sends outcomes as a flat, array-wrapped `"Outcome added"` webhook (`callOutcome`/`noSaleReason`/`callPreviewId`/`closerEmail` at root, no `event`/`data` wrapper). Three bugs in `src/lib/integrations/iclosed/normalize.ts` meant all 165 such deliveries were `skipped/unknown_event_shape`: (1) `inferCanonicalFromHook` had no "outcome" case → canonical `unknown`; (2) `callId` never read from root-level `callPreviewId`; (3) outcome fields read `data.call_outcome` not flat camelCase `callOutcome`. Fixes: hook→`outcome_added`, root `callPreviewId` fallback, numeric `root.contactId` via `idString`, `closerEmail` from root, plus new `mapIclosedNativeOutcome()` in `outcome-map.ts` mapping `callOutcome`+`noSaleReason` onto the 7-value `revops_sales_outcomes.outcome` CHECK enum (SALE→won w/ revenue+close_date+offer; NO_SHOW→no_show; FOLLOW_UP_SCHEDULE/CONTACT_CANCELLED/ADMIN_CANCELLED→follow_up; UNQUALIFIED→disqualified; NOT_INTERESTED/other→lost). 16/16 iclosed unit tests pass. **Backfill done** via existing admin route `POST /api/admin/integrations/iclosed/backfill {batch:true,limit:500,onlyUnknownShape:true}` (idempotent, unique index on `appointment_id`): `revops_sales_outcomes` **0 → 151 rows** (165 events → 151 appts; dist follow_up 101 / no_show 22 / won 16 / lost 6 / disqualified 6). Independent integrity check (outcome→appointment→prospect vs raw webhook email): **145/151 (96%) exact match**; 6 exceptions are 5 internal "Test Sancho" bookings + 1 iClosed-side call reassignment — none caused by the fix (they reflect how the call was originally *booked*). Going forward new outcomes auto-write. **Note:** the ng-agent reminder still reads the `iclosed_webhook_deliveries` workaround, not `revops_sales_outcomes` — both are now correct/consistent so no change needed, but a future simplification could point it at the table.
- **2026-05-19 — Phone normalizer extended to MX safely (US/MX disambiguation via GHL country hint).** The first phone-normalization pass ([PR #17](https://github.com/neurogrowth-cr/ng-agent/pull/17)) handled CR (8-digit) vs US/CA (10-digit) but didn't account for MX prospecting — MX national numbers are also 10 digits (post-2019 IFT unification), so the original "10 digits → +1" rule would have mis-tagged every Mexican lead as US *and* corrupted the GHL contact via the writeback. **Fix:** `normalizePhone(raw, countryHint)` at [index.js:6263](../index.js) now takes the GHL contact's `country` field as a tiebreaker. Logic: explicit prefix (`+1` / `+52` / `+506`) always wins; bare 8-digit → CR (unambiguous); bare 10-digit + hint `US`/`CA` → +1; bare 10-digit + hint `MX` → +52; bare 10-digit with no/unknown hint → display raw + skip writeback (refuses to guess between US and MX). Webhook handler now reads `cd.country || ct.country || payload.country` and passes it through. Also added handling for pre-2019 MX mobile format (`+52 1 55 1234 5678` = 13 digits starting `521`) by stripping the legacy `1`. **Trade-off:** strictly safer than v1 — we lose the aggressive auto-fix on 10-digit US numbers whose GHL country field is empty/wrong (those now stay raw, no writeback), but we never corrupt an MX record. Verified against US/MX/CR cases with and without country hints, mis-prefixed `+506`-on-US, legacy `521` MX mobile, and non-target countries like `+44`.
- **2026-05-19 — Lead phone numbers standardized in Slack + auto-corrected in GHL.** Bug: US numbers (e.g. `931-859-5650`) arrived from GHL without a country code, so GHL's location default (+506) flagged them as Costa Rican — team had to fix the country flag by hand on every lead. Inverse case also seen in the wild: 10-digit US numbers mis-stored with a `+506` prefix. **Fix ([PR #17](https://github.com/neurogrowth-cr/ng-agent/pull/17), commit `70fe240`):** new `normalizePhone()` helper at [index.js:6263](../index.js) disambiguates by national-number length — CR national = 8 digits, NANP = 10 digits, no library needed. Strips any leading country code (`1`, `506`, mis-prefixed `+506` + 10-digit US, etc.) before judging length and returns `{e164, display, confident}`. Wired into `handleGHLWebhook`: (a) Slack "New Lead" post now shows `+1 (931) 859-5650` / `+506 8888 8888` instead of the raw value; (b) a conservative `PUT /contacts/{id}` writes the corrected E.164 back to the GHL contact — gated on `confident === true` AND `e164 digits !== raw digits`, so already-correct numbers don't trigger redundant writes and non-US/CR or malformed numbers are never auto-modified (left as raw display, no GHL write). Activity log row `event_type: 'ghl_update', action: 'phone_normalized'` records every writeback with from/to. Verified against: raw 10-digit US, raw 8-digit CR, already-`+1`, already-`+506`, mis-tagged `+506` + 10-digit US, UK `+44`, malformed shorts. Dedup `phoneLast10` logic unchanged (still last-10-digits of raw — consistent regardless of formatting).
- **2026-05-13 — iClosed setter-reveal: root cause was a missing Slack event subscription + setter @mention/DM upgrade.** The `slack.event('message')` listener at [index.js](../index.js) (iClosed "Strategy call booked" relay) had never fired once since it shipped 2026-05-05 — `agent_knowledge` had zero `iclosed-setter-reveal-*` dedup rows. **Root cause:** `#ng-sales-goats` is a *private* channel. The Slack app was subscribed to `message.channels` (public only) but not `message.groups` (private channels), so every message posted there was silently never delivered to Max — only DMs reached the listener (`channel_type: "im"`). Confirmed via a temp `msg-event debug:` trace in Railway logs (all lines were `im`, zero for `C0AJANQBYUE`). Scopes (`groups:history`) and channel membership were already fine; the gap was purely the event subscription. **Fix:** added `message.groups` bot event in the Slack app config (api.slack.com/apps/A0AMJFZDY2X → Event Subscriptions). No reinstall required since `groups:history` was already granted; Socket Mode re-handshakes automatically. **Lesson (also logged in lessons.md):** a Slack listener on a *private* channel needs `message.groups`, not just `message.channels` — the public/private split is silent and easy to miss when the bot can still post (chat:write) and receive `reaction_added`. **Feature upgrade in the same pass** (commit `3d1e273`, requested by William after he missed a call): the threaded reveal now tags the setter via `<@slackId>` (resolved through existing `resolveSetterSlackId()` at [index.js:5983](../index.js)) instead of a plain-text name so they actually get pinged, and Max now also DMs the setter the booking details (prospect name/email, date, time, closer, permalink back to the `#ng-sales-goats` post). DM only fires when the Slack ID resolves and is wrapped in its own try/catch so a DM failure can't break the channel reveal. Temp diagnostic traces (`msg-event debug:` / `iClosed relay debug:`) have been removed.
- **2026-05-18 — Sales EOD Report grooming: authoritative lead attribution + unanswered-reply guardrail.** Two problems on the May 18 report: (1) LEADS TODAY undercounted setter ownership (Oscar credited with 1 lead, actually owned 9; total said 14, actually 19). Root cause: the prompt told Claude to derive lead counts/owners by reading immutable `#ng-sales-goats` post text — ✋/✅ claims only write `setter_claims` + a threaded reply, never edit the post, so reaction-claimers were uncredited. (2) No guardrail forcing unanswered client replies to the top of tomorrow's priorities. **Code fix:** new authoritative branch in `getSalesIntelligence()` ([index.js](../index.js), ~line 2540) triggered by query `"leads today"` — runs before the "today calls" branch and before the portal fetch (lead data is on the primary `supabase` project, not portal). Buckets `lead_posts` by Costa-Rica day (UTC−6, no DST), dedupes to one logical lead per `slack_message_ts` (FB→WA dup path shares the original's ts), joins `setter_claims` by `ghl_contact_id` (earliest claim wins, any date) for ownership, resolves name via `claimed_by_setter_name` then a Slack-ID→setter map, and returns a `LEADS_TODAY_DATA` block (total, per-setter, unclaimed count, sources). `get_sales_intelligence` tool description updated so Claude routes the call. No new tool registration (dispatch already routes any query string). No migration, no `TASK_HEADERS` change. **Prompt fix (Supabase `scheduled_tasks` id `8fa09ed3-0922-45c7-a2f0-8ad1b5cdee21`, no source change):** STEP 1 now calls `get_sales_intelligence "leads today"` and the channel read is for iClosed booking notifications only. LEADS TODAY renders verbatim from the block (stale hardcoded JOSEPH/DEBBANNY scaffold removed). WORKED VS UNWORKED collapsed to three numbers (worked / unanswered / stale) — no lists. TOMORROW'S PRIORITY now strictly ordered: every `<-- inbound` conversation (client replied, no response) first with a "REPLY FIRST" guardrail, then 3+ day stale, then tomorrow's booked calls; setter Slack tags corrected (Oscar `U0B1S1UMH9P`, William `U0B16P6DQ2F`, Joseph `U0A9J00EMGD`). **Requires Railway restart** for the new prompt to load (prompts are read into closures at boot). Verified pre-deploy against May 18 data: lead_posts+setter_claims → 19 total, William B 10, Oscar M 9, 0 unclaimed.
- **2026-05-13 — Weekly Sales Pod Leaderboard + four reporting gap fixes.** Shipped in two PRs ([#12](https://github.com/neurogrowth-cr/ng-agent/pull/12) `6e30834`, [#13](https://github.com/neurogrowth-cr/ng-agent/pull/13) `05de0b0`). **PR #12 fixes:** (a) Daily Call Roster setter fallback no longer hard-codes `'VSL self-booking'` ([index.js:2557](../index.js)) — VSL funnel is paused, so the literal label was mis-attributing every self-booked call; same fix applied to call-prep `sourceLine` and convo fallback. (b) GHL contact lookup in `searchGHLContact()` ([index.js:4922](../index.js)) now tries `/contacts/search/duplicate` (exact email) first, then fuzzy query with email-preference among results; diagnostic `[GHL]` logs added to both `searchGHLContact` and `fetchGHLConvoForContact` so the next "conversation not found" is debuggable from Railway logs. (c) New `getCloserWeeklyStats()` helper ([index.js:2739](../index.js)) pulls truth from `revops_appointments` + `revops_sales_outcomes` and only folds in `revops_closer_eod_daily` numbers for closers with no iClosed rows; cron loader injects this as a `PRECOMPUTED DATA` block into the `Weekly Closer Comparison` prompt. (d) `TASK_HEADERS['Weekly Closer Comparison']` ([index.js:1140](../index.js)) is now `[]`; the validator derives required header names at runtime from the precomputed stats, so a week where only Jonathan has activity still passes validation. **PR #13 adds the leaderboard:** new `getSetterWeeklyStats()` mirrors the closer helper (iClosed `revops_appointments.setter_id` = truth for calls booked/attended/no-shows; `revops_setter_eod_daily` supplies new conversations + qualified leads). `formatCloserWeeklyStatsBlock` pre-ranks by revenue→sold; `formatSetterWeeklyStatsBlock` pre-ranks by qualified leads→calls booked. Cron payload for the leaderboard task injects both blocks. Validator's dynamic-headers branch now requires a quorum from the union of closer+setter activity. **Supabase side (already applied):** `scheduled_tasks.channel` for `Daily iClosed Call Roster` (`U05HXGX18H3` → `ng-sales-goats`) and `Weekly Closer Comparison` (`ng-pm-agent` → `ng-sales-goats`). Daily Roster prompt rewritten for public-channel audience and no longer hard-codes "VSL self-booking". Sales EOD Report prompt has same VSL → setter unknown fix in its setter-attribution rules. `Weekly Closer Comparison` prompt rewritten as `WEEKLY SALES POD LEADERBOARD` with `TOP CLOSERS` (🥇🥈🥉 medals + revenue/sold/close-rate ranking) and `TOP SETTERS` (medals + qualified-leads/calls-booked ranking) sections plus a `POD TAKEAWAY` paragraph; 🚩 flags fire for closers below 22% close rate or above 50% no-show rate, and for setters logging fewer than 3 EODs that week. Task name kept as `Weekly Closer Comparison` (id `83c84032-62e7-48b6-95a5-42501dd2e81e`) so the code's `task.name === 'Weekly Closer Comparison'` branch keeps wiring stats injection + dynamic validation — only the rendered headline says "WEEKLY SALES POD LEADERBOARD". First leaderboard run: Monday 2026-05-18 07:00 CR.
- **2026-05-08 — Initiative 2: Approval mode added (per-draft ✅ before send).** New env var `STALLED_FOLLOWUPS_MODE` with values `dry_run` (default) | `approval` | `live`. `STALLED_FOLLOWUPS_LIVE='true'` kept as legacy shorthand for `mode=live`. Approval mode runs the same guardrails as live (business hours, 14d cooldown, 2-lifetime cap, daily cap) but instead of POSTing each generated draft to GHL, it DMs Ron a draft post per contact with `metadata.event_type='campaign_draft'` and a new `source: 'stalled_cron'` field. Reuses the existing campaign reaction handler — ✅ sends via `sendCampaignMessage` (writes `campaign_sends`); ❌ skips. The handler now additionally writes a `prospect_followups` row when `source === 'stalled_cron'` so the cooldown / lifetime cap queries respect approval-batch sends, and DMs the setter who owns the thread a "Auto-followup approved + sent" confirmation. `revise <contactId>: <new text>` already worked for any `campaign_draft`-shaped DM, so it works here too. Use approval mode for the first batch (eyeball every draft before it reaches a real prospect), then flip to `mode=live` once you trust the generator.
- **2026-05-08 — Initiative 2: Stalled-prospect live-send path wired (flag-gated).** After 5 business days of clean dry-run (Mon 5/4 → Fri 5/8 — today's run was 32 candidates / 3 would-send / 27 `setter_already_replied` skips, the right shape), built the live path behind `STALLED_FOLLOWUPS_LIVE='true'`. New migration `008_prospect_followups.sql` (audit table for sent followups, indexed on `(contact_id, sent_at desc)` for cooldown queries). `runStalledProspectFollowups` in [index.js](../index.js) now: generates the message via the existing `generateCampaignDraft` helper (single sentence, conversation language, ≤25 words, ends with question); POSTs `https://services.leadconnectorhq.com/conversations/messages` with `{ type: 'WhatsApp', contactId, message }` (same shape as `sendCampaignMessage`); inserts an audit row in `prospect_followups` whether sent or failed; DMs the setter what was sent on their behalf; cross-posts a `🔔 Auto-followup sent — N prospect(s) nudged today` line to `#ng-sales-goats`. Guardrails: business hours 10:00–18:00 CR, 14-day per-contact cooldown, 2-lifetime cap, daily volume cap default 3 (override via `STALLED_FOLLOWUPS_DAILY_CAP` env var; raise to 10 after 2 weeks of clean reply quality). New skip gate `setter_paused` in `evaluateStalledCandidate` checks `agent_knowledge` for `category=setter_pref, key=pause:<contactId>`. New DM commands available to all roster members: `pause <contactId>` (upserts the pause row) and `unpause <contactId>` (deletes it). **Pre-go-live:** apply migration 008; verify the `ng-pm-agent` GHL PIT has `conversations.write` scope; set `STALLED_FOLLOWUPS_LIVE=true` on Railway. Default behavior unchanged until the env flag is flipped.
- **2026-05-08 — iClosed time bug: source-of-truth fix in dash.neurogrowth.io.** Bug: every `revops_appointments.scheduled_start` was stored as CR-wall-clock-tagged-as-UTC, so every downstream display ran 6h early (Eliacid's 2pm CR call rendered as 8am). Root cause: iClosed sends two time fields — `event.start_time` (closer-local wall clock with a misleading `Z` suffix) and `event.utc_start_time` (real UTC). The normalizer at `src/lib/integrations/iclosed/normalize.ts` in [dash.neurogrowth.io](https://github.com/neurogrowth-cr/dash.neurogrowth.io) preferred `start_time`. Fixed by reordering the resolver to read `utc_start_time` first ([PR #2](https://github.com/neurogrowth-cr/dash.neurogrowth.io/pull/2), commit `28d3f48` — live on Vercel production). Added regression test that asserts `utc_start_time` wins when both are present. Side fix in ng-agent: `formatICTime()` helper (line 2473 in [index.js](../index.js)) routes all 6 display sites that read `scheduled_start`; helper now uses `America/Costa_Rica` so new (correct) rows render correctly. Tradeoff: pre-fix rows still display 6h early — no backfill performed (Ron prefers no destructive UPDATE on prod data; legacy rows age out naturally). Webhook delivery payloads stored in `iclosed_webhook_deliveries` table on the proposals Supabase project for future debugging. Range/window queries that compare `scheduled_start` to real "now" elsewhere in [index.js](../index.js) (call prep firing window at line ~4952, no-show alert ranges, etc.) will now work correctly for new rows automatically; legacy rows will mismatch until they age out.
- **2026-05-07 — Daily iClosed Call Roster + Sales EOD setter attribution.** Two changes to scheduled reports, both in `scheduled_tasks` Supabase table (no source code logic change). (a) New cron `Daily iClosed Call Roster` — fires `30 8 * * * America/Costa_Rica`, DMs Ron a roster of every iClosed call booked for today (prospect, time CR, closer, setter, status). Reuses the "today" branch in `getSalesIntelligence` for data + setter resolution. Added `'Daily iClosed Call Roster': []` to `TASK_HEADERS` in [index.js](../index.js) so quiet-day reports pass the relaxed length check (>20 chars) instead of the 300-char default. (b) `Sales EOD Report` prompt updated — STEP 1 now also calls `get_sales_intelligence("today calls")` to pull setter attribution; STRATEGY CALLS BOOKED line now renders `• PROSPECT — date at time CR — with HOST — setter: SETTER FIRST NAME or "VSL self-booking"`. Both prompts are loaded into closures at boot via `registerDynamicCron`, so future prompt edits to either row need a Railway redeploy to take effect.
- **2026-05-07 — Email proxy: Ron-approval bypass for trusted setters (Oscar M first promoted).** New constant `EMAIL_PROXY_AUTOSEND_SETTERS` in [index.js](../index.js) — set of Slack IDs that skip Stage-2 (Ron's "yes" approval) and auto-send the moment the setter says "se ve bien". Setter still gets the standard ✅ success or ❌ failure DM from `executeEmailSend`; Ron gets a parallel `[autosend · <setter>] ...` audit-trail DM (success or error stack) for visibility without forcing approval. Implemented inside `promoteDraftToRon` — short-circuit before the `pendingApprovals[RON_SLACK_ID]` stage, build a synthetic `pending` with `autosent: true`, call `executeEmailSend` directly with a `say` wrapper that routes Ron-side messages to his DM. Promoted: Oscar M (`U0B1S1UMH9P`) — earned bypass after his first clean send. To promote others, add their Slack ID to the set in source. Vacation responder also disabled today (manual UI toggle, unrelated to proxy — was triggering empty auto-replies to every inbound).
- **2026-05-07 — Disabled proactive-alert daily Slack posts (3pm + 8pm CR).** Both daily `runProactiveAlerts` cron registrations at [index.js:5608-5609](../index.js) commented out. The posts had stopped adding signal — they recycled the same stale `agent_knowledge` alerts every day ("blocked clients, Make notifications broken, Softon domains expiring…") even when nothing had moved. Coverage is redundant: Monday gap detection (Mon 8am) catches what's still open at week start, Friday weekly recap (Fri 5pm) closes the loop, anomaly detection (daily 6am) flags fresh metric deltas, and the daily fulfillment + sales standups (Mon–Fri 9am) surface stalled clients per owner. `runProactiveAlerts` function definition at [index.js:3082](../index.js) preserved for manual use or future re-enable.
- **2026-05-06 — Email proxy hotfixes after first live send.** First production send (Jose's Heinsohn cotización) surfaced two bugs: (a) Subject mojibake — "Cotización" rendered as "CotizaciÃ³n" and em-dash as "Ã¢ÂÂ" because Apple Mail defaulted the Subject header to Latin-1 when no charset was declared; fixed by adding `encodeMimeHeader()` that wraps non-ASCII strings as `=?UTF-8?B?<base64>?=` per RFC 2047 and applied to Subject + Cc. Body parts also switched from `Content-Transfer-Encoding: 7bit` to `base64` for bulletproof transport regardless of relay 8bit handling. (b) Signature missing — `getRonSignature()` was caching an empty string from a 403 on initial deploy (likely Railway's `GOOGLE_TOKEN` predates the `gmail.settings.basic` scope rotation). Reordered the resolver: `RON_SIGNATURE_HTML` env var → `sendAs.get` for primary → `sendAs.list` find-any-with-signature → DM Ron with setup instructions if all empty. **Resolution applied:** Ron pasted his Gmail signature (3,335 chars of HTML — Regards block, GTM tagline, LinkedIn + WhatsApp icons, headshot) into Railway env var `RON_SIGNATURE_HTML`. Pulled directly via `gmail.users.settings.sendAs.get` from local `token.json` since the API path returned the full signature locally. Commit `eb46f6b`.
- **2026-05-05 — Setter attribution unified across call prep, iClosed relay, and Sales EOD.** New helper `resolveSetterForContact(email, name)` in [index.js](../index.js) is the single truth source: searches GHL contact, fetches opportunities, filters to `GHL_SETTER_PIPELINE_IDS`. If any opp matches → `{source: 'appointment-setting', setter}`; else → `{source: 'vsl', setter: null}`. Three callers wired: (a) `runSalesCallPrep` no longer mis-tags appointment-setting calls as "VSL self-booking" — old code only checked `contact.assignedTo` and fell through when empty; (b) new `slack.event('message')` listener on `#ng-sales-goats` watches iClosed bot posts containing both "Strategy call booked" and "Event: LinkedIn Flywheel - Appointment", parses the prospect email (excludes known closer/setter emails via `SALES_TEAM_MAP`), posts a threaded `👤 Setter: <name>` reply with `reply_broadcast: true` (the "Also send to #ng-sales-goats" checkbox), dedup'd via `agent_knowledge` key `iclosed-setter-reveal-<ts>`. VSL events are silently ignored; (c) `getSalesIntelligence` "today's calls" path now resolves setter per call so the Sales EOD STRATEGY CALLS BOOKED section can render `closer: ... — setter: ...`.
- **2026-05-05 — Email proxy v1: Max sends sales emails on behalf of setters.** New flow: setter DMs Max conversationally → Max collects to/cc/subject/body → setter approves draft (Stage 1) → Ron approves (Stage 2, reuses APPROVAL_NEEDED pattern) → `sendEmail()` sends from `ronny.duarte@neurogrowth.io` with Ron's Gmail signature appended (multipart text+HTML, fetched once via `gmail.users.settings.sendAs.get`). Replies polled hourly 8am–8pm CR Mon–Fri (`runEmailReplyPoller`); new inbound DMs the setter who initiated; setter replies in DM go through the same two-stage flow with proper threading (`threadId`, `In-Reply-To`, `References`). New table `email_threads` (migration `004_email_threads.sql`) tracks `(gmail_thread_id, last_rfc822_message_id, rfc822_message_id_chain, to/cc, subject, initiated_by_slack_id, last_message_at, active)`. Two new tools `draft_outbound_email` + `draft_reply_email` in TOOLS array, gated behind `EMAIL_PROXY_LIVE='true'` env var. Stage-1 setter approval uses regex bypass on "looks good"/"se ve bien"/"cancel" so deterministic phrases never depend on Claude interpretation. Slot-filling guidance + Spanish/English mirroring added to system prompt only when flag is on. Failure paths DM both setter (with "contact Ron/admin to help") and Ron (with stack); approval expiry, Ron-rejection, send errors, and reply-poll failures (3-strike) all wired. **Pre-deploy:** apply `migrations/004_email_threads.sql`; set `EMAIL_PROXY_LIVE=true` in Railway; verify `gmail.settings.basic` scope (re-auth if 403). Out of scope for v1: contact@ Send-As alias, attachments, push notifications, /close-thread, lifting Ron-approval gate.
- **2026-05-03 — Setter roster transition: Oscar M + William B onboarded, Debbanny offboarded.** New active setters: Oscar M (Slack `U0B1S1UMH9P`, GHL `ZcmdIz2EEraPd80W2zop`, `oscar.neurogrowth@gmail.com`) and William B (Slack `U0B16P6DQ2F`, GHL `N8mvtuHbbbY7QppqNMr7`, `william.neurogrowth@gmail.com`). Updated 13 places in [index.js](../index.js) split between **active routing** (claim handler, sales standup, leaderboard, stalled-followups, role/permissions — Debbanny removed, Oscar/William added) and **historical resolution** (display name maps, GHL ID lookups, iClosed email map — Debbanny preserved so old EOD data still renders her name). Portal Supabase `admin_users` table updated: Oscar/William inserted with `revops_can_set: true`, Debbanny flipped to `revops_can_set: false`. Closers Jonathan + Jose also removed from leaderboard `SETTER_GHL_IDS` array (they don't submit setter EOD; were always rendering zeros). Commit `edb9d3c`.
- **2026-05-03 — Initiative 1 hardening: 5 bug fixes after live deploy.** (a) Skin-tone reaction modifier (`hand::skin-tone-3`) was bypassing the allow-list — strip suffix before checking, commit `903d1b6`. (b) Hardcoded Appointment Setting Pipeline ID had a confusable character (`KH1l...` lowercase L → `KH1I...` uppercase I) — every opp was being silently filtered out of reassignment, commit `3ec4565`. (c) Threaded confirmation reply had nested parens and jargon — rewrote to multi-line bullets with plain English, commit `d32cdd5`. (d) Ron's GHL user ID added to `GHL_USER_NAMES` so `claimed_by_setter_name` populates on his test claims, commit `d32cdd5`. (e) Leaderboard joined on GHL user ID but `revops_setter_eod_daily.setter_id` actually stores email — Saturday's first leaderboard would have rendered all zeros despite real activity. Refactored to a single `ACTIVE_SETTERS` roster carrying name + email + Slack ID; EOD joins by email, claims by Slack ID, no conversion needed, commit `c0a6866`.
- **2026-05-03 — Initiative 1 GHL scope unblock.** Initial reaction-driven claims hit `401: token not authorized for this scope` on `PUT /contacts/{id}`. Root cause: the `ng-pm-agent` Private Integration Token was missing `opportunities.write` (and any scope changes don't propagate to existing tokens — they're baked in at issue time). Fixed by enabling the missing scope + rotating the PIT + updating `GHL_API_KEY` in Railway. Active scopes on the PIT now: `contacts.readonly`, `contacts.write`, `opportunities.readonly`, `opportunities.write`, `users.readonly`, `conversations.readonly`. End-to-end loop verified: Maria Fernanda Troconis lead claimed successfully, row landed in `setter_claims`, GHL contact + Appointment Setting Pipeline opportunity both reassigned to claimer.
- **2026-05-03 — Build Cycle 2: setter_claims audit trail + twice-weekly leaderboard.** New table `public.setter_claims` in primary Supabase (migration `003_setter_claims.sql`) records every Slack reaction-driven lead claim with `seconds_to_claim` (computed at insert time from Slack `ts` vs `claimed_at`). Reaction handler in [index.js](../index.js) inserts into `setter_claims` after successful GHL writes — non-fatal on insert failure. Threaded reply unchanged (no time delta shown to setters; metric is for offline SLA mining only). New cron `0 18 * * 3,6 America/Costa_Rica` runs `runSetterLeaderboard` and posts MTD per-setter performance to `#ng-sales-goats`. Data sources: portal Supabase `revops_setter_eod_daily` (calls_placed, qualified_leads, calls_show, calls_no_show, new_conversations, follow_ups) + primary Supabase `setter_claims` (leads_claimed). Metrics: leads_claimed, calls_placed, engagement_actions (new_conversations + follow_ups — proxy for "chats sent"), qualified_leads, show_rate. Ranked by qualified_leads desc, tiebreak calls_placed. Static cron (not dynamic) so it bypasses the `#ng-sales-goats` approval gate. No LLM call.
- **2026-05-02 — Initiative 1: Reaction-driven lead claim → GHL `assignedTo` write.** GHL leads come in unassigned by design (race-for-leads culture). The `#ng-sales-goats` post in `handleGHLWebhook` now carries Slack `metadata` with `{contact_id, location_id, full_name, correlation_id}`. New `slack.event('reaction_added')` handler in [index.js](../index.js): when a setter reacts ✋/✅ on a lead post, Max resolves their Slack ID → GHL user via new `SLACK_TO_GHL_USER` reverse map, calls `PUT /contacts/{id}` with `assignedTo`, adds a ✅ reaction (idempotency anchor), and posts a threaded confirmation. Race-safe: second reactor sees the ✅ already there and is ignored. Non-setters get a friendly "ping Ron to add you" thread reply. **Slack app scopes required:** `reactions:read`, `reactions:write` + `reaction_added` event subscription — verify in Slack app config before deploy. Channel constant `LEAD_CHANNEL_ID = 'C0AJANQBYUE'`. Activity log: `event_type: 'ghl_lead_claimed'`.
- **2026-05-02 — Initiative 2: Stalled prospect detection (DRY-RUN mode).** New cron `0 11 * * 1-5` runs `runStalledProspectFollowups`. Finds WhatsApp conversations where last message is inbound + ≥ 2 business days old + assigned to a known setter. Applies 8 skip gates (no-setter, thread-too-short, voice-note-or-empty, emoji-only, opt-out phrase scan EN+ES, no-phone, DNC tag, future iClosed booking, attended call). Eligible prospects DM'd to the **assigned setter** (each setter sees only their own list); summary DM to Ron with skip breakdown. **Live auto-send is gated by `STALLED_FOLLOWUPS_LIVE='true'` env var (default off) and is intentionally not yet wired** — falls back to dry-run DM with a warning log. Live path will land after 2 business days of clean dry-run output. Helpers: `businessDaysBetween`, `hasOptOutSignal`, `ghlGetConversationMessages`, `ghlGetContact`, `hasFutureBooking`, `hasAttendedCall`, `evaluateStalledCandidate`. Reads (no writes): GHL `/conversations/search`, `/conversations/{id}/messages`, `/contacts/{id}`; Supabase `revops_iclosed_bookings`.
- **2026-04-28 — VSL · WhatsApp Help Click intake workflow + `context` field.** New GHL workflow "VSL · WhatsApp Help Click — Intake & Setter Handoff" fires on WhatsApp helper click → setter chat → opportunity on setter pipeline → existing `/webhook/ghl-lead`. Webhook payload now includes a `context` custom field (e.g. `"VSL · WhatsApp Help Click — booking friction"`) explaining why the lead is showing up. `handleGHLWebhook` in [index.js](../index.js) now reads `cd.context`, threads it into Max's setter-DM briefing prompt with adjusted action guidance, and adds a `📝` line to the `#ng-sales-goats` channel post. Commit `97a3bdd`.
- **2026-04-25 — Phase 1 anomaly detection layer.** New tables `metric_observations` + `metric_baselines` in primary Supabase. Daily 6am Costa Rica cron scrapes 8 business metrics (Meta CPL, close rate, setter calls, Phase 0→1 conversion, Phase 1/2 cycle days, Day 7 at-risk count, GHL response time), recomputes 28-day rolling baselines, and DMs domain-routed roles when any metric drifts ≥1.5σ. Anomalies persist to `agent_knowledge` (category `alert`, visibility `shared`). Two new tools: `detect_anomalies` (ad-hoc dry-run) and `query_metric_history`. Bootstraps with 7-day warmup window — silently skips metrics with sample_size < 7. Routing constant `ANOMALY_ROUTING` in [index.js](../index.js) at the top of the anomaly block.
- **2026-04-24 — Team-wide pilot rollout.** Max opened to Ron + Tania + Josue + David. See git log `0700aac` and `6ebc8dc`.

## Outstanding — Two Initiatives effort

### Initiative 1 — Reaction-driven lead claim → ✅ shipped, live, verified

End-to-end loop working: ✋/✅ on a `#ng-sales-goats` lead post → GHL contact + Appointment Setting Pipeline opportunity reassigned to claimer → ✅ from Max + threaded confirmation → row written to `setter_claims` with `seconds_to_claim`. No pending work on this initiative.

### Initiative 2 — Stalled prospect auto-follow-up → 🟢 live + approval modes shipped, awaiting flip

Build complete behind `STALLED_FOLLOWUPS_MODE` env var. Detection cron (Mon–Fri 11 AM CR) still runs dry-run by default — setting `STALLED_FOLLOWUPS_MODE=approval` (recommended for the first batch) or `=live` is the deliberate go-live moment.

**Shipped 2026-05-08:**
- Migration `008_prospect_followups.sql` (audit table + cooldown/cap source of truth)
- Live-send branch in `runStalledProspectFollowups` reuses `generateCampaignDraft` (the recoverable-leads campaign generator — same single-sentence/≤25-word/conversation-language spec) and the same `POST /conversations/messages` shape used by `sendCampaignMessage`
- Send-time guardrails: business hours 10:00–18:00 CR, 14-day per-contact cooldown, 2-lifetime cap, daily cap default 3 (override via `STALLED_FOLLOWUPS_DAILY_CAP`)
- New skip gate `setter_paused` in `evaluateStalledCandidate` (reads `agent_knowledge` `setter_pref` rows)
- DM commands `pause <contactId>` / `unpause <contactId>` available to all roster setters
- Cross-post `🔔 Auto-followup sent — N prospects nudged today.` to `#ng-sales-goats` after live runs that sent ≥1
- Per-setter DM showing exactly what was sent on their behalf

**Recommended rollout:**
1. Apply `migrations/008_prospect_followups.sql` on the primary Supabase (`zbuqpdwjpxgsetduhjeo`)
2. Verify the `ng-pm-agent` PIT has `conversations.write` scope (it should — `sendCampaignMessage` already uses it)
3. Set `STALLED_FOLLOWUPS_MODE=approval` on Railway. Monday's 11 AM CR run DMs Ron each generated draft. ✅ to send (auto-writes both `campaign_sends` + `prospect_followups`, also notifies the setter), ❌ to skip, or DM `revise <contact_id>: <new text>` to edit before sending.
4. Once Ron is satisfied with draft quality across at least one batch, switch to `STALLED_FOLLOWUPS_MODE=live` (or unset and use legacy `STALLED_FOLLOWUPS_LIVE=true`). Ron's summary DM will then show `Sent: N` and any `Live-skip: cooldown_active|lifetime_cap|daily_cap_reached`.
5. After 2 weeks of clean reply quality, raise daily cap from 3 → 10 via `STALLED_FOLLOWUPS_DAILY_CAP=10`

### Items deferred from the original spec audit (v1.1 / not in scope for the two-initiatives effort)

These were called out in the spec but explicitly de-scoped or never targeted for v1:

- [ ] **Form-response block in lead post** (revenue, country, decision-maker, offer-interest) — needs GHL custom-field IDs from Ron before it can be wired.
- [ ] **10-min unclaimed safety alert** — `chat.scheduleMessage` queued at lead-post time, cancelled on claim. Useful safety net for after-hours leads.
- [ ] **5-min auto-release rule** — if no GHL activity within 5 min of claim, clear `assignedTo` and re-open the lead. Spec marked v1.1.
- [ ] **True AQC attribution** — currently leaderboard shows setter-self-reported `qualified_leads` from EOD daily. True AQC requires `revops_appointments.setter_id ↔ revops_sales_outcomes.outcome` join with confirmed outcome enum value (`AQC` literal? `Qualified`? Need to confirm with closer workflow).
- [ ] **Migrating "engagement actions" from EOD proxy to true GHL outbound message aggregation** — would require a daily sync table walking every conversation's messages.
- [ ] **End-of-month archive post** — on the 1st of each month, post the previous month's final standings before MTD numbers reset.
- [ ] **/dispute Slack command** — lightweight command for setters to flag a `Not Qualified` disposition for Ron's review.

## What this is

**Max** — an AI Slack teammate for NeuroGrowth, a single-file Node app (`index.js`, ~3000 lines) + `ghl-mcp/` sidecar. Runs as a long-lived Node process on **Railway** (auto-deploys from GitHub `main`). Slack Socket Mode.

- Repo: `neurogrowth-cr/ng-agent` on GitHub
- Runtime: Node ≥18, Slack Bolt, Claude Sonnet 4.6 via `@anthropic-ai/sdk`
- Entrypoint: `node index.js`

## Integrations

- **Slack** (Bolt + Socket Mode) — messages, @mentions, DMs, file uploads, scheduled messages
- **Claude API** — Sonnet 4.6, tool use, multi-round reasoning, vision on images/PDFs
- **OpenAI** — Whisper for audio transcription
- **Supabase** (2 projects):
  - **ng-agent main** (`zbuqpdwjpxgsetduhjeo`) — `conversations`, `agent_knowledge`, `scheduled_tasks`
  - **neurogrowth-proposals** (`xqzfhofxtmqjozowrwdo`) — portal: `client_dashboards`, `customer_activities`, `customer_activity_templates`, `flywheel_ai_onboarding`, view `v_phase0_fulfillment`
- **Google** — Gmail, Drive, Calendar (read + write), Sheets, Docs
  - OAuth token stored as `GOOGLE_TOKEN` env var in Railway
  - Scope: `gmail.readonly`, `gmail.send`, `calendar.events`, `drive.readonly`, `drive.metadata.readonly`
  - Re-auth script: `node reauth_google.js` → paste new token to Railway
- **GoHighLevel** — MCP server (`ghl-mcp/`) for pipelines, contacts, opportunities, conversations
- **Meta Ads** — account/campaign/ad-set performance
- **Notion** — search + task creation (Operations Tracking + Project Sprint Tracking)

## Domain model

- **Client lifecycle:** Phase 0 (pre-portal onboarding) → Phase 1 (optimization) → Phase 2 (campaign launch) → Phase 3 (stabilization, 20-day checkpoint) → Live → blocked-if-stuck
- **Phase 0 steps** (via `v_phase0_fulfillment.phase0_step`) — Tania owns steps 1–4, Josue takes over at step 5:
  1. `1_awaiting_signup` — no clerk_user_id, no T&C
  2. `2_awaiting_terms` — signed up, T&C not accepted
  3. `3_awaiting_form` — T&C signed, form not completed
  4. `4_awaiting_activation_call` — form done, no booking
  5. `5_ready_for_handoff` — all done, go_live_at still null → Josue kicks off Phase 1
  - Filter: `go_live_at IS NULL`
- **SLA:** 14-day activation window (Day 1 = activation call completed_at)
- **Stabilization anchor:** `stabilization_started_at` (Phase 3 Day 1)
- **Build & Release** — 14-day delivery model
- **Retention tiers** — OMEGA / ROLEX / PATEK
- **Customer types** — `flywheel-ai` / `full-service`

## Team roles

| Person | Slack ID | Owns |
|--------|----------|------|
| Ron | RON_SLACK_ID | CEO — approves all team-channel posts |
| Tania | U07SMMDMSLQ | Phase 0 (steps 1–4), SLA enforcement, Phase 3 client success + 1:1s, blocked client outreach |
| Josue | U08ABBFNGUW | Phase 0 step 5 handoff, Phase 1–2 ops, activation calls, campaign sequencing |
| Valeria | U09Q3BXJ18B | Delivery docs, Claude Projects, Phase 1 |
| Felipe | U09TNMVML3F | Campaign launches, Prosp, Phase 2 |
| Joseph | — | Setter |
| Debbanny | — | Setter |
| Jose Carranza | — | Closer |
| Jonathan Madriz | — | Closer |

## Tools (33 total)

**Portal/fulfillment:**
- `get_client_status` — all active clients phases 1–live with activity details
- `get_phase0_clients` — pre-portal pipeline from `v_phase0_fulfillment`, grouped by step
- `get_portal_alerts` — Phase 0 (OVERDUE/AT RISK/HANDOFF READY) + Phase 1/2 (blocked/overdue/at-risk), split into two sections

**Calendar (NEW — requires `calendar.events` scope):**
- `add_calendar_attendees` — add guests to existing event, Google sends invites (`chat.scheduleMessage`)
- `create_calendar_event` — create new event with attendees

**Slack:**
- `create_slack_reminder` — one-off scheduled message via `chat.scheduleMessage` (channel name, channel ID, or user ID for DM); max 120 days out

**Sales/CRM:**
- `get_sales_intelligence` — iClosed + RevOps Supabase tables (closer/setter EOD daily)
- `get_ghl_conversations` — GHL pipeline conversations with setter assignment

**Knowledge:**
- `search_knowledge`, `save_knowledge`, `get_knowledge_category`

**Google:**
- `search_gmail`, `send_email`, `get_calendar_events` (now returns event IDs in brackets), `get_recent_emails`, `search_drive`, `read_google_sheet`, `read_google_doc`

**Notion:** `search_notion`, `get_notion_page`, `create_notion_task`

**Meta Ads:** `get_meta_ads_summary`, `get_meta_campaigns`, `get_meta_adsets`, `get_meta_ads`

**Scheduled tasks admin:** `create_scheduled_task`, `list_scheduled_tasks`, `clean_duplicate_tasks`, `delete_scheduled_task`

**Slack:** `read_slack_channel`, `draft_channel_post`

## Scheduled jobs (cron — all CR timezone)

| Time | Days | Job |
|------|------|-----|
| 5:30 AM | Mon–Fri | Nightly Learning — scan 5 channels + Gmail + calendar → `agent_knowledge` |
| 8:00 AM | Mon–Fri | Proactive Team DMs (`runProactiveDMs`) — event-triggered alerts: Josue (Day 7/14 today, stalled phases), Tania (blocked, Phase 0 stuck ≥7d, Phase 3 Day 20), Valeria (Phase 1 stalled), Felipe (Phase 2 stalled) |
| 9:00 AM | Mon–Fri | Fulfillment Standup (`runFulfillmentStandup`) — personalized daily brief DMs to Josue, Valeria, Felipe, Tania |
| 2:00 PM | Monday | Gap Detection — blocked/overdue/stale clients + Phase 0 gaps; Ron approves before posting to ops channel |
| 3:00 PM + 8:00 PM | Daily | Proactive Alerts — unresolved `alert` knowledge entries >24h |
| 6:00 PM | Mon–Fri | Fulfillment EOD Pulse → `#ng-fullfillment-ops` |
| 10:30 PM | Friday | Weekly Portal Trends + Friday Delivery Wrap-Up |
| 9:00 AM | Daily | Phase 3 Day 20 Check-in Alert |

**Dynamic crons** stored in `scheduled_tasks` — validated section headers, reject + notify Ron if malformed.

## Standup DM contents (9 AM)

**Josue:** Blocked clients (with what's blocking) → Day 14 today (must launch) → Day 7 today (at risk) → Phase 0 activation calls needed + handoff-ready → pipeline snapshot.

**Valeria:** Phase 1 clients with urgency flags (Day 10+ ⚠️, Day 7+ 👀) + next pending doc activities → blocked clients to check.

**Felipe:** Phase 2 clients with urgency flags + next pending campaign activities → Phase 3 stabilization day counts.

**Tania:** Phase 0 full pipeline (all steps, Day 7+ flagged, Day 14+ overdue, step 5 = Josue handoff) → SLA watch (Day 14 due today + past-SLA clients) → Phase 3 all clients (Day 18+ = 1:1 soon, Day 20+ = 🔴 schedule now) → blocked clients for outreach.

## Fulfillment report scheduled tasks (in Supabase `scheduled_tasks`)

Both `Fulfillment EOD Pulse` and `Friday Delivery Wrap-Up` now call:
- `get_phase0_clients` in STEP 1
- Output includes `PHASE 0 — PRE-PORTAL` section (grouped by step, tags Josue `<@U07SMMDMSLQ>` if stuck >7 days)

## Architecture notes

- **Channel cache** — 10-min TTL on Slack channel list (rate limit guard)
- **Approval flow** — team-channel posts require Ron's DM approval; 30-min TTL on pending approvals
- **Thread context** — @mentions in threads fetch full thread history before responding
- **Rate limit** — per-user 10 req / 60s
- **Conversation pruning** — 40 rows per user
- **Knowledge categories** — client, team, process, decision, alert, intel
- **Deployment** — `git push origin main` → Railway auto-redeploys in ~1–2 min

## What was built today (2026-04-23)

1. **Phase 0 tool** — `getPhase0Clients()` + `get_phase0_clients` tool reading `v_phase0_fulfillment`
2. **Phase 0 fully wired** — `getPortalAlerts()` now includes Phase 0 tiers (OVERDUE/AT RISK/HANDOFF READY); `runProactiveDMs()` DMs Tania on Phase 0 stuck ≥7 days; `runMondayGapDetection()` includes Phase 0 in gap report
3. **EOD/Friday reports updated** — both scheduled task prompts now call `get_phase0_clients` and render a PHASE 0 section
4. **Slack reminders** — `createSlackReminder()` + `create_slack_reminder` tool (one-off scheduled messages, channel or DM)
5. **Google Calendar write** — `addCalendarAttendees()` + `createCalendarEvent()` tools; scope upgraded to `calendar.events`; token updated in Railway
6. **Morning standup cron** — `runFulfillmentStandup()` at 9 AM Mon–Fri, personalized DMs to Josue, Valeria, Felipe, Tania
7. **ProactiveDMs fixed** — moved from 2 AM Tue–Sat → 8 AM Mon–Fri; Josue language updated from "tomorrow" to "today"
8. **Tania standup** — comprehensive 9 AM brief: Phase 0 pipeline, SLA watch, Phase 3 with 1:1 flags, blocked clients
