# ng-agent — Project State

Last updated: 2026-08-12


> **Convention (since 2026-08-12): new entries are APPENDED at the BOTTOM of the
> "Recent changes" section (newest LAST), which is the last section of this file.
> Never prepend, never rewrite an existing entry — parallel sessions collide when
> everyone inserts at the same top line. Entries older than ~60 days get moved to
> [tasks/archive/project-state-archive.md](archive/project-state-archive.md).

## What this is

**Max** — an AI Slack teammate for NeuroGrowth, a single-file Node app (`index.js`, ~3000 lines) + `ghl-mcp/` sidecar. Runs as a long-lived Node process on **Railway** (auto-deploys from GitHub `main`). Slack Socket Mode.

- Repo: `neurogrowth-cr/ng-agent` on GitHub
- Runtime: Node ≥18, Slack Bolt, Claude Sonnet 4.6 via `@anthropic-ai/sdk`
- Entrypoint: `node index.js`

## Integrations

- **Slack** (Bolt + Socket Mode) — messages, @mentions, DMs, file uploads, scheduled messages
- **Claude API** — Sonnet 4.6, tool use, multi-round reasoning, vision on images/PDFs
- **OpenAI** — Whisper for audio transcription
- **Supabase** (2 projects):
  - **ng-agent main** (`zbuqpdwjpxgsetduhjeo`) — `conversations`, `agent_knowledge`, `scheduled_tasks`
  - **neurogrowth-proposals** (`xqzfhofxtmqjozowrwdo`) — portal: `client_dashboards`, `customer_activities`, `customer_activity_templates`, `flywheel_ai_onboarding`, view `v_phase0_fulfillment`
- **Google** — Gmail, Drive, Calendar (read + write), Sheets, Docs
  - OAuth token stored as `GOOGLE_TOKEN` env var in Railway
  - Scope: `gmail.readonly`, `gmail.send`, `calendar.events`, `drive.readonly`, `drive.metadata.readonly`
  - Re-auth script: `node reauth_google.js` → paste new token to Railway
- **GoHighLevel** — MCP server (`ghl-mcp/`) for pipelines, contacts, opportunities, conversations
- **Meta Ads** — account/campaign/ad-set performance
- **Notion** — search + task creation (Operations Tracking + Project Sprint Tracking)

## Domain model

- **Client lifecycle:** Phase 0 (pre-portal onboarding) → Phase 1 (optimization) → Phase 2 (campaign launch) → Phase 3 (stabilization, 20-day checkpoint) → Live → blocked-if-stuck
- **Phase 0 steps** (via `v_phase0_fulfillment.phase0_step`) — Tania owns steps 1–4, Josue takes over at step 5:
  1. `1_awaiting_signup` — no clerk_user_id, no T&C
  2. `2_awaiting_terms` — signed up, T&C not accepted
  3. `3_awaiting_form` — T&C signed, form not completed
  4. `4_awaiting_activation_call` — form done, no booking
  5. `5_ready_for_handoff` — all done, go_live_at still null → Josue kicks off Phase 1
  - Filter: `go_live_at IS NULL`
- **SLA:** 14-day activation window (Day 1 = activation call completed_at)
- **Stabilization anchor:** `stabilization_started_at` (Phase 3 Day 1)
- **Build & Release** — 14-day delivery model
- **Retention tiers** — OMEGA / ROLEX / PATEK
- **Customer types** — `flywheel-ai` / `full-service`

## Team roles

| Person | Slack ID | Owns |
|--------|----------|------|
| Ron | RON_SLACK_ID | CEO — approves all team-channel posts |
| Tania | U07SMMDMSLQ | Phase 0 (steps 1–4), SLA enforcement, Phase 3 client success + 1:1s, blocked client outreach |
| Josue | U08ABBFNGUW | Phase 0 step 5 handoff, Phase 1–2 ops, activation calls, campaign sequencing |
| Valeria | U09Q3BXJ18B | Delivery docs, Claude Projects, Phase 1 |
| Felipe | U09TNMVML3F | Campaign launches, Prosp, Phase 2 |
| Joseph | — | Setter |
| Debbanny | — | Setter |
| Jose Carranza | — | Closer |
| Jonathan Madriz | — | Closer |

## Tools (33 total)

**Portal/fulfillment:**
- `get_client_status` — all active clients phases 1–live with activity details
- `get_phase0_clients` — pre-portal pipeline from `v_phase0_fulfillment`, grouped by step
- `get_portal_alerts` — Phase 0 (OVERDUE/AT RISK/HANDOFF READY) + Phase 1/2 (blocked/overdue/at-risk), split into two sections

**Calendar (NEW — requires `calendar.events` scope):**
- `add_calendar_attendees` — add guests to existing event, Google sends invites (`chat.scheduleMessage`)
- `create_calendar_event` — create new event with attendees

**Slack:**
- `create_slack_reminder` — one-off scheduled message via `chat.scheduleMessage` (channel name, channel ID, or user ID for DM); max 120 days out

**Sales/CRM:**
- `get_sales_intelligence` — iClosed + RevOps Supabase tables (closer/setter EOD daily)
- `get_ghl_conversations` — GHL pipeline conversations with setter assignment

**Knowledge:**
- `search_knowledge`, `save_knowledge`, `get_knowledge_category`

**Google:**
- `search_gmail`, `send_email`, `get_calendar_events` (now returns event IDs in brackets), `get_recent_emails`, `search_drive`, `read_google_sheet`, `read_google_doc`

**Notion:** `search_notion`, `get_notion_page`, `create_notion_task`

**Meta Ads:** `get_meta_ads_summary`, `get_meta_campaigns`, `get_meta_adsets`, `get_meta_ads`

**Scheduled tasks admin:** `create_scheduled_task`, `list_scheduled_tasks`, `clean_duplicate_tasks`, `delete_scheduled_task`

**Slack:** `read_slack_channel`, `draft_channel_post`

## Scheduled jobs (cron — all CR timezone)

| Time | Days | Job |
|------|------|-----|
| 5:30 AM | Daily | Nightly Learning — scan 5 channels + Gmail + Calendar + REVI + strike-mover roll-up → `agent_knowledge` |
| 7 AM–9 PM, every 2h | Daily | Auto Strike Mover (LIVE) — advances setter pipeline cards on human WhatsApp follow-ups; silent per-run, logs `strike_sweep` to `agent_activity`, daily summary in nightly learning + 9:30 PM CR sales digest (since 2026-08-12) |
| 8:00 AM | Mon–Fri | Proactive Team DMs (`runProactiveDMs`) — event-triggered alerts: Josue (Day 7/14 today, stalled phases), Tania (blocked, Phase 0 stuck ≥7d, Phase 3 Day 20), Valeria (Phase 1 stalled), Felipe (Phase 2 stalled) |
| 9:00 AM | Mon–Fri | Fulfillment Standup (`runFulfillmentStandup`) — personalized daily brief DMs to Josue, Valeria, Felipe, Tania |
| 2:00 PM | Monday | Gap Detection — blocked/overdue/stale clients + Phase 0 gaps; Ron approves before posting to ops channel |
| 3:00 PM + 8:00 PM | Daily | Proactive Alerts — unresolved `alert` knowledge entries >24h |
| 6:00 PM | Mon, Wed | Fulfillment EOD Pulse → `#ng-fullfillment-ops` (was Mon–Fri; slimmed 2026-06-17) |
| 10:30 PM | Friday | Weekly Portal Trends + Friday Delivery Wrap-Up |
| 9:00 AM | Daily | Phase 3 Day 20 Check-in Alert |

**Dynamic crons** stored in `scheduled_tasks` — validated section headers, reject + notify Ron if malformed.

**Deactivated 2026-06-17** (cadence slim-down, `active=false` in `scheduled_tasks`, easily restorable):
- `Blocked Client Report — MWF` (was Mon/Wed/Fri 8 AM) — note: Friday Wrap-Up still points aged >14d-no-activity blocks here, so those no longer surface anywhere until/unless the Friday prompt's block-filtering rule is relaxed.
- `Phase 0 Aging Alert` (was Mon/Wed/Fri 8 AM) — Phase 0 aging now surfaces only in EOD (Mon/Wed) + Friday Wrap-Up.

## Standup DM contents (9 AM)

**Josue:** Blocked clients (with what's blocking) → Day 14 today (must launch) → Day 7 today (at risk) → Phase 0 activation calls needed + handoff-ready → pipeline snapshot.

**Valeria:** Phase 1 clients with urgency flags (Day 10+ ⚠️, Day 7+ 👀) + next pending doc activities → blocked clients to check.

**Felipe:** Phase 2 clients with urgency flags + next pending campaign activities → Phase 3 stabilization day counts.

**Tania:** Phase 0 full pipeline (all steps, Day 7+ flagged, Day 14+ overdue, step 5 = Josue handoff) → SLA watch (Day 14 due today + past-SLA clients) → Phase 3 all clients (Day 18+ = 1:1 soon, Day 20+ = 🔴 schedule now) → blocked clients for outreach.

## Fulfillment report scheduled tasks (in Supabase `scheduled_tasks`)

Both `Fulfillment EOD Pulse` and `Friday Delivery Wrap-Up` now call:
- `get_phase0_clients` in STEP 1
- Output includes `PHASE 0 — PRE-PORTAL` section (grouped by step, tags Josue `<@U07SMMDMSLQ>` if stuck >7 days)

## Architecture notes

- **Channel cache** — 10-min TTL on Slack channel list (rate limit guard)
- **Approval flow** — team-channel posts require Ron's DM approval; 30-min TTL on pending approvals
- **Thread context** — @mentions in threads fetch full thread history before responding
- **Rate limit** — per-user 10 req / 60s
- **Conversation pruning** — 40 rows per user
- **Knowledge categories** — client, team, process, decision, alert, intel
- **Deployment** — `git push origin main` → Railway auto-redeploys in ~1–2 min

## What was built today (2026-04-23)

1. **Phase 0 tool** — `getPhase0Clients()` + `get_phase0_clients` tool reading `v_phase0_fulfillment`
2. **Phase 0 fully wired** — `getPortalAlerts()` now includes Phase 0 tiers (OVERDUE/AT RISK/HANDOFF READY); `runProactiveDMs()` DMs Tania on Phase 0 stuck ≥7 days; `runMondayGapDetection()` includes Phase 0 in gap report
3. **EOD/Friday reports updated** — both scheduled task prompts now call `get_phase0_clients` and render a PHASE 0 section
4. **Slack reminders** — `createSlackReminder()` + `create_slack_reminder` tool (one-off scheduled messages, channel or DM)
5. **Google Calendar write** — `addCalendarAttendees()` + `createCalendarEvent()` tools; scope upgraded to `calendar.events`; token updated in Railway
6. **Morning standup cron** — `runFulfillmentStandup()` at 9 AM Mon–Fri, personalized DMs to Josue, Valeria, Felipe, Tania
7. **ProactiveDMs fixed** — moved from 2 AM Tue–Sat → 8 AM Mon–Fri; Josue language updated from "tomorrow" to "today"
8. **Tania standup** — comprehensive 9 AM brief: Phase 0 pipeline, SLA watch, Phase 3 with 1:1 flags, blocked clients

## Call cancelled/rescheduled Slack alerts (2026-07-31, verified 2026-08-02)

Replaces the iClosed-native cancel/reschedule notifications lost at the 2026-07-23 GHL cutover. Booked alerts were already rebuilt (Make scenario 5148796 `[PROD] GHL Appt Booked → CAPI + Slack Alert`); this adds the missing two.

**Path:** GHL workflows ("Outcome: Cancelled", "Neurogrowth Dashboard Webhook") → dash.neurogrowth.io webhook → Supabase `ghl_webhook_deliveries` (project `xqzfhofxtmqjozowrwdo` / neurogrowth-website) → **DB trigger `trg_ng_appt_change_notify`** (fn `ng_appt_change_notify()`, pg_net) → Make hook 2642038 → Make scenario **5822303 `[PROD] GHL Appt Cancelled/Rescheduled → Slack Alert`** → #ng-sales-goats (C0AJANQBYUE).

- **Cancelled** (🔴): `customData.ng_event = call_cancelled` OR `calendar.appoinmentStatus = cancelled`; "(by client)" vs "(by team)" from `last_updated_by_meta`. One alert per appointment (dedupe table `ng_appt_slack_alerts`).
- **Rescheduled** (🟠): appointment event whose `startTime` differs from the latest prior event for the same `appointmentId` (expression index added). Shows old → new time. One alert per (appointment, new start).
- Trigger is wrapped in a catch-all exception — alerting can never block dash ingestion. If Make hook is down, pg_net fire-and-forget = alert lost (no retry) — check `net._http_response`.
- Known quirks: a reschedule ALSO re-fires the booked alert (GHL re-triggers the booked workflow — that's what happened with Ricardo Jiménez's "booked" alert on 7/31); ~~dash normalizer still marks reschedule payloads `unknown_event_shape`~~ → fixed 2026-08-03, see below.

## Dash normalizer: GHL reschedule/cancel ingestion (2026-08-03, PR #20 MERGED + deployed)

dash repo `fix/ghl-reschedule-normalizer` → [PR #20](https://github.com/neurogrowth-cr/dash.neurogrowth.io/pull/20), merged to main (`dd92557`), Vercel production deploy succeeded 2026-08-04 00:11 UTC.

**What it fixes:** calendar-shape GHL payloads (no `type`, no `customData.ng_event` — sent by the "Neurogrowth Dashboard Webhook" workflow) were all skipped as `unknown_event_shape` (39+ rows), so rescheduled call times went stale in `revops_appointments`. Now they classify from `calendar.appoinmentStatus`, and a provisional `call_booked` flips to `ghl.call_rescheduled` when delivery history for the same `appointmentId` shows a different prior `startTime` (also catches GHL's booked-workflow re-fires on reschedule).

**Traps encoded in the fix (don't regress):**
1. `calendar.id` = the CALENDAR's id, not the appointment's — always use `calendar.appointmentId`.
2. Webhook `calendar.startTime` is NAIVE LOCAL time in `calendar.selectedTimezone`; API-hydrated rows store UTC. Both normalize paths convert via `naiveLocalToUtcIso()`; the history comparison uses raw payload startTimes (format-stable).
3. `upsertAppointmentFromEvent` used to null-wipe `setter_id`/`closer_id` when an event carried no owner fields — now only writes owners when non-null (or on insert). Protects leaderboard attribution.

**Data backfill (one-time, 2026-08-03):** 11 `revops_appointments.scheduled_start` rows corrected from delivery history (real reschedules that had never applied, e.g. Jul 28 → Aug 14). Side effect worth remembering: stale times made the Aug 3 DAILY CALL ROSTER say "no calls" when Ricardo Jiménez had a 2 PM call that day.

**Verification:** 222/222 vitest pass (5 new tests + fixture `calendar-shape-update.json`); tsc clean on touched files; next real reschedule should show `normalized_event_type='ghl.call_rescheduled'`, `status='applied'`.

## Appointment-alert hardening (2026-08-04): booked dedupe + delivery retry

**Booked-alert dedupe** — a reschedule no longer double-alerts (GHL re-fires the booked workflow on reschedule). Make scenario 5148796 now calls Supabase RPC `ng_register_booked_alert(appt_id)` (PostgREST, anon key, fail-open) before its Slack module; the Slack post is filtered out only when the RPC returns literal `false` (appointment already alerted). Dedupe state lives in `ng_appt_slack_alerts` kind='booked' (backfilled for all pre-existing GHL appointments). CAPI event_ids in that scenario fall back to `calendar.appointmentId` when the `appointment_id` merge tag is empty.

**Delivery retry (outbox)** — cancel/reschedule alerts from `ng_appt_change_notify()` no longer fire-and-forget: the trigger writes `ng_alert_outbox` (body + pg_net request_id) and pg_cron job `ng-alert-outbox-sweep` (*/5 min, website project) marks 2xx delivered, retries failures/timeouts/lost responses (≤6 attempts over 48h), purges rows >14d. A Make outage now delays alerts instead of losing them. Monitor: `select * from ng_alert_outbox where not delivered`.

**iclosed_* migration debt** — verified CLOSED by the post-cutover truth pass; remaining `iclosed_*` code refs are legacy-row reads + the frozen `getNonFlywheelCallIds` memo (deletable ~2026-11-20).

**Ops caution (Make blueprint edits):** validate modules with `validate_module_configuration` before `scenarios_update` on live scenarios, and re-check `isActive` after — an invalid blueprint bricked scenario 5148796 for ~7 min on 2026-08-04 (see tasks/lessons.md).


## Outstanding — Two Initiatives effort

### Initiative 1 — Reaction-driven lead claim → ✅ shipped, live, verified

End-to-end loop working: ✋/✅ on a `#ng-sales-goats` lead post → GHL contact + Appointment Setting Pipeline opportunity reassigned to claimer → ✅ from Max + threaded confirmation → row written to `setter_claims` with `seconds_to_claim`. No pending work on this initiative.

### Initiative 2 — Stalled prospect auto-follow-up → 🟢 live + approval modes shipped, awaiting flip

Build complete behind `STALLED_FOLLOWUPS_MODE` env var. Detection cron (Mon–Fri 11 AM CR) still runs dry-run by default — setting `STALLED_FOLLOWUPS_MODE=approval` (recommended for the first batch) or `=live` is the deliberate go-live moment.

**Shipped 2026-05-08:**
- Migration `008_prospect_followups.sql` (audit table + cooldown/cap source of truth)
- Live-send branch in `runStalledProspectFollowups` reuses `generateCampaignDraft` (the recoverable-leads campaign generator — same single-sentence/≤25-word/conversation-language spec) and the same `POST /conversations/messages` shape used by `sendCampaignMessage`
- Send-time guardrails: business hours 10:00–18:00 CR, 14-day per-contact cooldown, 2-lifetime cap, daily cap default 3 (override via `STALLED_FOLLOWUPS_DAILY_CAP`)
- New skip gate `setter_paused` in `evaluateStalledCandidate` (reads `agent_knowledge` `setter_pref` rows)
- DM commands `pause <contactId>` / `unpause <contactId>` available to all roster setters
- Cross-post `🔔 Auto-followup sent — N prospects nudged today.` to `#ng-sales-goats` after live runs that sent ≥1
- Per-setter DM showing exactly what was sent on their behalf

**Recommended rollout:**
1. Apply `migrations/008_prospect_followups.sql` on the primary Supabase (`zbuqpdwjpxgsetduhjeo`)
2. Verify the `ng-pm-agent` PIT has `conversations.write` scope (it should — `sendCampaignMessage` already uses it)
3. Set `STALLED_FOLLOWUPS_MODE=approval` on Railway. Monday's 11 AM CR run DMs Ron each generated draft. ✅ to send (auto-writes both `campaign_sends` + `prospect_followups`, also notifies the setter), ❌ to skip, or DM `revise <contact_id>: <new text>` to edit before sending.
4. Once Ron is satisfied with draft quality across at least one batch, switch to `STALLED_FOLLOWUPS_MODE=live` (or unset and use legacy `STALLED_FOLLOWUPS_LIVE=true`). Ron's summary DM will then show `Sent: N` and any `Live-skip: cooldown_active|lifetime_cap|daily_cap_reached`.
5. After 2 weeks of clean reply quality, raise daily cap from 3 → 10 via `STALLED_FOLLOWUPS_DAILY_CAP=10`

### Items deferred from the original spec audit (v1.1 / not in scope for the two-initiatives effort)

These were called out in the spec but explicitly de-scoped or never targeted for v1:

- [ ] **Form-response block in lead post** (revenue, country, decision-maker, offer-interest) — needs GHL custom-field IDs from Ron before it can be wired.
- [ ] **10-min unclaimed safety alert** — `chat.scheduleMessage` queued at lead-post time, cancelled on claim. Useful safety net for after-hours leads.
- [ ] **5-min auto-release rule** — if no GHL activity within 5 min of claim, clear `assignedTo` and re-open the lead. Spec marked v1.1.
- [ ] **True AQC attribution** — currently leaderboard shows setter-self-reported `qualified_leads` from EOD daily. True AQC requires `revops_appointments.setter_id ↔ revops_sales_outcomes.outcome` join with confirmed outcome enum value (`AQC` literal? `Qualified`? Need to confirm with closer workflow).
- [ ] **Migrating "engagement actions" from EOD proxy to true GHL outbound message aggregation** — would require a daily sync table walking every conversation's messages.
- [ ] **End-of-month archive post** — on the 1st of each month, post the previous month's final standings before MTD numbers reset.
- [ ] **/dispute Slack command** — lightweight command for setters to flag a `Not Qualified` disposition for Ron's review.

## Recent changes

<!-- APPEND new entries at the BOTTOM. Oldest first, newest LAST. Pre-2026-06-13 entries: tasks/archive/project-state-archive.md -->

- **2026-07-06 — New setter onboarded: Sebastian Serrano (Slack `U0BFA4SRVQC`, GHL `Wdjte1temxfR0lpi5RGV`, `sebastian.neurogrowth@gmail.com`).** Triggered by a live symptom — the iClosed setter-reveal for a Mali Arroyo booking posted "setter unmapped — check GHL" because `resolveSalesMember()` didn't recognize his GHL `assignedTo` ID. Root cause confirmed via live GHL lookup (`ghl_contact_search` → `assignedTo: Wdjte1temxfR0lpi5RGV` → `/users/{id}` → "Sebastian Serrano"). **Code ([index.js](../index.js), mirrors the Oscar/William 2026-05-03 pattern):** added to `TEAM_MEMBERS` (role `setter`, auto-grants `ROLE_PERMISSIONS.setter` + email-proxy autosend bypass), system-prompt team roster + EOD-reports line + closer-prompt line, `SALES_TEAM_MAP`, `GHL_USER_NAMES`, `GHL_USERS` / `GHL_USERS_GAP` / `ghlUserNames` (3 duplicate local copies), `GHL_TO_SLACK`, `SLACK_TO_GHL_USER`, `EMAIL_TO_GHL_USER_ID`, `SETTER_BY_SLACK_ID` (leads-today attribution), and the sales-standup DM `setters` array. **Supabase `scheduled_tasks` (2 rows, direct UPDATE):** `Sales EOD Report` (`8fa09ed3`) — setter Slack-tag line now includes Sebastian; `Daily iClosed Call Roster` (`7873fe73`) — setter-labeling rule now includes Sebastian. `Weekly Closer Comparison` (`83c84032`) needed no edit — fully data-driven from `getSetterWeeklyStats`/`setter_attributions`, no hardcoded roster. No `ACTIVE_SETTERS`/`SETTER_GHL_IDS` array exists anymore (confirmed via grep) — that leaderboard-roster pattern was retired in the 2026-06-02 attribution rewrite. **Verified:** `node --check index.js` OK. **Portal Supabase (`xqzfhofxtmqjozowrwdo` — same MCP token, mislabeled "neurogrowth-website" in the project list, actually hosts `admin_users`/`revops_*`):** inserted `admin_users` row for `sebastian.neurogrowth@gmail.com` (`revops_can_set: true, revops_can_close: false`), mirroring Oscar/William exactly — `id` has no FK to `auth.users`, so this is a standalone attribution flag, not a portal login. **Side fix (same table):** noticed `gerald.neurogrowth@gmail.com` (added 2026-07-08, before this session) had `revops_can_set: true, revops_can_close: true` — Ron confirmed Gerald is fulfillment, not sales, so both flags flipped to `false` to match the other fulfillment rows (Valeria/Josue/David). **Deployed:** commit `587a943` pushed to `main`; Railway auto-redeploys from there. `reauth_google.js` (pre-existing unrelated local edit) was intentionally left out of this commit.
- **2026-07-08 — Fulfillment EOD Pulse: fixed sales/marketing leak + gave anomalies their own header.** Ron flagged the Monday 7/6 pulse (posted to `#ng-fullfillment-ops`) for two defects: (1) it included a "June launch list" recap and a "Prosp campaign stop alert" — sales/marketing content with no place in a fulfillment-only report; (2) the Phase 2 cycle-time anomaly (p50=27 vs baseline 18) was buried as a floating sentence inside `BLOCKERS` instead of its own section. **Root cause (1):** every scheduled report gets a `clientCtxBlock` injected ([index.js:1065-1074](../index.js)) — the last 7 days of `agent_knowledge` rows tagged `category='client'`, with no domain filtering. `runNightlyLearning` ([index.js:3619](../index.js)) populates those rows nightly by scanning 5 Slack channels *including* `ng-sales-goats` into one combined digest, extracting "client status changes... who launched" with no domain tag — so sales/marketing content saved under `category='client'` silently re-surfaces in every report's context, fulfillment included. Reworking the knowledge pipeline to tag/filter by domain end-to-end would touch 5+ report prompts and a schema change — disproportionate to one report's leak and risky for the others. **Root cause (2):** the BLOCKERS section instructions literally said "if the PRECOMPUTED FULFILLMENT ANOMALIES block contains triggered anomalies, list each one here too" — a prompt-level instruction bug, not a data bug (the anomaly data itself was already correctly fulfillment-scoped via `METRIC_REGISTRY.filter(m => m.domain === 'fulfillment')` at [index.js:1105](../index.js)). **Fix:** rewrote the `Fulfillment EOD Pulse` prompt (Supabase `scheduled_tasks`, ng-agent project `zbuqpdwjpxgsetduhjeo`) with (a) a SCOPE GUARD instructing Max to exclude sales pipeline data, leads, ad/marketing campaign alerts, deal stages, and sales-sourced launch recaps even if they appear in `search_knowledge`/`read_slack_channel`/the injected client-context — "when in doubt, exclude it"; (b) restructured output from 3 to 4 sections: `WINS TODAY`, `BLOCKERS`, `ANOMALIES` (new, dedicated), `TOMORROW` — anomalies moved out of `BLOCKERS` into their own header, same precomputed data block, same fallback text ("No fulfillment anomalies triggered in the last 24h.") for the untriggered case. **Code ([index.js:1191](../index.js)):** added `'ANOMALIES'` to `TASK_HEADERS['Fulfillment EOD Pulse']` so the structural whitelist guard enforces the new section on every run, not just today. Comment above the precompute block ([index.js:1099-1102](../index.js)) updated to describe the new section split. **Verified:** `node --check index.js` OK; Supabase row read back to confirm the new prompt saved; Railway deployment `d840213b` from commit `42d2aca` confirmed `SUCCESS`. **Not yet exercised live** — next scheduled run (Mon/Wed 12:00 PM CR) is the first real test; worth eyeballing the Ron-DM approval draft for a clean WINS TODAY and a distinct ANOMALIES header.
- **2026-07-09 — Gerald Arias onboarded to Max's roster; fixed fulfillment-role persona bug.** Gerald posted his daily follow-up report in `#ng-marktech-internal` and Max bounced him: "You're not on Max's roster yet — ping Ron and he'll add you." Root cause: Gerald (`gerald.neurogrowth@gmail.com`) was already flagged `fulfillment` in the portal `admin_users` table on 2026-07-08 (see prior entry) but was never added to `TEAM_MEMBERS` in `index.js`, so `isRosterMember()` ([index.js:453](../index.js)) rejected him. **Fix ([index.js](../index.js)):** added `'U0BAAC0KS82': { name: 'Gerald', role: 'fulfillment', displayName: 'Gerald Arias NG' }` to `TEAM_MEMBERS`; added his line to the "THE TEAM" system-prompt roster. **Side bug found and fixed:** the `fulfillment` role had exactly one member (Valeria) since inception, so three prompt strings hardcoded her name instead of templating off `member` — Gerald becoming the 2nd fulfillment member would have had Max address him as "Valeria" or greet Gerald with "Good morning Valeria!" Fixed all three to be name-generic (mirrors how the `setter` role already handles multiple members): the DM persona text (`roleContext.fulfillment`), the `#ng-pm-agent` channel-join greeting (`roleIntros.fulfillment`), and the proactive fulfillment standup DM greeting in `runFulfillmentStandup` (previously built one static "Good morning Valeria!" string reused for every recipient in the role — now looks up each recipient's name via `getMemberContext(id)` inside the send loop). **Verified:** `node --check index.js` OK. **Deployed:** commit `725fe6e` pushed to `main`; Railway deployment `c6fafeca` confirmed `SUCCESS`, logs show clean boot (health check + Socket Mode + all 9 dynamic crons registered), no crash. Replied in the original Slack thread telling Gerald to retry. **Not yet exercised live** — worth confirming his next `#ng-pm-agent` join or DM greets him correctly, and that tomorrow's fulfillment standup (9 AM CR) DMs both Valeria and Gerald with their own names.
- **2026-07-09 — Disabled "Sales Call Prep Reminder"; fixed timezone bug in Daily iClosed Call Roster's "today calls" logic.** Two related requests in one session, both around sales-call reporting. **(1) Spam complaint.** Ron flagged the daily `#ng-pm-agent` DM asking him to "confirm the prospect research briefs are ready" as noise, not value. Identified as the `Sales Call Prep Reminder` scheduled task (Supabase `scheduled_tasks` id `74791f53-3533-481b-b6cd-959671663461`, cron `35 18 * * 1-5` = 12:35 PM CR weekdays, `#ng-pm-agent`) — it only ever checked *Ron's* calendar and pinged Ron, it never reached the closers. Soft-disabled via `UPDATE scheduled_tasks SET active = false WHERE id = '74791f53...'` (row preserved, easy to re-enable). Confirmed via Railway boot logs that it no longer registers as one of the 8 active dynamic crons. **Follow-up finding:** audited whether closers (Jose, Jonathan) have their own call reminders — they don't. The only thing closers see is the shared `Daily iClosed Call Roster` post in `#ng-sales-goats` (8:30 AM CR) and real-time iClosed booking notifications; no personal DM reminder exists. Not built (Ron didn't ask for it) — flagged as a possible future addition. **(2) Timezone bug.** Ron separately reported the Daily Call Roster sometimes shows the wrong day's calls. Root cause: `getSalesIntelligence()`'s "TODAY'S CALLS" branch ([index.js:2619-2628](../index.js), pre-fix) computed `todayStart`/`todayEnd`/`todayStr` (and `weekStart`/`monthStart` below it) using server-local `Date` methods (`new Date()`, `.setHours(0,0,0,0)`, `.toISOString()`) — these run in the container's system timezone (Railway defaults to UTC, no `TZ` env var set), not Costa Rica (UTC−6). This shifts the "today" window ~6 hours early relative to CR's actual calendar day: appointments scheduled CR-yesterday-evening (6PM–midnight) were wrongly counted as "today," while appointments scheduled CR-this-evening (6PM–midnight) were wrongly dropped and would appear as tomorrow's. Notably, the identical bug class was already fixed 20 lines above in the same function for the "LEADS TODAY" branch (explicit CR-anchored boundaries via `toLocaleDateString('en-CA', {timeZone: 'America/Costa_Rica'})`) — the "today calls" branch and the week/month boundaries just never got the same treatment. **Fix ([index.js:2619-2634](../index.js)):** rewrote `toDateStr` to use `toLocaleDateString('en-CA', {timeZone: 'America/Costa_Rica'})`; `todayStart`/`todayEnd` now built from the CR calendar-date string as explicit UTC instants (`${todayStr}T06:00:00Z` = CR midnight); `weekStart` derived via day-of-week computed from the CR date string (`getUTCDay()` on a date parsed from the CR-date string, immune to server TZ); `monthStart`/`monthStartStr` derived the same way from CR year/month. All three boundaries (day/week/month) now hardcode `America/Costa_Rica` and no longer depend on the container's local timezone at all. **Verified:** `node --check index.js` OK; standalone simulation confirmed a CR-7PM call correctly falls inside "today" and a CR-yesterday-evening call correctly falls outside it (old logic would have gotten both wrong, direction depending on server TZ). **Deployed:** commit `2268546` pushed to `main`; Railway deployment `60194a0f` confirmed `SUCCESS`, clean boot log (8 dynamic crons registered, `Sales Call Prep Reminder` correctly absent). **Confirmed live (2026-07-11):** cross-checked the Jul 11 `DAILY CALL ROSTER` Slack post against raw `revops_appointments` — all 6 calls matched exactly, including two evening CR calls (Gerardo Mendoza 6PM, Mauricio Villagra 7PM) that land in the next UTC calendar day and would have been silently dropped under the pre-fix logic. Direct before/after proof the fix works. `Sales Call Prep Reminder` also reconfirmed `active: false` in Supabase with no reminder DMs since disabling.
- **2026-07-09 — Call-prep DM: iClosed intake fallback extended to setter-booked calls; dead "GHL conversation not found" placeholder removed.** Follow-up to the same-day Call Prep work below. Ron shared a live example: William's setter-booked Carolina Babbar call showed `👤 Booked by: William B` but the GHL CONVERSATION HISTORY section just printed "GHL conversation not found for this prospect." with nothing underneath — dead weight in the DM. Root cause: `runSalesCallPrep` ([index.js:5711](../index.js)) only ever tried the iClosed booking-intake fallback (the prospect's own Q&A answers from `iclosed_webhook_deliveries`, via `fetchIClosedIntakeForProspect`) inside the `setterInfo.source === 'vsl'` branch — a leftover from the 2026-05-26 fix which was scoped narrowly to self-bookings. Setter-booked calls (`source === 'appointment-setting'`) that hit a GHL contact with no conversation thread had no fallback at all, just the sentinel string. Ron's ask: either make the summary real, or show nothing. **Fix ([index.js:5757-5838](../index.js)):** `convoSection` now starts `null` instead of a placeholder string. The iClosed intake fallback block moved out of the `vsl`-only branch and now runs whenever `convoSection` is still empty after the GHL lookup, regardless of booking source — reuses the already-fetched `intake` object when the `vsl` branch fetched it, otherwise fetches once. Brief assembly (`briefLines`) now only pushes the `*GHL CONVERSATION HISTORY:*` header + content when `convoSection` is truthy; if both GHL and iClosed intake come up empty, the section is omitted from the DM entirely — no more dead line. **Guardrail preserved:** the `sourceLine` self-booked-vs-setter-unknown determination for `vsl` calls is untouched — still derived from the iClosed booking payload (`call_booked_from`, `event.setter`), not from whether a GHL contact exists, because most self-booked leads still get a GHL contact via the ad pipeline and using contact-presence as the signal would reintroduce the VSL mislabeling bug fixed 2026-05-08/13/26 (caught and corrected mid-session before committing — first draft of this fix made that exact mistake). **Verified:** `node --check index.js` OK; standalone brief-assembly simulation confirmed clean output for both "nothing found → section omitted" and "intake fallback renders" cases, correct blank-line spacing. **Deployed:** commit `6f4a13b` pushed to `main`; Railway deployment `e922a9c9` confirmed `SUCCESS`, clean boot log (8 dynamic crons registered, no crash). **Confirmed live (2026-07-11):** found the real DM in Ron's own history — CALL PREP for Oscar Vizcaino Arroyo (Jul 10, 9:00 AM CR, William-booked, no GHL conversation thread) rendered the iClosed intake fallback correctly: `📋 No GHL message history — here's what they shared when booking:` followed by the prospect's real Q&A answers (business type, ICP, timeline) and booking metadata, instead of the old dead "not found" line. Working as designed.
- **2026-07-14/15 — Added stale unclaimed-lead alerting (2h nag + daily digest to Ron); caught + fixed a silent-failure bug in it same-day.** Investigating a "Failed" GHL webhook execution (unrelated Make/GHL debugging session) surfaced a real incident: a Facebook lead (Adrian RM) sat unclaimed in `#ng-sales-goats` for **11 days** (Jul 2 → Jul 13) before anyone reacted — the claim flow (setter reacts ✅ → GHL reassignment) had zero time-based fallback if nobody reacted. Confirmed via full-file audit: no cron, no elapsed-time check, nothing tied to unclaimed-lead age existed anywhere. **Shipped ([index.js:6446-6604](../index.js), commit `dd1fb22`):** `getUnclaimedLeads(sinceMs)` — shared query reusing the leads-today report's two-query-then-diff-in-JS join pattern (`lead_posts` vs `setter_claims`, grouped by `slack_message_ts` to correctly handle the FB→WhatsApp dup-contact path); `runStaleLeadNagCheck` — every 30 min, 7am–9pm CR, posts one threaded reminder per lead the first time it crosses 2h unclaimed (one-shot via `agent_knowledge` dedupe key `stale-lead-nag:<contact_id>`, never repeats), tagging the `@setters` Slack user group resolved at runtime via `usergroups.list()` (not hardcoded — falls back to `<!channel>` + loud warning if the group can't be found); `runStaleLeadDailySweep` — 6pm CR daily, ONE DM to Ron listing every lead still unclaimed, oldest-first, persists on the list every day until claimed (no "seen it once" suppression — that persistence is the actual point). Both modeled directly on the existing `runUnloggedOutcomeReminders` shape (dedupe pattern, Ron-DM-on-cron-failure try/catch, `America/Costa_Rica` cron timezone convention). **Live-verified same day via `agent_activity` + Railway logs, found 2 real issues:** (1) **Slack bot token is missing the `usergroups:read` OAuth scope** — `usergroups.list()` throws `missing_scope`, so nags are currently falling back to `<!channel>` instead of tagging `@setters`. Needs Ron to add the scope in Slack's app config + reinstall/reauthorize the app (admin-only, not fixable via API). First tick nonetheless correctly nagged a backlog of 14 already-stale leads that predated this deploy (8 at 23:00 UTC, 6 at 00:00 UTC) — worth eyeballing `#ng-sales-goats`. (2) **Real bug, caught and fixed same day (commit `7ff5b68`):** at 00:00 UTC four crons landed on the same tick (hourly call prep, the two new stale-lead jobs, setter attribution reconcile) and the daily sweep's `setter_claims` query hit a transient `fetch failed` under the concurrent load. `getUnclaimedLeads` originally caught that and returned `[]`, which is indistinguishable from "genuinely nothing unclaimed" — the sweep logged "nothing unclaimed" and sent Ron no alert, silently masking exactly the kind of failure this feature exists to catch. Fixed: both error paths now `throw` instead, so the caller's existing try/catch (already wired to DM Ron on any thrown error) actually fires. Redeployed, clean startup confirmed. **Both crons confirmed running clean going forward** (`agent_activity` `cron_run` status `ok` at 23:00/23:30/00:00 UTC). **Open item:** the `usergroups:read` scope fix above is the only thing between this and fully matching spec.
- **2026-07-21 — Max ↔ REVI cross-agent integration, wave 1 (hub decision executed): `reviSupabase` client, `get_revi_intelligence` tool, call-prep REVI INTEL section, REVI health-heartbeat cron.** Context: Ron decided Max is the **hub** over the sibling agents (Kai, REVI) — the July 1 audit's open "hub vs one-of-many" question. Key enabling fact: REVI (`~/ng-revi`, sales-call coaching + leadership initiative tracking, Fathom-fed) lives in a dedicated **`revi` schema inside Max's own Supabase project** (`zbuqpdwjpxgsetduhjeo`; PostgREST already exposes `public, graphql_public, revi`), so cross-agent access is just a schema-scoped client — no new env vars, no new infra. **(1) Plumbing ([index.js:24-32](../index.js)):** `reviSupabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY, { db: { schema: 'revi' } })`, read-only by convention. **(2) REVI DATA ACCESS section ([index.js](../index.js), just above `METRIC_REGISTRY`):** ALL `revi.*` queries live in this one section (schema-coupling firewall — REVI can migrate its schema and breakage is findable in one place; every caller is try/caught non-fatal so surfaces degrade to pre-REVI output). Helpers: `reviFindCallsByProspect` (scores + `prospect_signals` jsonb + closer embed), `reviGetCoachingSummary` (per-closer avg score/outcomes + latest coaching focus — `forCloserEyes` flag switches Ron-only `teardown_text` vs closer-safe `closer_draft_text`; **never route teardowns to closers**), `reviGetRecentDeals` (won/lost tags + loss reasons, **never** `transcript_full` — token bomb), `reviGetOpenInitiatives` (open initiatives + last-2 leadership meeting digests, `days_since_movement` computed). **(3) One new tool, Ron-gated:** `get_revi_intelligence(topic: coaching|initiatives|deals|prospect|scoreboard, query, days)` — added to `TOOLS`, `dispatchTool`, and `RON_ONLY_TOOLS` (teardowns + initiative data are leadership material). This is the CEO-assistant surface: "what initiatives are open?", "why are we losing deals?". Deliberately ONE tool, not five (audit: 13/39 tools never invoked). **(4) Call-prep enrichment (`runSalesCallPrep`):** new `*REVI INTEL:*` section in the closer DM — prior REVI-scored calls for the same prospect email (score, outcome, buying-signal strength, objection, timeline, DM-status + coaching doc link) plus the closer's own current coaching focus (closer-safe `closer_draft_text`, first-name closer match). Non-fatal: brief still sends if `revi.*` is unreachable; section omitted when empty (same pattern as `convoSection`). **(5) REVI health heartbeat (`runReviCrossChecks`, cron `30 6 * * 2-6` CR via `wrapCronJob`):** alert-only, silent when healthy. Check 1: no scored calls in ≥2 business days while sales calls WERE held → DM Ron (this is exactly the Jul 16 Fathom team-sharing silent failure, which blinded REVI's ingestion with zero alerts). Held-calls gate reads `revops_appointments` via `_countSalesCallsSince()` — **deliberately NOT** the `iclosed_calls_held_yest` metric (zombie — months of zeros, audit INS-09; live check confirmed 31 appointments in 7d while the metric summed 0). `_countSalesCallsSince` is THE single swap point for the iClosed→GHL migration. Check 2: `coaching_runs` stuck in `draft` >24h (scored but never delivered). Dedup: `agent_knowledge` key `revi-health:<lastScoredDate>:<stuckRunDates>` — one alert per distinct condition, not per day. **Verified:** `node --check` OK; standalone read-only harness against production confirmed all 5 query shapes (88 scored calls, closer embed resolves, 13 open initiatives — one of which is literally "Migrate calendar booking from iClosed to GHL"); heartbeat simulation: real state → silent ✓, forced 5-day stall → alert fires ✓ (14 held calls detected), Fri-evening→Mon-morning weekend edge → 1 business day, no false alarm ✓ (a first-draft loop bug counting a day past the end bound was caught by this test and fixed). **Deployed:** commit `0b5dde8` pushed to `main` (rebased over another session's stale-lead alerting commits `dd1fb22`–`38b583d`; index.js merged clean, project-state conflicts hand-merged keeping all entries); Railway deployment `cae2af1a` confirmed `SUCCESS`, clean boot (health server up, 8 dynamic crons registered, no errors). First heartbeat fires Tue–Sat 6:30 AM CR; first REVI INTEL section appears on the next call-prep DM whose prospect has REVI-scored history. **Sequenced next (per approved plan `~/.claude/plans/https-app-notion-com-p-max-agent-build-o-warm-kernighan.md`):** C1 coverage cross-check (built with the GHL migration read-path, doubles as cutover verification), C2 outcome reconciliation + derived closer EODs, weekly-recap coaching block, `public.team_roster` shared roster (REVI reads it — kills the hand-mirrored roster.js), knowledge cross-reads. **The burning priority remains the iClosed→GHL migration — iClosed dies end of July and no migration work has started.**
- **2026-07-21 — Max ↔ REVI wave 1.5: autonomous REVI cognition — nightly learning digests REVI data into agent_knowledge; sales scheduled reports get REVI coaching context.** Follow-up to wave 1 (below), answering Ron's ask: "I want Max to be able to do stuff on its own with the revi context schema" — wave 1's tool was pull-only (prompted Q&A); nothing fed REVI into Max's autonomous reasoning. **(1) `reviBuildDailyDigest(hours)`** (REVI DATA ACCESS section): compact plain-text digest of REVI activity in a window — calls scored (closer × prospect, score/100, outcome, buying-signal strength, objection + timeline capped at 180 chars each), leadership initiative movements (`initiative_updates` with embedded initiative title/owner), and processed leadership meetings with movement counts. Objective data only — **deliberately no teardown_text**, because downstream insights land in shared-visibility knowledge the whole team can search. **(2) Nightly learning** (`runNightlyLearning`): new `=== REVI ===` digest section (26h window, non-fatal on error) + extraction rule #9 in the learning prompt — recurring objections / per-closer score trends / notable outcomes → `team`/`intel`; initiative done/dropped → `decision`; repeatedly rediscussed-no-action → `alert`. This is the write-path: Max now autonomously distills REVI context into `agent_knowledge` every night, and those rows surface in every future `search_knowledge`, standup, and report. Confirmation post updated to "+ REVI". **(3) Scheduled-report context injection** (`registerDynamicCron`): `Sales EOD Report` (26h) and `Weekly Closer Comparison` (7d) get a `REVI COACHING CONTEXT` block appended to `taskDataBlock`, with explicit guardrails in the injected instructions: weave in only where it explains the sales numbers, don't turn the report into a coaching report, don't rank closers by coaching score. Non-fatal on error. **Verified:** `node --check` OK; live read-only digest test against production — 26h window returned 1 scored call, 7d window returned 13 calls + 15 initiative movements with the embed join resolving correctly; the data already shows a real distillable pattern (liquidity objection recurring across ≥5 stalled calls). **Privacy note:** nightly-learning knowledge rows are shared-visibility; digest excludes teardowns but includes per-closer scores — flagged to Ron as a deliberate tradeoff. **Deployed:** commit `050603b` pushed to `main`; Railway deployment `fac1acc4` confirmed `SUCCESS`, clean boot (8 dynamic crons registered incl. both injection targets). First autonomous REVI learning pass runs tonight 11:30 PM CR; first REVI-aware Sales EOD Report next weekday 6:30 PM; first REVI-aware Weekly Closer Comparison Monday 7 AM.
- **2026-07-21 — Nightly learning post now includes a "What I learned tonight" executive summary.** Ron's ask after seeing four count-only confirmations ("Knowledge entries saved: 21") in #ng-pm-agent: show WHAT Max learned, not just how many rows. **Change (`runNightlyLearning`):** the save loop now collects every persisted entry (`savedEntries`); after saving, one small Sonnet call (max_tokens 350) distills them into 3–6 plain bullets, priority-ordered alert → decision → client → team → intel → process, each ≤18 words, appended to the existing confirmation post as `*What I learned tonight:*`. Non-fatal: on summary-generation error the post falls back to the plain count line (no dependency added to the learning pipeline itself — knowledge writes complete before the summary runs). Entry list capped at 7,000 chars into the prompt. **Verified:** `node --check` OK; live end-to-end test of the exact prompt/params with realistic entries (incl. REVI-derived ones) rendered a clean priority-ordered post. First real firing: tonight's nightly learning run — the same run that debuts the `+ REVI` digest source, so the first sneak peek should surface REVI patterns (e.g. the recurring liquidity objection). **Post-review refinements (same commit):** per-entry 300-char cap instead of tail-truncating the entry list (guarantees every source's entries reach the summarizer — tail-truncation would have silently dropped the priority-sorted intel/process tail on heavy nights), and prompt retuned per Ron to HIGH-LEVEL blended bullets: 3–6 lines, themes over details, no metrics/counts unless essential, ≤14 words each, breadth across all sources required. **Deployed:** commit `7db4ca8` pushed to `main`; Railway deployment `5d053bd5` confirmed `SUCCESS`, clean boot. First real firing: tonight's nightly learning run (same run that debuts the REVI digest source).
- **2026-07-22 — Financial confidentiality fix: Ron's Gmail no longer feeds team-visible surfaces.** A scheduled evening report posted to a team channel leaked the iClosed $360 payment failure AND the Tech-Stack account balance — root cause: `registerDynamicCron` injected `getRecentEmails()` (Ron's unread inbox, where bank/billing notices land) into EVERY scheduled report prompt regardless of destination channel. **Rule (Ron):** company financials (balances, payment failures, billing status, invoices, card/account info) are Ron-only. **Changes (`index.js`):** (1) email live-context now injected only when `task.channel === RON_SLACK_ID` (`deliversToRonOnly` gate) — team-channel reports structurally never see inbox content; (2) team-channel report prompts carry an explicit CONFIDENTIALITY rule (defense-in-depth for financial signals arriving via Slack digests/knowledge base); (3) nightly learning gained a `confidential` extraction category — financial/billing findings are saved as `visibility='private'` knowledge under Ron's user_id (invisible to team queries; proactive-alerts cron only re-broadcasts `shared` alerts), DM'd to Ron directly, and excluded from the public "What I learned tonight" summary, whose generator prompt also forbids financial details. **Verified:** `node --check` OK; `agent_knowledge` checked — no financial entries had leaked into shared knowledge (the leak was the one Slack post only). **Deployed:** commit `369646d` pushed to `main`; Railway deployment `c10f1e9a` confirmed `SUCCESS`. First guarded runs: today's scheduled reports + tonight's nightly learning (which would otherwise have re-ingested the same unread billing email).
- **2026-07-26 — GHL-native sales refactor: Max reads GHL truth; EOD + attribution reconciler retired.** After the 2026-07-23 iClosed→GHL calendar cutover, every sales code path still keyed on `iclosed_call_id` (NULL on all GHL rows): the setter leaderboard silently attributed 0 calls and reverted to dead EOD self-reports (closer EOD 0 rows/7d, setter EOD stale Jul 22), outcome reminders never fired on GHL calls, and call-prep briefs lost booking Q&A. **Changes (6 commits `e437ae5`…`f06a6af`, `index.js`):** (1) roster — Josue Duran added (setter), Jose's GHL id typo fixed; (2) `getSetterWeeklyStats` rewritten on native `revops_appointments.setter_id` (GHL `createdBy` → roster email, populated upstream in dash — widget/closer-booked = no setter credit, per Ron); `runSetterAttributionReconcile` + 2h cron DELETED (`setter_attributions` = frozen history; migration 011 table kept); (3) `getCloserWeeklyStats` relabeled `source:'ghl'`, EOD fallback removed; unlogged-outcome reminders accept `ghl_appointment_id` and check `qualification_snapshot.ghl.cancelled`; DM copy → "log the outcome in GHL"; (4) scrapers outcome-derived (held/sales/close-rate from `revops_sales_outcomes`; setter-booked from native `setter_id`) — METRIC_REGISTRY keys unchanged, labels only; flywheel guard documented: GHL rows are sales-only by construction (dash `GHL_SALES_CALENDAR_IDS` allowlist — do NOT add a second allowlist here); (5) `get_sales_intelligence` GHL-native (native per-call setter, SETTER/CLOSER branches render weekly stats, EOD tables no longer queried), tool description + system prompt updated; (6) call prep: `fetchGhlIntakeForProspect` (Spanish qualification Q&A from `ghl_webhook_deliveries` root keys, setter via `payload.user`), native `setter_id` "Booked by" line. **Upstream (dash):** hydration now captures `createdBy.userId` → `setter_id` and `dateAdded` → `booked_at` (PR #15); 13 existing GHL rows repaired by one-off SQL (Oscar 4 / Sebastian 9). **Deployed:** Railway `39da83c3` SUCCESS, clean boot. **Watch:** first Monday leaderboard run (7am cron), 4pm outcome reminders on the Jul 23–25 unlogged GHL calls, morning call-prep briefs showing "Booked by: Oscar M/Sebastian S".
- **2026-07-28 — Leaderboard accuracy audit: three-layer data fix (Max + dash + GHL) after the Jul 27 closer leaderboard reported Ron at $0.** Ron flagged the Weekly Closer Leaderboard (Jul 20→27) as inaccurate and asked for a GHL ↔ Supabase ↔ Slack cross-check. It was: three independent defects, one per layer, each verified against raw data before fixing. **(1) Max — flywheel slug allowlist (commit `f2cb544`, Railway SUCCESS).** `SALES_FLYWHEEL_SLUGS` ([index.js:2639](../index.js)) omitted `estrategia-linkedin-flywheel-selfserving-ron`, Ron's personal iClosed event type, so `getNonFlywheelCallIds()` excluded all 4 of its calls from every sales report as "non-flywheel" — hiding **two won deals: $3,500 (Josstinne Montaño, Jul 22) and $3,895 (Jun 19)**. Every report touching those windows under-reported Ron's revenue. One-line allowlist addition. Note this is a *historical*-reporting fix: post-cutover GHL rows (`iclosed_call_id` NULL) always pass the filter by construction, so its value decays as legacy rows age out of the 120-day webhook window. **(2) dash — GHL ingestion, two bugs (commit `0f9da5a`, Vercel production READY).** (a) The sales-calendar allowlist defaulted to `['fYQJCzbk4hvV0brpJqoE']` in BOTH `src/lib/integrations/ghl/process-event.ts` and `src/app/api/integrations/ghl/webhook/route.ts` — the "LinkedIn Flywheel - Intro" calendar (`KRTGx8XteIJSCcKAShHS`, Ron's) was absent, so its bookings never entered `revops_appointments` (proof: Jessika Brown, GHL appt `3Qp3OjRslmHmbLuunedg`, booked Jul 27, no row). (b) `appointmentToEvent` in `backfill.ts` mapped GHL `assignedUserId` into the **setter** fields and never set closer — so any row created by the API backfill path landed with `closer_id` NULL (proof: Daniel Carrillo `kDfLmk4s90p3GRaygvUq`, assigned to Jose in GHL, unattributed in Supabase). Now matches webhook semantics: closer = `assignedUserId`, setter = `createdBy.userId` unless self/widget-booked. 39 GHL tests pass. **(3) GHL — workflow trigger coverage (workflow `3abff40c` v8→v9, published).** "Neurogrowth Dashboard Webhook"'s appointment trigger filtered to the main calendar only; **0 of 49 webhook deliveries ever carried the Intro calendar id** vs 19 for the main one — GHL was never *sending* Intro bookings, so no amount of dash-side allowlisting would have helped. **GHL's "In calendar" filter is SINGLE-SELECT** — the fix is a *duplicate trigger card per calendar* (multiple triggers OR into the same action), not more values in one filter. Ron added the Intro trigger + renamed both cards (the original was misleadingly named "Appointment Status Changed (All Events)" while filtering `appointment status is "confirmed"` — it is a booking capture; no-show/showed/cancelled reach dash via the separate `Outcome: *` workflows). **Data corrections (direct SQL, portal Supabase):** Jessika Brown backfilled (prospect `52b428d6` + appointment `393721a9`, closer Ron, Jul 31 19:30Z); Daniel Carrillo's `closer_id` set to `jose.neurogrowth@gmail.com`. Completeness check: the Intro calendar has had exactly **one** booking ever (Jun 1–Aug 27 window), so no hidden backlog. **Corrected week:** Ron 8 booked / 2 attended / 1 won / **$3,500** (report said 7 / 1 / $0); Jose 17 booked / 2 attended / 1 no-show / $0 (report said 16 / 2 / 0 no-shows). **Fathom cross-check — the important finding:** ~12 of Jose's 14 "pending" calls have recordings and definitely happened (Miguel Hernández, Carlos Ortiz, David Mora, Alberto Romero, Marcelo Méndez, Sara, Alonso Víquez, Edwin Ulloa, Kathy Araya, Luis Santillán, Frank Prado, Carina Borges); only Jake Vargas + Mitchell V have none. "Pending" means *outcome not logged*, not *call didn't happen* — show rate and close rate stay fiction until closers log outcomes. Verified `runUnloggedOutcomeReminders` is firing correctly (daily 22:00Z, per-call nudge counts up to 5×, 3-day escalation live) — this is a discipline gap, not a tooling gap. **Infra side-fix (permanent):** Vercel had been BLOCKING every dash deploy authored by Ron's git identity because GitHub `neurogrowth-ron` had no linked Vercel account (only `neurogrowth-cr` merges deployed). Ron linked `neurogrowth-ron` to his team-member Vercel account (`ronnyduarte-1992`) under Account → Authentication → Login Connections; verification commit `ced801b` deployed straight to READY. Direct pushes to dash main now deploy normally. **Known-unfixable via automation:** GHL's public API is list-only for workflows (`GET /workflows/{id}` → 404, no write endpoints) and the builder SPA does not render in an automation-controlled tab — all workflow edits are manual UI work. **Not yet exercised live:** no Intro booking has traversed the full chain (Jessika was hand-backfilled); the next real Intro booking is the end-to-end test, as is the Monday Aug 3 closer leaderboard.
- **2026-07-29 — Setter leaderboard split, meshed cross-system intel, and attribution moved to lead ownership.** Ron asked for setter recognition twice weekly, separate from closers, then drove three rounds of metric-semantics fixes. **(1) Schedule split:** new `Setter Leaderboard` dynamic task (`0 7 * * 2,6` CR, #ng-sales-goats) renders setters only; `Weekly Closer Comparison` (Mon 7am) is now closers-only. Validators resolve per-person headers per task. **(2) Meshed intel** in the setter precompute — GHL bookings/outcomes (truth) + `setter_claims` (the ✋ flow, which IS the #ng-sales-goats data in structured form) + REVI `closer_call_scores` avg score per setter's prospects (lead quality fed to closers) + GHL conversation hygiene (unread/stale per assigned setter). Every enrichment non-fatal — the board still posts if REVI or the GHL API is down. **(3) "0 qualified calls" friction (Ron):** AQC is a POST-CALL outcome (call happened + not disqualified), NOT booking-form qualification (~85% pass that) — it sits at 0 until closers log outcomes. Renamed to "Converted calls (AQC, closer-logged)"; pending weeks now open with claims/bookings + "outcomes pending on N call(s)" instead of a zero; prompt forbids flagging setters for pending outcomes and redirects the flag to the closer-side logging gap. **(4) Control-ordered layout (Ron):** every setter line is now OWNED (claims → calls) → SHARED (show rate, attended, no-shows) → DOWNSTREAM (AQC). Ranking moved from AQC to calls so a setter is never out-ranked on a number gated by closer follow-through. **(5) ATTRIBUTION RULE (Ron, decisive):** a call belongs to the setter who CLAIMED the prospect, regardless of whether the setter or the prospect clicked book — "they own the prospects." GHL's recorded booker is now only a fallback for calls on unclaimed leads. Dropped `Booked by them` + the booker≠claimer mismatch flag (meaningless under lead ownership, and the two-number split was what read as contradictory: "7 booked / 21 claimed→booked"). Side effect: setters now carry show rates and conversions for leads they claimed but did not personally book — including Eric Pacheco, the week's one won deal, which previously credited nobody. Distinct-lead count surfaces only when a prospect rebooked. **(6) Reminder cron scoped to fulfillable calls:** outcomes attach to a prospect's LATEST appointment via the GHL opp card, so pre-cutover rows and orphaned rows (prospect rebooked → outcome lands on the newer appt) can never be resolved and are no longer nagged; the cutover floor self-expires out of the 14d window. iClosed-BOOKED post-cutover calls STAY in scope — verified via live GHL API that those prospects hold open opps in the Appt Setting pipeline, so which tool booked the call is irrelevant to logging it. **Unlogged-outcome audit:** 36 total → **25 fulfillable** (15 GHL + 10 iClosed post-cutover), 2 orphaned, 9 pre-cutover history; list handed to Ron by closer (Jose 19, Ron 6), oldest 7 days. **Also:** `Daily iClosed Call Roster` → `Daily Sales Call Roster`; `get_sales_intelligence` description now pins pipeline provenance (GHL workflows → dash webhook → Supabase; claims tables = channel data) after Max invented a "DAVID" who built the integration. **Deployed:** Railway `1c861aea` SUCCESS (rebased onto a parallel session's strike-mover commit `f8efecb`). **Verified live:** PR #15 setter/booked_at capture working (13/13 booked_at, 8/13 setter_id — the 5 nulls are widget/closer-booked, correct by policy); REVI scored 20 calls since cutover, latest today.
- **2026-08-03 — Sales EOD Report now fires every day.** Ron: "lets have a EOD every day, I want to make sure SETTERS are getting held accountable everyday." Supabase `scheduled_tasks` row `8fa09ed3` updated `30 18 * * 1-5` → `30 18 * * *` (6:30 PM CR, 7 days/week — dynamic crons run tz `America/Costa_Rica`); prompt unchanged (already setter-centric: per-setter lead counts, unclaimed count, setter attribution on booked calls, setter Slack tags on about-to-book/stalled items). Cron expressions register at boot only, so ng-pm-MAX was restarted via Railway redeploy of the current image (deployment `abd2e440`, SUCCESS) — boot log confirms `Registered dynamic cron: "Sales EOD Report" (30 18 * * *)`, 11 dynamic crons loaded. First weekend run: next Saturday/Sunday 6:30 PM CR. **Side finding (flagged, not fixed):** two ACTIVE near-duplicate closer reminder tasks — `Daily Closer Attendance & Outcome Reminder` (`ba082584`) and `Closers Daily Attendance & Outcome Reminder` (`0c9ce290`), created 45s apart on 2026-07-28, both `0 20 * * 1-6` to `C0AJANQBYUE` with different prompts — the boot dedup is exact-name-only so both register and likely double-post nightly; one should be soft-disabled (+ restart).
- **2026-08-03 — Duplicate closer-reminder crons deduped + APPROVAL_NEEDED re-prompt leak diagnosed.** Follow-up to the same-day side finding below. Confirmed via portal `agent_activity` + #ng-sales-goats history: BOTH tasks fired back-to-back at 8 PM CR nightly since Jul 29 — two reminder posts seconds apart, and on several nights (Jul 31, Aug 1) one of the two posted a raw `APPROVAL_NEEDED|C0AJANQBYUE|0|…` sentinel string verbatim into the channel. **Leak root cause (code — FIXED later same day):** `registerDynamicCron` strips the sentinel from the FIRST `callClaude` reply ([index.js:1285](../index.js)) but NOT from the re-prompt reply ([index.js:1377](../index.js)) — and these reminder tasks aren't in `TASK_HEADERS`, so their short output falls to the unknown-task ≥300-char check, fails, re-prompts, and whenever the re-prompt uses `draft_channel_post` the sentinel posts verbatim (its prefix even pads the reply past 300). Affects ANY task not in `TASK_HEADERS` whose re-prompt emits the sentinel. Fix shipped later the same day (Ron approved via task chip): `stripApprovalSentinel()` helper applied to BOTH the first reply and the re-prompt reply before validation, + the reminder task added to `TASK_HEADERS` with `[]` (len>20 check instead of the 300-char unknown-task floor). **Shipped without code:** soft-disabled the newer terse duplicate `Closers Daily Attendance & Outcome Reminder` (`0c9ce290`, active=false); kept `Daily Closer Attendance & Outcome Reminder` (`ba082584`) and hardened its Supabase prompt — now pulls "today calls" for a factual nudge (calls on board / outcomes missing), explicitly forbids `draft_channel_post`/posting tools ("return ONLY the final reminder text"), and requires ≥300 chars so it passes validation on attempt 1 and never enters the leaky re-prompt path. Railway ng-pm-MAX restarted (redeploy `ad3efcd1`) to de-register the disabled cron. **Also noticed:** a native Slackbot `/remind` ("Dont forget to submit your EOD Report") fires `<!channel>` at the same 8 PM CR minute in #ng-sales-goats — with Max's reminder that's still 2 pings/night; only Ron can delete a Slackbot reminder.
- **2026-08-03/04 — Post-iClosed-cutover truth pass (3 ng-agent pushes + dash PR #21 + Phase-5 verification).** Ron asked to confirm iClosed was 100% decommissioned, then approved a 5-phase plan closing the silent gaps. **Verified state:** iClosed webhooks dead since Jul 23; dash GHL ingestion live since Jul 26 (`source='ghl'`, `booked_at` 32/34, `attended` real post-call via GHL workflows since ~Jul 28, `qualification_snapshot` survey 29/34). **Phase 1 (dash, [PR #21](https://github.com/neurogrowth-cr/dash.neurogrowth.io/pull/21) — MERGED 2026-08-04 on Ron's go, Vercel production READY; parallel session's [PR #20](https://github.com/neurogrowth-cr/dash.neurogrowth.io/pull/20) also merged):** phone-null guard in `buildProspectPatch` (every phone-less GHL event was wiping prospect phones — breaks call prep), owner-null guard in `ensureAppointment` update path (thin events wiped setter/closer attribution; the `upsertAppointmentFromEvent` twin fix deliberately left to PR #20), admin-UI outcomes now `source='manual'` + `applyRevopsSourceFilter` includes manual under ghl mode. 216 tests pass. **Phase 2 (`94ed36b`):** `runSalesStandup` re-pointed off the retired EOD tables (every closer had gotten "0 calls | 0 closes" DMs since cutover) onto `getCloser/SetterWeeklyStats` scoped to CR-anchored yesterday, "N awaiting outcome" so pending days don't read as dead; `resolveSalesMember` warns on unmapped ids; leaderboard formatters drop raw-id garbage rows (`isUnresolvedSalesId`); Ron's GHL user id added to `CLOSER_SLACK` (call-prep routing). **Phase 3 (`6ee753b`):** `classifyOutcome` full-enum (`nurture`→showed; `rescheduled` own bucket — was inflating Pending forever; unknown values warn); `filterFlywheelAppts` drops synthetic `opp:%` pre-book-disqualification rows at the shared choke point (+`ghl_appointment_id` added to 6 selects); **held = `attended===true` OR outcome-showed** (dash isHeld parity — 6 of 12 post-cutover held calls were invisible: GHL marked them showed, no outcome logged); all six daily scrapers on CR-anchored UTC bounds (`_crDayBoundsUtc` — old naive strings drifted 6h). METRIC_REGISTRY keys byte-identical. **Phase 4 (`fdf41d4`):** Monday gap no-shows keyed on `classifyOutcome` (attended=false also means CANCELLED — would have nagged closers about cancellations); duplicate "outcomes not logged" gap section deleted (4 PM reminder owns it); iClosed Slack relay deleted (~90 lines); `fetchIClosedIntakeForProspect` + call sites deleted; call-prep source line native-only; `getNonFlywheelCallIds` frozen to boot-time memo (deletable ~2026-11-20); label truth sweep (`iclosed_*` metric keys frozen ON PURPOSE, documented); call-prep DMs render `🎯 Lead quality score: NN/100` from `qualification_snapshot.parsed`. **Phase 5 — no build needed, verified already done:** the planned Meta-Purchase re-home already exists — Make scenario **5148801 `[PROD] GHL Opp Won → CAPI Purchase`**, active since cutover, successful runs Jul 27–30, and Meta insights show **3 `purchase` actions in the last 14d** → `meta_cac_today` is alive again; 4636094 already deactivated/renamed at cutover. CAC comments corrected to cite 5148801. **Deploys:** Railway `0fabf885` (`fdf41d4`) SUCCESS, clean boot, 10 dynamic crons (correctly reflecting the parallel session's same-night cron soft-disable below — their `ad3efcd1` restart briefly resurfaced the pre-truth-pass image; the Phase-4 push superseded it 3 min later). **Watch:** Mon 9 AM standup (first real-number briefs), Mon 7 AM closer leaderboard (new held definition), 6 AM scraper values, next call-prep DM with a lead-quality score.
- **2026-08-04 — APPROVAL_NEEDED re-prompt leak FIXED in code (commit `b3f03de`, Railway `7820384b` SUCCESS, clean boot, 10 dynamic crons).** Ships the fix diagnosed in the 2026-08-03 dedupe entry below, approved by Ron via task chip. `stripApprovalSentinel()` helper ([index.js](../index.js), above `registerDynamicCron`) now strips the `draft_channel_post` sentinel from BOTH `callClaude` replies — first attempt AND the structural-validation re-prompt — before `validateFinalReport` runs, so the validator always sees the real message and nothing sentinel-prefixed can reach a channel (previously only the first reply was stripped; the re-prompt reply posted verbatim, and its prefix padded short replies past the 300-char unknown-task floor). `Daily Closer Attendance & Outcome Reminder` added to `TASK_HEADERS` with `[]` so the short nightly nudge uses the len>20 check and stops entering the re-prompt path at all. Rule going forward: any new `callClaude` call site inside the cron handler must pass its reply through `stripApprovalSentinel()` before use, and every new scheduled task gets a `TASK_HEADERS` entry (headers, or `[]` for short/flexible output). Combined with the same-night dedupe below, tonight's 8 PM CR closer reminder should be exactly one clean post — remaining noise is the native Slackbot `/remind` in #ng-sales-goats (Ron-only to delete via `/remind list`). Memory `project_bug_fixes_apr27.md` updated: the Apr 27 sentinel fix was path-incomplete, this closes the second path.
- **2026-08-06 — Make `[PROD]` scenario watchdog + booking→alert divergence check (DEPLOYED 2026-08-11 in `f8931ac`; watchdog ARMED same day — `MAKE_API_TOKEN` set on Railway, first sweep caught 2 queued failures on 5148796).** *This entry was restored 2026-08-12 from branch `make-scenario-watchdog` (`8201ede`): `f8931ac` hand-copied that branch's code onto main but rewrote this docs entry from stale knowledge (said hourly polling, omitted `dlqCount` and the divergence check entirely).* Closes the blind spot exposed on 2026-08-04 when 5148796 auto-deactivated after an invalid blueprint push and the only signal was a Make email to Ron's inbox. Two independent watchers, both alerting #ng-fullfillment-ops tagging Ron, in the MAKE HEALTH block above START in [index.js](../index.js). **(1) `checkMakeScenarioHealth()`** — polls Make API v2 `GET /scenarios?teamId=432699` every 10 min (NOT hourly: the Aug 4 outage lasted ~4 min and an hourly poll would have seen `isActive:true` on both sides of it) and alerts on three distinct failure modes for any scenario named `[PROD] …`: `isActive===false` (deactivated, webhooks queuing), `isinvalid===true` (bad blueprint — what *precedes* an auto-deactivation), and `dlqCount>0` (**active but erroring** — the "the scenario has NOT been paused" class that `isActive` alone never catches; each incomplete execution is a booking with no CAPI event and no Slack alert). Name-prefix match so new `[PROD]` scenarios need no code change. Debounced one-alert-per-outage + all-clear on recovery, in-memory ON PURPOSE so a Railway restart re-alerts on a still-open outage. Paginates `pg[limit]`/`pg[offset]` (Make does not document the default page size; silent truncation would drop scenarios from the watch with no symptom). A dead token raises its own "the watchdog is blind" alert. `MAKE_WATCHDOG_IGNORE` (default `5776020`) because **`[PROD] Auto Strike Mover` is deactivated deliberately** — superseded by Max's own `runAutoStrikeMover` cron — so without it the watchdog false-alarms on its first run; better long-term fix is renaming it out of the `[PROD]` prefix per the existing `[DISABLED …]`/`[ARCHIVED …]` convention. **(2) `checkBookingAlertDivergence()`** — every 30 min, cross-checks `revops_appointments` (source=ghl, 6h lookback, 30-min grace so in-flight bookings are not gaps, synthetic `opp:%` rows and non-flywheel calls filtered via the shared `filterFlywheelAppts`) against `ng_appt_slack_alerts` kind='booked'. This is the only check that watches the OUTCOME rather than the plumbing: Make reports full health when a GHL workflow simply stops firing the webhook — exactly the GHL `customData` bug that silently skipped 33 deliveries on 2026-08-02 with no system anywhere reporting a fault. **FAILS CLOSED** — `revops_appointments` is RLS-protected (`service_role` only) while `ng_appt_slack_alerts` has RLS off, so an unreadable alerts table would make every booking look unalerted; that path logs loudly and posts nothing rather than storming the channel and training everyone to mute it. **Live dry run:** 16 GHL bookings over 7 days, **0 would have alerted** — a window 28× the code's actual 6h lookback, so the check is quiet by default. **Contract verified** against the live team-432699 payload: 29 scenarios, all carrying `isActive`/`isinvalid`/`dlqCount`, 4 matching `[PROD]`. Bot posting rights confirmed — Max already posts the daily FULFILLMENT EOD PULSE to #ng-fullfillment-ops. **Tests:** `test/make-watchdog.test.js` 32/32 (extracts the real functions out of index.js like `strike-rules.test.js`, stubs fetch/Slack/Supabase); strike-rules 11/11, booking-gates 25/25.
- **2026-08-04 — Auto strike mover: per-run Slack posts silenced, daily summary folded into nightly learning.** The strike mover (live since ~2026-07-29, `STRIKE_MOVER_MODE=live`, `STRIKE_MOVER_MAX_MOVES=150` on Railway — rollout complete, this was its first project-state entry) was posting an unconditional summary to #ng-pm-agent on every 2-hourly sweep (8/day) even with `Moved: 0`. Ron chose a daily digest inside the nightly learning report instead. Changes: (1) `runAutoStrikeMover` now persists each sweep to `agent_activity` (portal Supabase) as `event_type='strike_sweep'` with metadata `{mode, scanned, moved, capped, skips, moves≤50, failures≤10}` via `logActivity`, and the per-run Slack post only fires in dry-run mode (testing visibility) — live mode is silent per-run. (2) `migrations/012_strike_sweep_read.sql` (APPLIED to portal project `xqzfhofxtmqjozowrwdo`) adds a narrow anon-select RLS policy scoped to `event_type='strike_sweep'` only, since `portalSupabase` uses the anon key and `agent_activity` was select-to-authenticated; rest of audit log stays locked. (3) New `strikeBuildDailyDigest(24)` (index.js, after `runAutoStrikeMover`) rolls up the last 24h of sweeps → `{digestBlock, slackLine}`; zero sweeps returns a cron-may-be-dead alert instead of silence. (4) `runNightlyLearning` appends `=== AUTO STRIKE MOVER ===` to the LLM digest (prompt bullet 10: anomalies only — dead cron, failure spikes, unusual volume; routine days get no KB entry) and the nightly Slack post now carries a deterministic `Strike mover (last 24h): N sweeps | N moved | N transient failures` line. Known quirk left as-is: nightly learning actually fires 5:30 AM CR (cron `30 5 * * *` was written as UTC before the CR timezone option was added; comment says 11:30 PM) — 5:30 AM cleanly covers yesterday's 7am–9pm sweeps, so it works; same drift exists on `runWeeklyPortalTrends` and `runMondayGapDetection` if ever cleaned up. GHL 401/503 sweep failures remain retry-less by design (card self-heals next sweep) and now live in metadata, not Slack. **Deployed & verified:** [PR #35](https://github.com/neurogrowth-cr/ng-agent/pull/35) merged by Ron (merge commit `d5b8d32`, 22:45 UTC), Railway `10171dee` SUCCESS, clean boot (10 dynamic crons). First live sweep on new code (6 PM CR): scanned 1410, moved 0, 1 transient 401 — `strike_sweep` row written with full metadata, ZERO Slack post (last old-style per-run post was 4:04 PM CR, pre-deploy). Nightly-learning digest line confirmed in later posts. *(Tail folded in from PR #36 on 2026-08-12 during the docs restructure.)*
- **2026-08-04 (evening) — Three accuracy fixes: stale no-show on upcoming calls, MTD setter leaderboard, CR-anchored calendar windows** (PR #37 + dash PR #22). (1) *Roster*: GHL reschedules reuse the appointment row, so a pre-reschedule `no_show` outcome + `attended=false` rode along and `get_sales_intelligence("today calls")` printed `outcome: no-show` on calls that hadn't happened yet (live: Daniela Bruno Aug 4, José Manuel Aug 5 — 2 of 12 future appts). Upcoming calls now render `scheduled` or `scheduled — rebooked after earlier no-show`; outcomes query scoped `.in('appointment_id',…)`; same future-call guard in `getCloserWeeklyStats`/`getSetterWeeklyStats`. The 2 stale rows were deleted + `attended` reset in portal Supabase. Upstream: dash PR #22 (`clearStaleNoShowState`) skips/clears a `no_show` aimed at a future `scheduled_start` and clears no-show state on `call_rescheduled`. Roster prompt (`scheduled_tasks` 7873fe73) documents the new status wording. (2) *Setter Leaderboard is now MONTH-TO-DATE* (Ron's decision: monthly top-performer race, resets on the 1st; Tue+Sat cadence kept). CR-anchored month start replaces rolling UTC 7 days; claims-ownership lookback anchors 45d before window START; REVI enrichment limit 25→150 with per-prospect dedupe; stats block header now window-agnostic `SETTER STATS — <start> → <end>`; prompt (`scheduled_tasks` 0fec5e2f) reframed to MTD with `SETTER LEADERBOARD — <Month> MTD, <start> → <end>` header. (3) *Calendar*: `getCalendarEvents` windows were server-clock (UTC) based and 48h wide (`setHours(29,…)`), so after 6 PM CR every report's `TOMORROW'S CALENDAR` block actually held day+2 — Friday evenings surfaced Monday's `NG 'Win Da Week' Protocol` as "tomorrow". Now `_crMidnightUtc` builds exact-24h CR windows, event lines render CR weekday + wall clock, cron calendar blocks are labeled with the resolved CR date, and the nightly-learning date string is CR-anchored. DEPLOYED 2026-08-05: PR #37 merged (b8d1e5e) → Railway auto-deploy `194e9aec` SUCCESS, clean boot 04:55 UTC, all 10 dynamic crons registered on the updated prompts; dash PR #22 merged (bfda6fd) → Vercel production READY on dash.neurogrowth.io. Post-deploy verification: SQL confirms 0 outcome rows attached to future appointments; simulated Friday-7PM-CR calendar bounds return exactly Fri/Sat 24h windows (Win Da Week excluded from "tomorrow"). Watch items: Aug 5 8:30 AM CR roster (José Manuel should read scheduled / rebooked wording), Sat Aug 8 leaderboard (header must read `SETTER LEADERBOARD — August MTD, …`).
- **2026-08-05 (SUPERSEDED 2026-08-06 — edit was lost to a later same-day blueprint edit; re-done above with `Setter:` label) — Booked-call Slack alert now shows the assigned setter (Make scenario 5148796 blueprint edit, no code).** Ron: reschedule alerts show "Assigned to" but new-booking alerts don't. The reschedule scenario (5822303) gets `assigned_line` pre-built by the Supabase trigger fn `ng_appt_change_notify()` from the GHL webhook payload's `user` object (contact's assigned user = setter). Same object is present in the booked webhook payload — verified against live `ghl_webhook_deliveries` rows: Ana Jency's booked event carried `user` = Sebastian Serrano; Claudio Andrade's self-booked event had `user` = null. Edit: the `Slack: Booking Alert` module's text in 5148796 now appends a conditional `*Assigned to:* {{1.user.firstName}} {{1.user.lastName}}` line after the Host line — rendered only when `1.user.firstName` is non-empty (gated with the same `if(length(ifempty(…)) = 0; …)` pattern already used by the POSIBLE-DUPLICADO block; unassigned self-books simply omit the line, matching the reschedule alert's behavior). Followed the 2026-08-04 lesson: `validate_module_configuration` passed BEFORE `scenarios_update`, and the update response confirmed `isinvalid: false` / `isActive: true`. No new modules, no ops-count change. **Watch:** next setter-booked call alert in #ng-sales-goats should carry the Assigned-to line; self-booked (unassigned) alerts stay unchanged.
- **2026-08-06 — Booked + cancel/reschedule Slack alerts relabeled `Closer:`/`Setter:`; setter line re-added to booked alert (Make 5148796 blueprint edit + Supabase migration, no ng-agent code).** Ron asked for the setter on NEW bookings (not just reschedules), then to relabel `Host:` → `Closer:` and `Assigned to:` → `Setter:`. Two surfaces: **(1) Make 5148796 `Slack: Booking Alert` module** — `*Host:*` relabeled `*Closer:*` and a conditional `*Setter:*` line appended from the webhook payload's `user` object (`{{1.user.firstName}} {{1.user.lastName}}`, trim-gated `if(length(trim(…)) > 0; …)` so unassigned self-books omit the line entirely). Payload contract verified against live `ghl_webhook_deliveries` `call_booked` rows: `user` = Sebastian Serrano / William B / Oscar Mena on setter-assigned events, null on Claudio Andrade's self-book. Followed the 2026-08-04 lesson: `validate_module_configuration` passed BEFORE `scenarios_update`; response confirmed `isActive: true` / `isinvalid: false`. **(2) Supabase fn `ng_appt_change_notify()`** (project `xqzfhofxtmqjozowrwdo`, migration `relabel_closer_setter_in_appt_change_alerts`) — the cancel/reschedule alerts (Make 5822303) render `host_line`/`assigned_line` pre-built by this trigger fn, so the labels live in SQL, not the blueprint: `'*Host:* '` → `'*Closer:* '`, `'*Assigned to:* '` → `'*Setter:* '`; logic otherwise byte-identical, verified via `pg_get_functiondef` (old labels gone, new present). **Stale-entry correction:** the 2026-08-05 entry below recorded an Assigned-to line added to 5148796, but today's fetched blueprint did NOT contain it — the 2026-08-05 09:05 UTC blueprint edit evidently overwrote it (matches Ron's screenshots: last night's booked alerts had no setter line). Re-added here with the new label. **Watch:** next setter-booked alert in #ng-sales-goats should read `Closer: Jose Carranza (…)` + `Setter: <name>`; next cancel/reschedule alert should carry the same labels.
- **2026-08-06 (later) — The 5148796 edit below was CLOBBERED within 10 min and re-applied.** Ron reported the 16:59 CR booking alert (Mark Christianson) still said `Host:` with no setter. Execution history: my blueprint push landed 20:01 UTC; a second `modify` at 20:11 UTC — a parallel session's CAPI match-quality edit (`country: {{lower(8.body.contact.country)}}` in both CAPI modules, `action_source` → `system_generated`) — was built from a pre-20:01 fetch and wholesale-replaced the blueprint, silently reverting the Slack text (scenarios_update has no merge/version check; last writer wins). Same race ate the 2026-08-05 attempt. **Re-applied 00:35 UTC Aug 7 by editing the CURRENT blueprint** — Closer/Setter text restored AND the parallel session's CAPI changes kept; post-push re-fetch confirms both present, `isActive: true` / `isinvalid: false`. New lesson in tasks/lessons.md ("Make blueprints are a shared checkout"). If the labels vanish again, check `executions_list` for a `modify` event after 2026-08-07T00:35Z before re-diagnosing.
- **2026-08-06 — Leaderboard-accuracy chain closed out: Intro-calendar ingestion verified live end-to-end; 3 wiped attributions repaired (data-only, no code change).** Follow-through on the 2026-07-28 audit entry below. **(1) GHL workflow coverage completed by Ron.** All calendar-filtered triggers now cover the Intro calendar (`KRTGx8XteIJSCcKAShHS`) alongside the main one via *duplicate trigger cards* (GHL's "In calendar" filter is single-select — N calendars = N trigger cards, OR'd into the same action): `Appt Booked → Opp + Tag + CAPI` v14→v15, `Pre- Sales Call Reminders` v14→v15, `Outcome: Showed` v4→v5, `Outcome: No-Show` v5→v6, `Outcome: Cancelled` v4→v5, `Outcome: Follow-up` v4→v5, `Neurogrowth Dashboard Webhook` v9→v12. Still at their Jul 27 versions: `Outcome: No Fit`, `Outcome: Generic Lost`, `NeuroGrowth Outcome Webhook` — either unchecked or genuinely un-filtered; the workflows API exposes only name/status/version, so it cannot distinguish "no calendar filter" from "not opened". **(2) Intro pipeline PROVEN live.** Jessika Brown's booking produced the first Intro-calendar webhook delivery ever recorded (2026-07-31, payload `mediumId: KRTGx8XteIJSCcKAShHS`; previously 0 of 49 deliveries carried it, now 1 of 162). dash then *updated* the row that had been hand-backfilled on Jul 28 when she rescheduled to Aug 7 — matched on `ghl_appointment_id`, closer preserved, no duplicate row — confirming the manual backfill used the correct natural key. **(3) Outcome-logging discipline improved sharply:** 10 of 22 past calls since Jul 29 carry outcomes, vs 2 of 24 in the audited Jul 20–27 week, including a won $3,500 (Luis Guillermo Rodríguez, Jul 29, Ron closing / Sebastian setting). 12 still unlogged — the residual gap is behavioral, not technical. **(4) Attribution-wipe repair.** Three appointments had `closer_id` + `setter_id` nulled by thin reschedule/cancel events that carry no owner fields: `teyMBwDm44TFV7bHH0ic` (María Amalia Ramírez, wiped Jul 28), `8hNVw0pZ2ZJrsbsAyoqE` (Annel Ivette Varela, Jul 31), `T0skLY3TCNcELOP42yx9` (Daniela Bruno, pre-Aug-2 reschedule). **No code fix was needed** — a parallel session had already shipped the identical guard in BOTH write paths (`upsertAppointmentFromEvent` and `ensureAppointment`, dash PR #20 + #21, production 2026-08-02 ~16:46 UTC). **The lesson: those guards only PREVENT wipes, they never RESTORE them**, so pre-Aug-2 damage sat unrepaired and invisible — Daniela's row even *looked* like a post-fix regression (updated Aug 5) when that was just the guard correctly declining to write a null over an already-null field. Repaired by direct SQL from GHL truth (`closer = assignedUserId`, `setter = createdBy.userId`; creator ≠ closer on all three so setter credit applies), guarded with `and closer_id is null` for idempotency. `revops_appointments` now has **0** real appointments (non-null `scheduled_start`) missing a closer. **Deliberately NOT done:** no `no_show` outcome rows written for the two GHL-noshow rows — dash PR #22 suppresses stale no-shows on rescheduled calls by design, and writing them would reintroduce exactly that bug. **Watch item:** 10 rows with `ghl_appointment_id LIKE 'opp:%'` and NULL `scheduled_start` have accumulated Jul 30 → Aug 6 from opportunity events with no attached appointment — invisible to every leaderboard (all filter on `scheduled_start`) but growing ~1/day; worth deciding whether dash should stop creating them. **Process note:** this entry was authored from an isolated `git worktree` off fresh `origin/main` because the shared checkout held another session's uncommitted work (223 lines in `index.js`).
- **2026-08-09 — CLOBBERED AGAIN and re-applied (3rd time).** Ron reported Aug-9 booking alerts (Cindy Guerrero 17:06 CR, Shalo Mizrahi 17:49 CR) still on legacy copy. Modify history: the CAPI session pushed once more at **2026-08-07T00:50Z — 15 min after the 00:35 re-apply** — from yet another stale copy, reverting the Slack text a second time while advancing its CAPI mappers (calendar-conditional fbc/fbp + `action_source` website-vs-system_generated keyed on `1.calendar_id = 'HXLeEjxpa0gdiTPNiAzc'`, conditional `event_source_url`). No modifies between Aug 7 00:50 and this re-apply (**2026-08-10T04:19Z**) — that session appears done, so this application should hold. Closer/Setter Slack text re-applied on top of the current blueprint, ALL of the CAPI session's latest changes preserved; `isActive: true` / `isinvalid: false`. Any `modify` event after 2026-08-10T04:19Z that isn't documented here = another clobber.
- **2026-08-10 — REVI HEALTH CHECK rebuilt on REVI's poller heartbeat — false-stall alarm class killed** ([PR #39](https://github.com/neurogrowth-cr/ng-agent/pull/39) merged → Railway deploy `e82acd2c` SUCCESS; companion [ng-revi PR #35](https://github.com/neurogrowth-cr/ng-revi/pull/35) deployed the same minute). **The incident:** 2026-08-04 6:30 AM check DMed Ron "REVI appears stalled — 4 sales call(s) held since 2026-07-31" while REVI was healthy. The 4 counted `revops_appointments` rows (Aug 1–3) were all activation-host recordings (Josue) that REVI rightly skipped; the skip decisions lived only in REVI's Railway logs. **Key finding — do not re-derive:** filtering the count by `revi.revi_closers` can NOT fix this. All 4 phantom appointments carried `closer_id = jose.neurogrowth@gmail.com` in GHL while Fathom shows Josue hosted them — GHL booking data doesn't know who ran a call, and `attended` has been null on effectively every row since ~Jul 31. **Fix:** REVI now publishes `revi.engine_heartbeat` (single row `id='poller'`: `last_poll_at` / `last_seen_at` / `last_closer_seen_at`, upserted every ~12-min poll; migration `0010_engine_heartbeat.sql`, applied via MCP). `runReviCrossChecks` Check 1 reads it and splits the old single guess into three precise alerts: 🔴 **poller down** (`last_poll_at` stale >60 min — fires immediately and suppresses the redundant freshness alert), 🔇 **Fathom feed dark** (poller running, appointments on the calendar, zero new recordings seen — the Jul 16 team-sharing signature), 🟡 **seen but unscored** (closer-hosted recordings visible, nothing scored in 2+ business days — check skip reasons; all sub-10-min no-shows is benign). Recordings flowing with none closer-hosted = no closer-qual calls = **healthy, stays silent** (exactly the Aug 4 shape, replay-verified against the real Aug 1–3 window). Falls back to the legacy appointments-vs-scores heuristic with a skip-reasons caveat if the heartbeat row is ever missing. Dedup key now prefixes the alert-kind chain (`poller-down`/`stall-legacy`/`seen-unscored`/`feed-dark`/`stuck-drafts`) so one alert type can't suppress a different one sharing the same last-scored date. **Verified live post-deploy:** first heartbeat row written by REVI's 04:12 UTC poll cycle (`last_closer_seen_at` = a Sat Aug 8 closer call, inside the 72h lookback). First smarter check fires Tue 6:30 AM CR. Authored from an isolated worktree; shared checkout held another session's uncommitted work.
- **2026-08-11 — Max reliability package: raw dates in every "Day N" output, PORTAL DATA MAP in the system prompt, hard "never claim data doesn't exist without querying" rule, corrections now learned from interactive chats** (+ dash branch `feat/launch-sla-kpis` adds launch-SLA KPIs to the admin Client Progress tab). Trigger: Max told Ron activation call dates "aren't stored" — they live in `customer_activities.completed_at` joined to template title `%activation call%`, and Max's own day-anchor code read exactly that field then threw the date away. (1) New `resolveDayAnchor()` is the single source of truth for Day-N math; `getClientStatus`/`getPortalAlerts`/`runMondayGapDetection`/`runFulfillmentStandup` now print `Day N since activation call (YYYY-MM-DD)`. The standup's `getDayCount` was silently anchoring on `created_at` (latent bug) — now SLA-correct; **Josue's Day-7/Day-14 standup numbers will shift**. (2) `SYSTEM_PROMPT_DATA_MAP` (tables, join keys, canonical recipes, and the warning that `search_portal_schema` matches names not row values) rides every prompt, both Ron and team tiers. (3) Rule #6 rewritten: applies to every user; Max may not claim a field "doesn't exist" until data map + schema search + at least one `query_portal_db` SELECT; `MAX_TOOL_ROUNDS` 5→7. (4) `getGlobalLessons()` injects the last ≤10 shared `report-feedback`/`correction` lessons into EVERY interactive `callClaude` (reports already had their own loop); new `detectAndSaveCorrection()` (EN+ES regex gate → Haiku confirm) captures corrections from DMs and non-Max-rooted mention threads as `correction:<topic>:<date>` rows. **Data finding encoded in both repos:** `flywheel_ai_onboarding.go_live_at` is a PROVISIONING date — it predates the activation call for all 7 spot-checked clients; the real launch signal is campaign QA/validation activity `completed_at` (matches Ron's tracking sheet ±2 days). Portal side (dash commit `21b4c11`, build green, PR pending): per-client activation-call date + Day N, time-to-launch vs 14-day SLA (green/red), over-SLA-not-launched flag + `launch_overdue` critical alert, 3 exec StatCards (avg time to launch, % within SLA, over-SLA count). Ground truth at ship time: Synapse 29d OVER, Ubuntu Tech 20d OVER (launched Aug 3 — Ron's sheet was stale), Selva Design + CONAUDITA activation Jul 17 with no launch → will trip the alert. Follow-up flagged, not fixed: dash `activation-call-activity-gate.ts` exact-equality title match ('activation call') would miss differently-titled templates; it gates a write path, needs its own verified PR.
- **2026-08-12 — Daily strike-mover digest to #ng-sales-goats (`runStrikeSalesDigest`, 9:30 PM CR) + watchdog docs entry corrected.** Trigger: sales reported the GHL auto card-mover "isn't working." Diagnosis said otherwise — `strike_sweep` audit rows show live sweeps every 2h moving 2–34 cards each — but since PR #35 (2026-08-04) live mode posts nothing to Slack, so movement was invisible and sales reasonably concluded it was dead. Fix (Ron's call): `strikeBuildDailyDigest` now also returns a `salesBlock` (plain-language: cards moved, per-transition tallies, up to 20 names, a fixed footer explaining the three not-moved reasons and inviting sales to reply with a contact name to trace); new `runStrikeSalesDigest` posts it to `SALES_CHANNEL` daily at 9:30 PM CR (after the day's last 7am–9pm sweep, so the 24h window covers exactly today's 8 sweeps). Zero-sweep case posts a ⚠️ mover-may-be-down warning instead of silence. Nightly-learning consumers (`digestBlock`/`slackLine`) untouched. Known sales-perception traps documented for future traces: `automated_send` skips ~200 cards/sweep by design (only follow-ups typed in GHL count as strikes), and `ghlGetConversationMessages` reads only the last 20 messages (busy threads can misread) — both queued as an explicit rules decision with sales examples, not changed here. Also in this PR: the 2026-08-06 watchdog entry below was restored from branch `make-scenario-watchdog` (`8201ede`) after `f8931ac` overwrote it with a stale, inaccurate rewrite — see that entry's provenance note. This clobber (a session hand-copying a branch's code but rewriting its docs from memory) is incident #3 in the parallel-session series; repo guardrails (PR-only main, append-at-bottom docs, `.gitattributes merge=union`, CI) ship in the next PRs.
- **2026-08-12 — Weekly Sales & Marketing Recap rebuilt (next-gen): live GHL-native numbers, dead metric snapshots dropped, feedback loop made real.** The Friday 5 PM recap had been reporting garbage for months: "New contacts: 0" (its `ghl_new_contacts_today` scraper passed GHL **v1** `startDate/endDate` params to the **v2** `/contacts/` endpoint with no `res.ok` check, so every API failure recorded a fake 0 — poisoning the anomaly baseline too), "0.1% close rate" (fraction rendered with `%` and no ×100), "Calls held" read from the documented zombie `iclosed_calls_held_yest`, and its `fetchMetric` took the newest-7 `metric_observations` rows with no date filter so "this week" could span months. **Rebuild:** every number now computed live at report time over one CR-anchored window (Monday 00:00 CR → report time, WoW deltas vs the same elapsed window −7d, `crRecapBounds`). Leads = deduped `lead_posts` per-source (`getWeeklyLeadStats`, honestly labeled Form/WA funnel) + VSL self-booked line (`setter_id IS NULL` proxy on the booked cohort). Pipeline = single `scheduled_start` cohort via `getWeeklySalesStats` (flywheel-filtered, `classifyOutcome`, show rate = held÷(held+no-shows), close rate = won÷held×100), plus a "wins logged this week" line on `revops_sales_outcomes.created_at` so a deal closed this week on an older call can't contradict the cohort's "Closes: 0" or the sales-won anomaly DMs. Meta = new structured `getMetaAdsRange(since, until)` with an exact `time_range` matching the window (old `last_7d` rolling preset mismatched the Mon–Fri headline) + a Meta-vs-GHL lead reconciliation line flagging >20% gaps. Anomalies now **source-scoped to `anomaly-detection`** (the old unfiltered `category='alert'` dump mixed nightly-learning ops items, REVI infra health, and `confidential:*` rows), deduped to latest-per-metric, cap 5, sentence-boundary truncation (`firstSentence`) instead of `substring(0,120)` mid-sentence cuts; ops flags collapse to a count line. Client updates deduped latest-per-client, junk slugs (report titles filed as clients) filtered. **Feedback loop finally live:** a non-fatal Haiku pass (`composeRecapNarrative`) writes a 2–3 sentence READ takeaway from the final deterministic stat block with `getReportLessons('weekly-sales-marketing-recap')` injected — the old code prepended lessons as text onto a deterministic body, so corrections were stored but never applied. Also fixed the daily `_scrapeGhlNewContactsToday` at the source: now counts deduped `lead_posts` for the CR day and **throws** on error (metric key frozen — `metric_observations` PK with baseline history; label → "New leads (Form/WA funnel, today)"); expect 1–2 harmless ↑ anomalies while the zero-poisoned baseline self-heals. New Ron-only `preview_weekly_recap` tool returns the recap on demand without posting (`runWeeklySalesMarketingRecap(null, {preview:true})`). Verified via standalone dry run against live data (module NOT required — that would steal production's Socket Mode socket): 105 real leads vs the old 0, $3,500 win surfaced, reconciliation flagged a Meta-83-vs-GHL-105 gap. Cron schedule, Ron-DM delivery, and the `inferReportId` headline mapping unchanged. Authored from an isolated worktree off `origin/main`; rebased onto the reliability-package commits below.
- **2026-08-12 — Parallel-session guardrails SHIPPED & ENFORCED end-to-end (PRs #42/#43/#44 merged, all deploys SUCCESS) + first strike-mover sales digest verified live.** Trigger: two issues Ron raised — sessions clobbering each other's merges, and sales suspecting the GHL auto card-mover was broken (it wasn't; it was invisible — see the 2026-08-12 digest entry above). **Enforcement now active on main:** GitHub ruleset `protect-main` (id 20767736) — PR required (zero bypass actors, including Ron), force-push + branch deletion blocked, and the CI `checks` job is a REQUIRED, STRICT (branch-must-be-up-to-date) status check since #44 merged. Local layer: `.claude/settings.local.json` denies `git push origin main` variants. Repo layer: [CLAUDE.md](../CLAUDE.md) hard rules (worktree off fresh origin/main per session, rebase before PR, never hand-copy another branch's code, never reuse branch names) + [.github/workflows/ci.yml](../.github/workflows/ci.yml) (node --check + 3 test suites + docs-branch-touching-index.js guard + >50-lines-deleted revert detector, `major-change` label to override). Docs layer (#43): this file + lessons.md are append-at-BOTTOM with `merge=union` (verified: two branches appending in parallel merge clean, both lines survive); 33 pre-2026-06-13 entries archived to [tasks/archive/project-state-archive.md](archive/project-state-archive.md) (147KB→91KB). **Cleanup:** stale `recap-wt` worktree pruned; PRs #36 + #38 closed with their content preserved (#36's deploy-verification tail + cron rows folded into #43; #38's code was already on main via f8931ac, its correct docs entry restored by #42); 18 dead remote branches deleted (13 claude/*, feat/auto-strike-mover, fix/max-stalled-nudges, docs/revi-stall-fix-shipped, feat/agent-consult-endpoint, fix/revi-stall-false-positive). **PR #41 (recap-nextgen) merged through the new pipeline as its first live exercise** — required check enforced, strict up-to-date satisfied via branch update, `major-change` label created and applied (the −106 index.js lines are the old recap block being replaced, verified intentional). Deploy 5fff1092 SUCCESS, clean boot, 10 dynamic crons. **Verified live:** first `runStrikeSalesDigest` post hit #ng-sales-goats 2026-08-12 21:30 CR — 35 cards moved (20 NL→IC, 11 IC→S1, 3 S1→S2, 1 S2→S3) with names; CI green on PR #44 and on main post-merge. **Flagged to Ron, pending his action:** (1) the repo is PUBLIC — client names/deal amounts in these docs are world-readable; going private needs GitHub Pro to keep the ruleset enforcing; (2) live Railway + Meta tokens embedded in old `.claude/settings.local.json` allow-entries should be rotated; (3) watch the first next-gen weekly recap run for sane GHL-native numbers. **Convention reminder for every future session:** append entries at the BOTTOM of this section, work from a worktree, land via PR — CI and the ruleset will reject everything else.
- **2026-08-12 — Fulfillment-team ecosystem access: Ops Master Tracker tool, team-safe REVI client context, and the "REVI is OAuth-gated" hallucination killed at the root.** Trigger: Josue asked Max in Slack whether he could read the REVI schema for client-call summaries; Max answered "REVI is connected only via Ron's personal OAuth" and offered a manual-summaries workaround. That claim was FALSE — the `revi` schema lives in Max's own Supabase and `reviSupabase` (schema-scoped, anon key) has always been able to read it. The real gate was `get_revi_intelligence` sitting in `RON_ONLY_TOOLS` (correct policy — coaching teardowns + leadership initiatives are Ron-only) while the SHARED refusal string blamed "Gmail and Calendar use his personal OAuth"; Max faithfully repeated the wrong reason. Shipped: **(1) `get_ops_tracker`** (roster-wide) — dedicated reader for the hand-maintained Ops Master Tracker sheet (`OPS_TRACKER_SHEET_ID` env var, falls back to the live ID). Tabs resolved by HEADER SIGNATURE, not title (real titles "Clients Audit" / "Stabilization FASE 3" / "Lanzamientos " incl. trailing space — ops can rename freely): infrastructure checklist, change_requests (defaults to OPEN items; 64 Done rows hidden unless `include_done` or a client filter), launch_history. Per-cell 350-char truncation (single change-request cells run >5k chars and would have blown `readGoogleSheet`'s whole-output 6k cap), accent-insensitive fuzzy client match, 10-min tab-map cache. Reuses Ron's existing Google OAuth — `drive.readonly` already authorizes Sheets v4 reads, zero new credentials/scopes. **(2) `get_revi_client_context`** (roster-wide) — team-safe REVI surface: topic=calls (quicksync_reports + activation_reports: Fathom-auto-ingested client call summaries — resumen/riesgos for quicksyncs, descripcion_negocio.que_hace + puntos_accion for activations — health, session number, PDF + recording links, alias-resolved client match) and topic=roster. Explicitly EXCLUDED: closer_call_scores, coaching_runs, won/lost_deals, leadership_meetings, initiatives — those stay Ron-only via `get_revi_intelligence`. Added to `CONSULT_DROP_TOOLS` (REVI must never cite its own reports as verification evidence). **(3) Truthful refusals:** new `RON_ONLY_REASONS` map — the REVI block now names confidentiality as the reason and points to the team tool; Gmail/Calendar keep the OAuth wording. Team-tier prompt (:382 area) + `SYSTEM_PROMPT_DATA_MAP` now document the tracker tabs and the REVI split, with an explicit "never claim REVI is OAuth-gated" instruction. **(4) Tania added to `TEAM_MEMBERS`** (`U07SMMDMSLQ`, fulfillment) — she was never on the roster and could not DM Max at all. Verified pre-PR: `node --check`, all 3 test files pass, and a live dry-run harness (scratchpad, real function source eval'd against the live sheet + revi schema) confirmed every tab resolves, filters work, and "what was discussed with CONAUDITA" returns real session summaries. Known context: RLS still disabled on all 23 revi tables (tracked in ng-revi, NOT touched here — enabling it naively kills REVI's anon-key crons). Authored from an isolated worktree; shared checkout held another session's dirty state.
- **2026-08-13 — Alert → agentic triage → proposal → team ✅ → execute bridge ([PR #47](https://github.com/neurogrowth-cr/ng-agent/pull/47), ships INERT behind `MAKE_TRIAGE_ENABLED`).** Trigger: the Make watchdog armed 2026-08-11 correctly caught 2 stuck executions on scenario 5148796 within 15 minutes, but the alert could only say "2 incomplete executions" + a link. Root cause is structural and affects ~20 detectors: every one of them formats a string, posts to Slack, and stops — **none feed their result into `callClaude`, so Max never sees his own alerts.** Built the generic bridge (reusable by any detector) and wired ONE consumer. **(1) `opts.onlyTools`** on `callClaude` — allow-list inverse of `dropTools`; a narrow mode like triage otherwise has to ban ~38 tools by name. **(2) `triageAlert(alert)`** — detector hands over `{facts, actions it authorizes, guidance, onlyTools}`; Max investigates with read-only tools and returns ONE structured proposal via `finalTool`. Same shape as `handleAgentConsult` (the REVI→Max verdict path, which was the only pre-existing precedent for detector→LLM in the file). Nothing executes in triage. **(3) Approval deliberately does NOT reuse `APPROVAL_NEEDED`/"reply yes"** — `pendingApprovals` is in-memory, 30-min TTL, dies on every deploy, keyed by approver only, and its stage-2 phrases are exact-match English ("sí"/"yes do it" silently do nothing); alerts fire hours before approval, across redeploys. Uses the campaign-draft **emoji + Slack-message-metadata** pattern instead (restart-proof, no TTL), plus an hourglass **claim-lock** that closes the double-execute window the campaign flow leaves open when two people react at once. Proposals post to #ng-fullfillment-ops; any roster member may ✅ (Ron's call), non-roster gets a polite bounce. Dedup scans the channel's own message metadata (6h cooldown) and **fails closed** — an unreadable history suppresses rather than storming the channel every 10 min. **(4) Executors re-validate at EXECUTION time**, hours after the proposal: `retryMakeDlqs` re-lists the live queue so ids the model invented and ids a human already cleared are dropped (cap 10); `reactivateMakeScenario` re-checks `isinvalid` — restarting a broken blueprint just re-breaks on the next event and *looks* like the fix worked (the 2026-08-04 outage shape). Both gated to `[PROD]`, non-ignore-listed. **Retry safety derived from the actual blueprint, not assumed:** both CAPI modules use deterministic event ids (`ghl_schedule_{{appointment_id}}`), so Meta dedups a replay — but only within 48h since `event_time` is `{{now}}`, hence a hard 44h refusal. Triage is told which modules sit before vs after CAPI (2/8 before → retry is a pure win; 21/23 after → re-sends, dedup-dependent) so it can reason about its case. `invalid` alerts are deliberately untriaged — no authorized action repairs a blueprint. **(5) New read-only tools** `make_list_dlqs`/`make_get_dlq`, so anyone can now ask Max "why is scenario X failing?". Tests: new `test/make-triage.test.js`, 30 checks/18 groups, asserting blocked paths issue **zero writes** rather than just returning an error string; `test/make-watchdog.test.js` passes **UNMODIFIED** as the flag-off regression guard. Caught during test authoring: triage is fire-and-forget by design, so three "nothing was posted" assertions were passing vacuously until a microtask drain was added. **Rollout is staged and each step is independently safe:** (1) merge inert, then DM Max "list the DLQ entries on Make scenario 5148796" — this also verifies the token's `dlqs:read` scope and the `/dlqs` envelope shape with zero write risk; (2) `MAKE_TRIAGE_ENABLED=true` + `REMEDIATION_DRY_RUN=true` — judge the triage reasoning on real alerts before it can touch Make; (3) unset dry-run. **Unverified, isolated:** `POST /scenarios/{id}/start` (a 404 surfaces as a ❌ thread reply, not a crash) and whether `/dlqs` returns a usable timestamp — if that field is absent the 44h guard silently no-ops, so confirm the envelope in step 1 (the parser already accepts `dlqs`/`incompleteExecutions` and three timestamp field names). **Flagged, NOT fixed:** `runMondayGapDetection` declares `_correlationId` but references bare `correlationId` when posting → ReferenceError swallowed by its own catch, so **the Monday gap report has never posted whenever gaps exist**; same line passes `say = null` to `executeChannelPost`, which calls it unconditionally. Needs its own PR + verification.
- **2026-08-13 — Closer Monthly Scorecard: one shared Postgres view, three consumers (dash portal page + Max tool; REVI sync is a later phase).** Trigger: Ron wants "finish a month → one query per closer" (assigned / attended / no-shows / won / revenue), and every past divergence (June's calls-booked count dispute) came from each surface doing its own math. **Views (portal Supabase, dash migration `20260813120000_closer_month_scorecard_views.sql`, already applied):** `closer_month_scorecard` — per (closer, CR-calendar-month anchored on `scheduled_start`): calls_assigned, unique_prospects (dedupes rebooks), outcomes_logged/pending, showed/no_shows/rescheduled, AQC, won/lost/follow_up/DQ, revenue, show-rate + close-rate-on-shows. Semantics = `classifyOutcome` exactly (the broken `attended` boolean is ignored); iClosed-era rows flywheel-filtered in SQL via `iclosed_webhook_deliveries` slugs (mirrors `SALES_FLYWHEEL_SLUGS` — keep the two lists in sync when adding event types); `opp:%`/NULL-`scheduled_start` outcome-carrier rows excluded from months and surfaced by companion view `closer_month_unattributed` instead (revenue never silently drops — that view catches the Aug opp-carrier won deal from the Jul 27 audit). Both views `security_invoker=on`. Verified against the audited June/July truth markers from the leaderboard audit (slug-regression case included). **Max tool `closer_monthly_scorecard`** (this PR): reads the views via `portalPg` (read-only SQL — immune to the anon-vs-service key question), REVI overlay via `reviSupabase`: recordings matched to appointments by prospect email + call_date within 36h of scheduled_start (contract mirrored in dash's API route — keep in sync), upgrading pending→"verifiably happened" only, never inventing no-shows. Live-data check: a majority of one closer's pending July calls have a matching Fathom recording — the logging gap made visible. **Dash side ([PR #31](https://github.com/neurogrowth-cr/dash.neurogrowth.io/pull/31)):** `/admin/closer-scorecard` page (standalone-route pattern, `RevopsMemberSelect` picker, pending-outcome warning banner, unattributed line admin-only) + API route guarded by `revopsRequireAdminOrRevopsRoster('closer')` — admins see all, closers self-only. Later phases logged in the plan: setter monthly scorecard, REVI score sync into portal DB, pointing the weekly leaderboard at a weekly view variant.
- **2026-08-14 — Make watchdog DLQ signal damped (it was flapping in production).** The `dlqCount > 0` condition shipped in `f8931ac` is a *stateful* signal, unlike `isActive`: Make retries incomplete executions, so the count drops to zero and comes back. Tested raw, it fired three alert/all-clear pairs in twenty minutes on 2026-08-13 (05:00, 05:10, 05:20 on 5148796) — the fastest possible way to train the team to mute #ng-fullfillment-ops. **Fix:** both edges hysteresis-damped in `checkMakeScenarioHealth()` — a DLQ must persist `DLQ_CONFIRM_POLLS` (3 polls ≈ 30 min at the `*/10` cadence) before it alerts, and must stay clear the same span before the all-clear posts. A dip resets the streak, so a flapping queue never reaches the threshold at all. `isActive`/`isinvalid` are unambiguous and remain **immediate** — no damping on the urgent paths. **Verified live before the fix:** watchdog running since 2026-08-11 20:40 UTC, `checkMakeScenarioHealth` 292 runs / 0 errors, `checkBookingAlertDivergence` 97 runs / 0 errors / **0 divergence gaps ever** (Slack alerts + CAPI events are landing); 7 alerts fired to date, all `dlq` on 5148796. **Still open for Ron:** 5148796 reports `dlqCount: 2` — two incomplete executions parked since 2026-08-11 that need retry-or-discard in the Make UI (every logged execution since 2026-08-05 succeeded, so these are stragglers, not an ongoing break). **Tests:** `test/make-watchdog.test.js` 40/40 — adds the flapping case (12 oscillating polls → zero alerts), settle-then-alert, and premature-all-clear suppression; strike-rules 11/11, booking-gates 25/25. **Process note:** this session rebuilt the entire watchdog from scratch before discovering `f8931ac` had already shipped it — the local checkout was pinned at `a3fd289`, several commits behind. `git fetch` + check `origin/main` for the feature BEFORE building it, not after. The duplicate PR #38 was closed and its branch deleted; `main` was never touched.
- **2026-08-14 — Weekly recap rollout verified in production; one known caveat left on the daily lead metric.** Follow-up to the 2026-08-12 rebuild entry. [PR #41](https://github.com/neurogrowth-cr/ng-agent/pull/41) merged as `750ae75` (2026-08-13 05:04 UTC), Railway deploy `5fff1092` SUCCESS; the code is live and has since rolled forward through PRs #45–#49. **Production proof, not inference:** `metric_observations.ghl_new_contacts_today` recorded **5** on 2026-08-13 12:00 UTC — the first non-zero value after an unbroken run of `0` back through 2026-08-02, confirming the silent-zero scraper bug is dead. **Known caveat, deliberately left:** the daily metric still scrapes a *today* window (`_crDayBoundsUtc(0)`) while the anomaly cron runs at 6 AM CR, so it only ever counts leads from CR midnight–06:00 — that is why the first real value is 5, not a full-day number. It is internally consistent (baseline compares like with like, so σ-detection still works) but the label says "today" while it measures a 6-hour slice; every other sales scraper uses *yesterday* for exactly this reason. Fix is a one-line switch to `_crDayBoundsUtc(1)` at the cost of a 28-day baseline refill — **not yet done**. **The weekly recap itself is unaffected by that caveat**: it queries `lead_posts` live over the full window rather than summing daily snapshots (dry run on the merged code returned 70 leads / 9 booked / 5 held for Aug 10–12). **Process note:** PR #41 went stale three times (CONFLICTING twice) because `main` moved under it — pushing `branch:main` directly requires a fast-forward and loses every race in this repo. Push the branch, open the PR, merge promptly; rebase before each retry.

- **2026-08-14 — REVI outcome loop: pre-filled outcome proposals, one-tap confirm, agent write path to the portal.** The scorecard's accuracy ceiling is outcome-logging discipline, so the 4 PM CR unlogged-outcome reminder now closes its own gap: each unlogged call is matched to a REVI/Fathom recording (same 36h email+time contract as the scorecard overlay, one recording vouches once) and, when REVI has a read, the closer gets a per-call DM — recording proof + REVI's read + prospect signals — where ✅ logs the outcome (`source='revi_confirmed'`), ❌ dismisses, and a text reply corrects. **Trust tiers are structural:** REVI `stall`→proposes `follow_up`, `lost`→`lost`; `won` is NEVER proposable (verbal close ≠ payment — DM asks the closer to reply `won <amount>`); `pending`/no-recording calls stay on the classic aggregate nudge, and absence of a recording proves nothing. One proposal DM per appointment ever (agent_knowledge `outcome-proposal:{id}`), 5/closer/run cap, REVI outage degrades to the classic reminder. **Write path:** new `PORTAL_WRITER_DATABASE_URL` (scoped role `max_outcome_writer`: INSERT on `revops_sales_outcomes` + UPDATE(status,updated_at) on `revops_prospects`, BYPASSRLS, probe-verified it cannot read financials or update/delete existing rows) — deliberately separate from the read-only pool. `logOutcomeToPortal` is one transaction: insert `ON CONFLICT (appointment_id) DO NOTHING` (first-writer-wins — agent writes can never clobber ingest/human rows; conflict reports the existing outcome instead) + prospect-status promotion via **duplicated dash contract #3** (`nextProspectStatusForOutcome` mirrors dash `status-map.ts`/`outcome-map.ts` RANK + terminal rules — change BOTH or statuses drift; `no_show`→`appointment_held` is dash's rule, not a bug). New `log_call_outcome` agent tool for stated-outcome replies (`source='closer'`, won requires revenue, ambiguity returns candidates, never overwrites). Reaction Route 0 sits ahead of the campaign route because closers aren't in `SLACK_TO_GHL_USER` (its DM fallthrough would swallow their ✅); only intercepts `metadata.event_type='outcome_proposal'`. Portal side: `source` CHECK constraint extended (`closer|revi_confirmed|revi_inferred` — `revi_inferred` reserved/unwritten; dash PR #32 records it; found by a zero-write permission probe, not in prod). Tests: `test/outcome-loop.test.js` (27 cases, slice-compiled from index.js: status-merge terminal/rank guards, won-never-proposable, 36h matcher edges).

- **2026-08-14 (late) — REVI outcome loop verified live in production.** PR #51 merged + deployed; `PORTAL_WRITER_DATABASE_URL` set on ng-pm-MAX by Ron (Railway UI — the credential write is outside what Claude sessions can do; the env-change redeploy is patch-only, no rebuild). Post-deploy checks: clean boot, all crons registered incl. the 4 PM CR unlogged-outcome cron that now sends proposal DMs, `/health` 200, Make watchdog 0/3 PROD scenarios down. Companion dash PR #32 (source CHECK constraint record) merged. First live proposal run: next 4 PM CR cron after this entry. Watch on first cycles: proposals sent vs ✅-confirmed vs text-corrected (correction rate = REVI read accuracy), and whether closer outcome-logging coverage moves. Session lesson worth keeping (also in ci.yml now): GitHub does not honor `merge=union`, and a CONFLICTING PR gets NO Actions runs at all — `gh pr checks` reporting "no checks" on a fresh PR means check `mergeStateStatus` for DIRTY and rebase, not wait longer; and ng-agent's CI lists test files by name, so a new test file must be added to `.github/workflows/ci.yml` or it silently never runs in CI.
- **2026-08-14 — First live run of the rebuilt weekly recap fired correctly (Fri 5:00 PM CR, on schedule).** Closes the recap thread from the two entries above. Delivered to Ron's DM at 17:00:08 CR on the PR #49 build (the PR #50 docs deploy landed at 23:06 UTC, after the cron). Output for Aug 10–14: leads **126** (▲ +22 WoW) with a per-source split (Facebook 102 / Social 15 / Paid Social 6 / …), booked 14, held 7, no-shows 0, show rate 100%, closes 0, Meta spend $855.30 / 112 form leads / CPL $7.64, reconciliation Δ +14 vs the GHL lead feed, **3** clean σ-anomalies, 6 deduped client updates, plus the READ narrative. Compare the last broken run: "New contacts this week: 0", "Avg close rate: 0.1%", and 20 mid-sentence-truncated "anomalies" that were mostly client-ops noise. **The predicted baseline self-heal fired as forecast:** `New leads (Form/WA funnel, today) ↑ 5 (baseline 0.18 ± 0.94, +5.10σ)` — an artefact of the old fake zeros still in the 28-day window, not a real spike; it decays out by ~2026-09-10. **Two honest observations from the live output, neither a regression:** (1) the pipeline block shows "Booked this week: 14" alongside "Held: 7 … (13 awaiting outcome)" — 7+13=20 ≠ 14 because booked is the `booked_at` cohort and held/pending are the `scheduled_start` cohort, by design, but the two numbers sitting adjacent reads as an inconsistency and the labels could say so explicitly. (2) 13 of 20 scheduled calls had no outcome logged, so "Closes: 0 / close rate 0%" reflects logging discipline at least as much as sales performance — the same gap the outcome-escalation DM flagged (11 calls, mostly one closer) and what [PR #51](https://github.com/neurogrowth-cr/ng-agent/pull/51) (REVI outcome loop) is built to fix.
- **2026-08-15 — Weekly recap follow-ups closed out; `preview_weekly_recap` verified end-to-end.** Clears every open item from the three recap entries above. **(1) Cohort labels ([PR #54](https://github.com/neurogrowth-cr/ng-agent/pull/54), `71e08ed`).** The live run printed "Booked this week: 14" above "Held: 7 … 13 awaiting outcome" (7+13=20), which reads as an arithmetic error — booked is the `booked_at` cohort, held/pending the `scheduled_start` cohort. Both are now named, the scheduled total is printed, the show-rate denominator says what it was computed on ("of 9 decided"), and a ⚠️ line fires when pending ≥ held so "Close rate: 0%" is not misread as lost deals. **(2) Daily lead metric window (same PR).** `_scrapeGhlNewContactsToday` used a *today* window against a 6 AM CR cron, so it only ever counted CR midnight–06:00. **The undercount was ~6×, not marginal:** it logged 5 and 1 on days whose real full-day totals were **33**. Now `_crDayBoundsUtc(1)` like every other sales scraper; expect a one-time level shift up while the 28-day baseline refills (on top of the fake-zero decay already noted above). Metric key stays frozen. **(3) `preview_weekly_recap` verified live** — DM to Max returned the full recap, confirming the tool-dispatch path that the cron does not exercise. **Caveat worth knowing: Max PARAPHRASES the preview.** It reformatted the anomaly lines and appended its own commentary, because the preview returns through the conversational tool loop while the cron posts the string verbatim. Previews are therefore good for spot-checking *numbers*, not *formatting* — do not use one to verify a layout change. **Data reality check from that preview:** 149 leads, 0 closes, and 10 of 18 calls with no logged outcome — the recap is now honest, but the close-rate line will keep understating performance until outcome-logging discipline improves ([PR #51](https://github.com/neurogrowth-cr/ng-agent/pull/51) outcome loop targets exactly this; this week is the baseline to judge it against). **Process note:** [PR #52](https://github.com/neurogrowth-cr/ng-agent/pull/52) went CONFLICTING against PR #53 even though both only appended to the bottom of this file — **GitHub does not run the `merge=union` driver**, it only applies to local merges. A docs PR that collides must be rebased locally (where union resolves it) and force-pushed; the union guardrail does not save the PR itself.
- **2026-08-15 — Alert triage bridge is LIVE in dry-run; first real triage fired and correctly declined to act ([PR #47](https://github.com/neurogrowth-cr/ng-agent/pull/47) + [PR #49](https://github.com/neurogrowth-cr/ng-agent/pull/49) merged & deployed).** Closes the structural gap behind the 2026-08-11 watchdog alert: ~20 detectors in index.js each format a string, post to Slack, and stop — **none fed their result into `callClaude`, so Max never saw his own alerts.** Now: detector → `triageAlert()` (facts + the actions it authorizes + read-only tools) → Max investigates → ONE structured proposal via `finalTool` → posted to #ng-fullfillment-ops → any roster member ✅/❌ → executor. Generic by construction; Make DLQ/inactive is the only consumer wired. **Approval deliberately does NOT reuse `APPROVAL_NEEDED`/"reply yes"** — `pendingApprovals` is in-memory, 30-min TTL, dies on every deploy, keyed by approver only, stage-2 phrases exact-match English ("sí"/"yes do it" match nothing); alerts fire hours before approval, across redeploys. Uses the campaign-draft **emoji + Slack-message-metadata** pattern (restart-proof, no TTL) + an hourglass **claim-lock** closing the double-execute window the campaign flow leaves open. **PR #49 (parallel session, rebased onto #47 by this session — its actual commit, not hand-copied) damps the DLQ signal:** raw `dlqCount > 0` produced three alert/all-clear pairs in twenty minutes on 2026-08-13, so a DLQ must now persist `DLQ_CONFIRM_POLLS`=3 (~30 min) before it counts as a reason, and stay clear as long before the all-clear; `isActive`/`isinvalid` stay immediate. The two compose cleanly — `reason = hardReason || (dlqStreak >= 3 ? 'dlq' : null)` and triage rides the unchanged `if (reason && previous !== reason)` branch, so **triage fires once on a confirmed condition and never on a flap**. Writing the interaction test (13d–13f, neither PR covered it alone) exposed a bug in the triage test's own fetch stub: `/scenarios` returned the same page for every pagination offset, replaying the scenario ~1000× per sweep and defeating any per-poll streak; it had passed only because the other tests use reasons that alert immediately. **Retry safety derived from the live blueprint, not assumed:** both CAPI modules use deterministic event ids (`ghl_schedule_{{appointment_id}}`) so Meta dedups a replay — but only within 48h since `event_time` is `{{now}}`, hence a hard 44h refusal (`MAKE_DLQ_MAX_AGE_H`). Executors re-validate at EXECUTION time (retry re-lists the live queue, dropping hallucinated + already-cleared ids, cap 10; reactivate refuses `isinvalid` — restarting a broken blueprint just re-breaks on the next event, the 2026-08-04 shape). New read-only tools `make_list_dlqs`/`make_get_dlq` mean anyone can now ask Max "why is scenario X failing?". **Live state: `MAKE_TRIAGE_ENABLED=true` + `REMEDIATION_DRY_RUN=true` on ng-pm-MAX (Ron set both).** **First real triage, 2026-08-15 04:10 UTC:** a deliberately deactivated `[PROD]` smoke scenario alerted at 04:10:01 and triage returned `no_action` 11 seconds later — it read the scenario name, saw an empty DLQ and `isinvalid: false`, concluded no real contacts/appointments were affected, and **declined to reach for the `reactivate_scenario` action it had been handed.** That is the judgment the dry-run stage exists to check. Smoke scenario deleted; Make is clean. **The 2026-08-05 DLQ pair on 5148796 was diagnosed and is NOT a data-loss event:** both failed ~45s after blueprint `modify` events (04:54:02Z / 04:59:00Z) during that day's setter-line edit session, zero appointments were created or booked in the window, every execution since has succeeded — they are test artifacts, and the right action is **discard, not retry** (they are also 9 days old, so the 44h guard would refuse a retry anyway). **STILL UNTESTED (Ron chose to let a real incident prove it rather than perturb a live scenario): proposal → Slack metadata round-trip → ✅ → executor.** Two silent failure modes live there — if `metadata.event_payload` does not persist on a channel post, approval is dead AND dedup re-proposes every 10 min; and if the bot lacks `reactions:write`/`channels:history` in #ng-fullfillment-ops, a ✅ produces silence. Watch the first real proposal closely. **Flagged, NOT fixed:** `runMondayGapDetection` declares `_correlationId` but references bare `correlationId` when posting → ReferenceError swallowed by its own catch, so **the Monday gap report has never posted whenever gaps existed**; same line passes `say = null` to `executeChannelPost`, which calls it unconditionally. Needs its own PR + verification.

- **2026-08-15 — Status check on the two July financial-signal fixes: both verified holding, no action needed.** (1) **Purchase CAPI value** — Make scenario 5148801 "[PROD] GHL Opp Won → CAPI Purchase" has fired 4 successful Purchase events since the Jul 28 value-mapping fix (Jul 28, Jul 30, Aug 6, Aug 8), all carrying `custom_data_value` (opp `monetaryValue` → webhook `deal_value` → `2900` floor) + `USD` + order_id. Blueprint re-verified 2026-08-13 after the Aug 6 parallel-session edit — that edit only ADDED `country` to user_data (match quality); value mapping intact. The Meta Events Manager "value must be > 0" warning (2 valueless events sent Jul 27–28 between the GHL cutover and the fix) has aged out of the rolling window; the two bad events are unrepairable by design (resend = dedup-drop or double-count) and were left alone. (2) **Financial confidentiality guard** (`deliversToRonOnly` gate + `confidential` nightly-learning category, commit `369646d`) — working in production: two financial signals captured since deploy (Relay/Stripe ACH deposit Jul 31; Flor Lizano payment-problem note Aug 11), both stored `visibility='private'` under Ron's user_id and DM'd to Ron only; zero financial content in team channels since the original iClosed-balance leak. Docs-only entry; no code changed.

- **2026-08-17 — GHL appointment DELETION sweep (closes the invisible-cancellation gap).** Ron's sanity check found zero 🔴 cancel alerts since cutover; ground truth showed zero status-cancellations actually happened — but a DELETED appointment fires no workflow/webhook at all and would vanish silently from rosters. New `runApptDeletionSweep` cron (every 3h, 7:20–19:20 CR): probes upcoming ingested GHL appointments (yesterday → +45d, skips already-cancelled) via `GET /calendars/events/appointments/{id}`; 404 or `deleted:true` → portal RPC `ng_mark_appt_deleted` (migration `ng_mark_appt_deleted_rpc`, SECURITY DEFINER, anon-exec) which atomically dedupes via `ng_appt_slack_alerts`, routes a 🔴 alert through the existing outbox → Make 5822303 pipeline labeled "(by team — appointment DELETED in GHL, not cancelled)", and marks the revops row cancelled (attended=false + qualification_snapshot merge, mirroring dash's cancelled mode). Direction labels now cover: by client (cancel link) / by team (status) / by team (deleted). Classifier `classifyGhlApptProbe` is fail-safe (500/401/malformed → 'error', never 'deleted' — a bad token cannot mass-cancel). Tests: `test/appt-deletion-sweep.test.js` (12 checks, extract-real-code pattern) + added to ci.yml. NOT covered (needs closer Google-calendar sharing with Max's account): client *declining the Google Calendar invite* — GHL doesn't see declines; proposal pending with Ron.

- **2026-08-17 (close-out) — Cancellation-visibility arc CLOSED; deletion sweep DEPLOYED.** PR #60 merged (`17c9c0b`), Railway deploy `268c77ef` SUCCESS, clean boot, all crons registered; first deletion sweep 7:20 PM CR 2026-08-17. Full cancellation matrix now live: by client (cancel link → webhook 🔴), by team (Cancelled status → webhook 🔴), by team (DELETED → sweep 🔴 + revops corrected). Sanity-check context that triggered this: zero 🔴 alerts since cutover was VERIFIED CORRECT (zero status-cancellations actually occurred across all 3 sales calendars, method validated against July's 4 known cancels; zero deletions among ingested appts either) — iClosed-era ran ~7 cancels/week, so the drop is behavioral: likely no client cancel link in GHL reminder flows (unverified) and/or team habits. DEFERRED by Ron: GCal invite-decline detection (needs Jose/Jonathan to share calendars read-only with Max's Google account — alert pipeline already supports a "(by client — declined invite)" label when access exists). This also closes the whole 2026-07-31 → 08-17 post-cutover arc: alerts+dedupe+retry, reschedule ingestion, workflow fixes, 14/14 outcome recovery, admin replay route (dash PR #33), deletion sweep.

- **2026-08-18 — Paso 1 retired for closers: Max marks Showed from REVI, and every unlogged call gets its own outcome card in GHL's own words.** Ron flagged that the outcome loop had degraded to a flat bullet list with one shared ✅, and asked for (a) one entry per call, (b) Max consulting the REVI schema to *compute* an outcome rather than echo a label, (c) the vocabulary to match the GHL outcomes closers actually set, and (d) show/no-show to stop being manual work. **Measured first, built second.** Paso 1 compliance was **~2%**: on the main sales calendar (`fYQJCzbk4hvV0brpJqoE`) 40 of 44 August appointments still sat at the default `confirmed` (1 `showed`, 2 `noshow`, 1 `cancelled`) — which is *why* `revops_appointments.attended` is null almost everywhere and show rate has to be derived from outcomes ([index.js:3348](../index.js)). **The decisive measurement:** over every post-cutover call with a logged outcome, REVI recording ⇒ held held 16/16 and **0 of 2 no-shows had a recording** — but *absence* proved nothing (25 of 27 recording-less calls were actually HELD; REVI covers ~39% of held calls). So the originally-planned "REVI has no recording — did they show?" DM was **cut before shipping**: it would have fired on ~60% of calls and recreated the SOP inside Slack. Attendance is instead **derived from the outcome** — every outcome except `no_show` means the call happened (`appointmentStatusForOutcome`), so one answer settles Paso 1 and Paso 2 together and the closer is never asked about attendance at all. **Layer 1 — `runAppointmentStatusSync`** (3 PM CR, 1h before the cards): cascade `already-terminal → cancelled-upstream → rescheduled → REVI recording ⇒ write showed → defer`. Writes **only** `showed`, never `noshow`/`cancelled`. **Stale-status detection (added after Ron asked how a 74-min recording could be a no-show):** GHL REUSES the appointment row on reschedule, so attendance set for an earlier booking rides onto the new date. Daniela Bruno no-showed **Jul 24**, was marked `noshow` Jul 28, rebooked to **Aug 4** (`rescheduledAt` Jul 31 20:29:08) — and the stale `noshow` followed her onto the Aug 4 row, which she then attended for 74 recorded minutes. The row's `dateUpdated` is Jul 31, four days BEFORE the call it describes. Rule: attendance (`showed`/`noshow`) written before `startTime` cannot describe that call, so it is a leftover, not an answer — the row falls through to the evidence. `cancelled` is exempt (cancelling before a call is normal). **3 of 16** post-cutover terminal statuses were stale this way, all rescheduled rows: Daniela (`noshow`), José Manuel Magaña (`noshow`), Melina Yáñez (`showed`). A genuine **conflict** — DM'd to Ron, never flipped — is now only a status written AFTER the call that disagrees with a recording. **Ships in dry-run**; `APPT_STATUS_SYNC_MODE=live` arms it and is the kill switch. **Layer 2 — outcome cards:** every unlogged call now gets its own flat DM (was: only calls with a matched recording AND `deal_outcome ∈ {stall,lost,won}` — which is why Aura Bonilla, `deal_outcome=pending` with a deposit agreed on the call, only ever appeared as a bullet). Max now reads `coaching_json` (`deal.status` alive/dead, `deal.recovery`, `icp_fit`, `cash_collected_usd`) and `revi.funnel_events` — **neither was ever read before**; the old proposal hung entirely on `deal_outcome`, which reads `stall` on 91 of 130 calls and `pending` on 27. GHL stage evidence is now read **independently of REVI**, since the closer moved that card by hand. **Vocabulary contract `GHL_OUTCOME_STAGES`:** cards name the literal stage the ✅ lands in (Open Deal / Closed / No Fit / Lost), and the two pipelines are **not** symmetric — VSL merges Lost and No Fit into one stage, so a VSL card offers 3 actions where Appointment Setting offers 4. Closers type short tokens (`open deal`, `no fit`) never the long stage label. **`won` is still never one-tap** in any path. **Confidence gate:** only fact-backed reads (high) are ✅-able; `ghl_stage_noshow` is deliberately **medium** because "No show / Rescheduling" merges two different answers. **One ✅ now writes three places:** portal outcome row → prospect status → GHL opportunity stage (reusing the strike mover's `ghlMoveOpportunityStage`, generalised to take a `pipelineId`) → GHL appointment status. Portal first and outside the transaction, so a CRM failure can never roll back a logged outcome; the card reports honestly when the move fails. **Bug found and fixed in passing:** `qualification_snapshot.ghl.cancelled` (the cancelled-call guard in the reminder loop) **never matched a single row** — dash writes the flag at the TOP level (`{cancelled: true, reason: 'deleted_in_ghl'}`); `.ghl` only ever carries `opportunity_source`. Cancelled calls were being nagged. Now `isAppointmentCancelled()` checks all three shapes. **Nag fatigue:** re-nudges are a thread bump on the existing card (capped at 4 days), not a fresh DM — Ron's screenshot showed "reminded 14×". **Dry-run against live Aug 1–18:** Layer 1 → 45 considered, **5 `showed`** (4 by recording + 1 stale-status supersede), **0 conflicts**, 2 stale-status defers, 1 rescheduled skip, 37 deferred to the card. Before stale detection this run produced 1 conflict and wrongly treated 2 stale rows as settled. Layer 2 → **11 of 20** unlogged calls now carry a concrete read (was: near zero). **Recipe spec:** `~/automations/ops/recipes/appointment-status-sync.md` (criteria filled, per the recipe standard). New agent tool `set_appointment_status` covers the calls REVI cannot read. **Watch:** first 3 PM dry-run log line; the conflict DM; then arm `APPT_STATUS_SYNC_MODE=live` and confirm one real write via `GET /calendars/events`; whether `attended` starts populating through dash ingestion (the outcome-derived fallback at index.js:3348 stays until it does).

- **2026-08-18 (same session) — REVI prospect notes → GHL contacts (Ron's ask after seeing Daniela's read).** Ron: "is there a way REVI can place a note like this on all cards/prospects who talked to our closers?" New cron `runReviProspectNotesSync` (2 PM CR, **dry-run**, `REVI_NOTES_MODE=live` arms it): sweeps `revi.closer_call_scores` (7d window), resolves the GHL contact via `revops_prospects.ghl_contact_id` by email, posts one Spanish note per scored call — deal VIVO/MUERTO, señal de compra, objeción, timeline, presupuesto, decisor, ICP fit, próximos pasos (from `coaching_json.deal.recovery`), recording link, and an explicit "Generado por REVI" disclaimer. Once per recording ever (agent_knowledge `revi-note:{fathom_recording_id}`); a failed POST does NOT write the marker, so it self-retries next run. Builder `buildReviProspectNote` is pure and inside the test slice. Dry run: 3 scored calls in window (Aura, Santi, Andrés), all 3 contacts resolved, notes ~2k chars, clean render. Notes API confirmed working on Max's key (setters' pre-call notes live on the same surface). Recipe: `~/automations/ops/recipes/revi-prospect-notes.md`. **Backfill of the ~127 older scored calls deliberately NOT swept** — separate decision once live runs are clean. Also this session: **Daniela Bruno's stale `noshow` corrected to `showed` by hand** (GHL PUT verified by independent GET; webhook chain fired in 3s and portal `attended` flipped to true — full pipeline proven live), after the stale-status detector shipped (see previous entry).

- **2026-08-18 (evening) — First live contact with the outcome cards surfaced two bugs; both fixed.** Within an hour of the 4 PM card run: **(1) Aura Bonilla's ✅ logged the outcome but couldn't move the GHL card** — `revops_prospects.ghl_opportunity_id` is only populated on 58 of 111 post-cutover prospects (dash fills it inconsistently), so `logOutcomeToPortal` had no opp to move. Fix: `ghlFindSalesOpportunityByContact` fallback — 109/111 prospects carry `ghl_contact_id`, and GHL's opportunity search resolves the card from that (scoped to the two sales pipelines, open-first, newest-first). **(2) A typed reply in a card's thread was invisible** — Ron replied `no show` under Jessika Brown's card and Max asked "¿para cuál prospecto?": the DM handler routed thread replies to the LLM with zero parent context. Fix: `handleOutcomeCardThreadReply` intercepts replies in threads whose parent metadata is `outcome_proposal` — `parseOutcomeReply` (pure, tested) resolves `won 3500` / `open deal` / `lost` / `no fit` / `no show` (+ `dq`, `follow up`, `3.5k` amounts) deterministically against the card's `appointment_id`, writing through the same `logOutcomeToPortal` path (portal → prospect status → opp stage → attendance). A conversational reply falls through to the LLM WITH the card's prospect injected as context ("never ask which prospect"). Ownership enforced (owning closer or Ron). Guards: a sentence merely containing "lost" is not a command (≤80 chars, anchored patterns); bare `won` asks for the amount. Also softened the benign `channel_not_found` pre-route log (unreadable non-card DM reactions were logging as errors 3× in a row; root cause of who reacts there still unknown — watch). Manual cleanup after the fix: Aura's GHL card moved to Open Deal by hand; Jessika's `no_show` logged per Ron's in-thread instruction.

- **2026-08-18 (close-out) — Outcome-loop rework ARMED and verified live end-to-end; evening ops re-sequenced.** Full arc from Ron's "thought this was going to be more dynamic" message to production, one day. **Armed:** both flags live (`APPT_STATUS_SYNC_MODE=live`, `REVI_NOTES_MODE=live`, set by Ron via CLI — the permission classifier still blocks Claude from Railway variable writes, same as 2026-08-14); restart `dfcad5c8` 22:46Z confirmed the re-sequenced evening in the boot log: `Daily Closer Outcome Reminder` (renamed + reworded outcomes-only, 8 PM CR) and `Sales EOD Report` moved 6:30→**9 PM CR** (Ron's call: nudge at 8, EOD reads a logged day at 9). **Recipe gates passed before arming:** 2 PM notes dry-run `✅ ALL GREEN — candidates 3, posted 3, no-contact 0` (Aura/Santi/Andrés, contacts resolved); 3 PM sweep dry-run `✅ ALL GREEN — considered 41, marked showed 4, skipped 0, deferred 37` (Mia, Hector, Mark, Aura queued; 0 conflicts — Daniela aged out of the 14d window hours earlier, hand-fixed that morning). **4 PM cards went live same day:** Jose 5 cards + 12 aggregate, Ron 3 cards, 16-call escalation. First live hour surfaced the two bugs fixed in PR #67 (see prior entry); manual cleanup done (Aura's opp → Open Deal; Jessika `no_show` logged + GHL noshow set — she has NO opp card at all, the hand-backfilled Intro booking never got one). **Correction for the record:** dynamic crons DO run in America/Costa_Rica — the tz option sits at the END of the 400-line `cron.schedule` call ([index.js:1554](../index.js)); a mid-session read of only the opening line produced a false "all 10 crons are 6h early" alarm, retracted before anything shipped. Do not "fix" dynamic-cron timezones. **Watch next:** first live 2 PM notes + 3 PM `showed` writes (2026-08-19), `attended` populating via dash, the `channel_not_found` reaction source (still unidentified), REVI-notes backfill decision (~127 older calls), divergence check for the sweep.
- **2026-08-18 — The 14-day clock now starts at the real activation call, read from the dashboard CRM.** **The find:** every Day-N figure Max printed was optimistic. `resolveDayAnchor` anchored Phase 1/2 on `customer_activities.completed_at` for the "Activation Call Completed" template — a fulfillment tick, not the call. Across the 16 clients with both dates the tick lagged the real call by a **median of 6 days, max 40** (Hidro Vital: called May 15, ticked Jun 24). On a 14-day window that is most of it, and it always errs toward "on track" — a client 20 days past their call whose box was ticked 5 days ago read as Day 5. **Shipped:** anchor priority is stabilization → real activation date → checkbox → portal creation, with the checkbox fallback **relabelled** `since activation call (checkbox)` so a soft number announces itself. All 4 `resolveDayAnchor` call sites (client status, portal alerts, Monday gap detection, fulfillment standup) plus the two inline KPI scrapers (`_scrapePhaseCycleP50`, `_scrapeDay7AtRiskCount` — both also had to start selecting `email`) share one `loadActivationDates()` lookup, two queries per report rather than per client. `SYSTEM_PROMPT_DATA_MAP` repointed, and it now tells Max to say so when he falls back to the checkbox. **Reads the dashboard CRM only — no GHL.** Source order: (1) `flywheel_activation_workflow.activation_call_done` **when `metadata.source = 'ghl_webhook'`** — the canonical anchor (its table comment has documented the 14-day count since March, and the B&R clock spec anchors billing on it); (2) `flywheel_ai_onboarding.activation_call_scheduled_for`, the booking-side date. **The metadata filter is load-bearing:** 19 of the 20 `activation_call_done` rows were ticked by hand and carry a bulk timestamp (calls held in May stamped 2026-06-19), so trusting `completed_at` blindly would reintroduce the exact drift this fixes. Only 1 row is webhook-sourced today; that number grows as the dash-side route fills in. **Architecture correction (Ron, 2026-08-18): fulfillment state never goes on a GHL contact — once a prospect becomes a client the dashboard CRM owns them.** The first version of this work recorded the call as a GHL contact custom field + tags and had Max sync those back daily. That was the wrong side of the sales/fulfillment line and, worse, **redundant**: the CRM already modelled `activation_call_scheduled` / `activation_call_done`, and `dash.neurogrowth.io/docs/cursor-prompts/done/bnr_clock_activation_call.md` (CEO-directed, same week) already specified this anchor and noted the GHL webhook **already receives activation-calendar events and drops them as non-allowlisted** — the sanctioned fix is a routing change in dash, not new ingestion. The sync, the GHL date normaliser and their cron were removed before merge; the GHL activation *calendar* stays, because the dash webhook consumes its appointment events. **Also corrected: scheduled ≠ held.** The first version anchored on the scheduled date; the B&R spec anchors on the call actually taking place (skip and a 30-day cap as fallbacks) because it drives the 90-day billing window — a no-showed call must not start a paid clock. **Data:** 19 historical clients carry Fathom-verified real dates in the CRM (Selva/Esteban Ossenbach and Johan Solis have no recording — ask Josue). **Known limitation:** 10 of the 12 current phase_1 clients had their activation calls before any of this existed, so they keep the checkbox anchor — now visibly labelled — until the webhook route or a manual date fills them in. **Tests:** `test/day-anchor.test.js`, 10 cases slice-compiled from index.js (the 40-day Hidro Vital gap, the overdue flip, day-boundary behaviour in both directions), wired into `.github/workflows/ci.yml` — this repo lists test files by name, so a new file silently never runs otherwise. Full suite green (8 files). **Open for Ron:** retire the two now-redundant GHL workflows + contact field/tags, and decide whether the activation calendar keeps `shouldAssignContactToTeamMember: true`, which reassigns a booking client's contact owner to Josue.

- **2026-08-18 — Won-deal handoff notes: closing-call executive summary → fulfillment CRM (ships dry-run).** Trigger: Ron — fulfillment onboards won clients with zero context from the closing call; the answer to "Max or REVI?" is **Max generates and places, REVI untouched, dash only displays**. New cron `runWonHandoffNotes` (9 AM + 5 PM CR, `WON_HANDOFF_MODE=live` arms it — dry-run default): sweeps `revops_sales_outcomes` wons (30d window — catches BOTH writers, closer-typed and GHL webhook), matches the closing call via the existing 36h `matchRecordingToCall` contract (synthetic `opp:` rows fall back to email ±14d, ambiguity skips), one-shot Claude call condenses `coaching_json` into a 6–10 line Spanish note (prompt bans closer coaching/scores/money — team-visible surface), and writes through ONE new portal RPC `ng_apply_won_handoff_note(appointment_id, note)` (SECURITY DEFINER, EXECUTE only for `max_outcome_writer`; applied to prod + probe-verified: fake id → `no_won_outcome`, marker-less → `missing_marker`, null note → `no_note`; ACL = postgres/service_role/max_outcome_writer only). RPC is idempotent and never clobbers: `outcome.notes` filled only when blank, one `closing_call_summary` activity per appointment (appointment_id kept in `counts` jsonb — table has no appt FK), `customer_closing_data.internal_notes` gets one marker-guarded APPEND (`RESUMEN LLAMADA DE CIERRE (REVI` is the guard) — that field is what fulfillment actually reads (32/66 filled by hand). Placement resolves prospect→client via `client_dashboard_id` bridge link first, exact-email fallback; `prospect_only` results retry daily until the client is provisioned or the won ages out. State machine in agent_knowledge `won-handoff:{appointment_id}` (placed/already_placed/gave_up terminal; no-recording waits 7d then gives up loudly — never fabricates). Recording-lookup facts: REVI coverage starts 2026-04-04; 4 of 7 post-REVI wons matched at build time; 0 of 38 won outcomes had notes. Dash side (same-day PR): migration file for the RPC + read-only "Resumen llamada de cierre" block in `ProspectDetailDrawer` (activities were already fetched and discarded). Tests: `test/won-handoff-notes.test.js` (extraction pattern; ADDED TO ci.yml — CI lists test files by name). Recipe: `~/automations/ops/recipes/won-handoff-notes.md`. Arming = Ron flips `WON_HANDOFF_MODE=live` in Railway after eyeballing one dry-run log.

- **2026-08-19 — Activation-anchor audit: swept every report and job, found two surfaces still counting from the wrong date.** Follow-up to PR #63, which fixed the anchor but only at the surfaces that already called `resolveDayAnchor`. Audited all 23 `run*` jobs, 10 active Supabase scheduled tasks, the 4 fulfillment KPI metrics, and every client-facing tool. **Already correct (inherit #63 automatically):** `get_client_status`, `get_portal_alerts` (and therefore the **Fulfillment EOD Pulse** precompute, which calls it), `runMondayGapDetection`, `runFulfillmentStandup`, `phase1/phase2_cycle_days_p50` + `day7_at_risk_count` (so the fulfillment anomaly detector too), and **Friday Delivery Wrap-Up** + **Ron Weekly Ops Digest**, which have no precompute and reach the data through the fixed tools. Worth noting: those two prompts already *demanded* activation anchoring — Ron Weekly Ops Digest literally says "Day count anchored to activation call" — so before #63 they were **claiming** an anchor the data path did not provide. **Fixed here: (1) `runWeeklyPortalTrends` (ACTIVE, Fri 4:30 PM CR)** computed `avgDaysToLive` from `client_dashboards.created_at` and published it as "Avg days since onboarding for live clients". Two problems: it mixed pre-activation waiting time into a delivery metric, and it `upsertKnowledge`d the result weekly under `intel`, so a mis-anchored number entered the knowledge base as a fact Max would later quote. Now counts from the activation call, averages **only** over clients that have one, and states the sample size (`N of M live clients have a recorded call date`) instead of hiding how thin the average is. **(2) `runProactiveDMs` (currently DISABLED)** — the Day-7/Day-14 warning DMs counted from `created_at`, i.e. the job whose entire purpose is the 14-day window was not anchored on it. Rewired, and display now shares one `dayNFor` helper with the classification loop — fixing only the loop would have produced a DM reading "Day 5" for a client selected as a Day-14 warning. **Flagged, NOT changed:** its thresholds are exact equality (`daysSince === 13`), so any day the cron does not run is a warning nobody ever receives — convert to "crossed and not yet notified" before re-enabling. **Also flagged, no code change:** `get_ops_tracker`'s `launch_history` tab is a hand-maintained Google Sheet of activation/QA/launch dates — a **second source** that can now disagree with the CRM. Decide precedence (CRM should win) or use it as a cross-check; today nothing reconciles them.

- **2026-08-19 — Launch-window warnings de-fragilised, and the ops sheet measured against the CRM: they have already drifted, badly.** Two follow-ups from the activation-anchor audit. **(1) `runProactiveDMs` thresholds.** The Day-7 and Day-14 warnings fired on **exact day equality** (`daysSince === 13`), so any day the job did not run — deploy, outage, weekend, or the cron sitting disabled as it is now — was a warning nobody ever received, indistinguishable from a client who never needed one. Now "crossed the threshold and **not yet notified**", with the notified-key stored in `agent_knowledge` (`proactive-dm:{clientId}:{milestone}:{anchorDate}`) — **the anchor date is in the key, so a rescheduled activation call re-arms both milestones**, which is correct: a new call date is a new 14-day window. Day 14 wins over Day 7 for a client who crossed both while the job was down (one DM, the urgent one). Keys are written **only after the DMs actually send** — marking them first would turn a Slack failure into permanent suppression, the exact silent-miss being removed. DM copy now prints the real day number instead of hardcoded "(Day 13)"/"(Day 6)" and no longer claims the client hits the deadline "today". Pure helper `proactiveMilestoneDue` extracted + `test/proactive-milestones.test.js` (8 cases, verified to FAIL 5/8 against the old exact-equality logic before being accepted). **(2) Ops tracker precedence — decided: the CRM wins, and it is not academic.** Compared the sheet's `Fecha de Activación` against the CRM for all 19 clients that have both: **they disagree for 14, median 11 days, max 40 (Hidro Vital), and the sheet is LATER in 14 of 14.** The sheet's dates match the portal's activation *checkbox* exactly (Hidro Vital 6/24, UMC 7/13, Licita 7/15, Global Apex 6/24…) — it records the same fulfillment tick, not the call. **Consequence for the team's own KPI: the sheet's "Días para Lanzar" understates time-to-launch — mean 15.5d reported vs 25.6d actual across 13 launched clients, and 6 of those 13 look inside the 14-day promise but are not.** Precedence + the measurement are now written into both `SYSTEM_PROMPT_DATA_MAP` and the `get_ops_tracker` tool description, so Max prefers the CRM, reports both when they disagree, and never quotes "Días para Lanzar" as real time-to-launch without the caveat. **Bonus find:** the sheet's notes column lists "Selva Design — 16 Jul", one of the two clients with no CRM activation date — worth confirming with Josue rather than writing in blind, since the same column's table row is blank (the sheet disagrees with itself). Consorcio CSM (Johan Solis) remains genuinely unknown.

- **2026-08-19 — Outcome-loop release VERIFIED live in production; one cosmetic leak found and machine-checked away.** Morning verification of yesterday's arc (scheduled the night before as three self-firing checks — two local, one cloud routine — so the verification did not depend on anyone remembering). **All green:** Railway deploy queue drained overnight (the GitHub incident cleared); current deployment `808a8a35` SUCCESS on `256c24c`, well past the `fbc19db` bar. Boot log confirms `Daily Closer Outcome Reminder (0 20 * * 1-6)` and `Sales EOD Report (0 21 * * *)`. **Both re-sequenced jobs fired on time:** nudge 8:00:14 PM CST, EOD 9:00:33 PM CST (not the old 6:30). **The nudge is the proof the whole design works** — it counted 6 real calls, named the 5 still needing an outcome, correctly excluded Alberto García, offered ONLY the GHL vocabulary (Open Deal / Closed / No Fit / Lost) plus the DM-card reply tokens, asked nothing about attendance, and stated outright *"attendance already handled by REVI/Max"*. Paso 1 is visibly retired in production. **Defect found:** that same nudge posted with the model's reasoning preamble attached (*"6 calls today. Alberto García is already marked no-show… Here is the final reminder text: ---"*) — a scheduled task's reply posts VERBATIM, so the team read the scaffolding. The prompt already said "return ONLY the final reminder text"; the model complied and framed it anyway. **Fixed machine-checked, not by rewording** ([PR #77](https://github.com/neurogrowth-cr/ng-agent/pull/77), `f340b86`): `TASK_LEAD_ANCHOR` declares each task's known FIRST token (`<!subteam^`) and `trimToLeadAnchor` cuts everything before it, on the first reply AND the re-prompt — whitelist, same reasoning as `TASK_HEADERS`. A reply with NO anchor passes through untouched on purpose: that is a different failure (no deliverable at all) and belongs to `validateFinalReport`'s re-prompt, not a silent blanking. **Second bug found while fixing the first, self-inflicted:** renaming the task on 2026-08-18 left a stale `TASK_HEADERS` key — the lookup returned `undefined`, so the nudge stopped matching its own short-nudge rule and silently fell through to the unknown-task 300-char branch. Nothing errored; the rule just stopped applying. Both now carry comments. `test/cron-preamble.test.js` (16 checks) uses the REAL leaked message as its fixture, is registered in `ci.yml`, and is mutation-checked (disabling the cut reddens 6). DB prompt also tightened as a second layer. **Also merged today from yesterday's backlog:** [#59](https://github.com/neurogrowth-cr/ng-agent/pull/59) GHL 429 retry (rebased) and [#63](https://github.com/neurogrowth-cr/ng-agent/pull/63) activation-date clock (had a CI-list conflict with #59 — both sessions appended a test file; resolved by keeping both). **Still open:** first LIVE notes/sweep writes (2 PM / 3 PM CR today, check queued); `attended` propagating via dash; the unidentified `channel_not_found` reactor; REVI-notes backfill (~127 older calls); divergence check for the status sweep.

- **2026-08-19 — REVI notes bind to the appointment, not just the contact (Ron's ask: "can we put it on the opportunity/appointment card?").** **API findings (probed live, test note created and deleted):** GHL has **no notes endpoint on opportunities** (`GET /opportunities/{id}/notes` → 404) — notes live on the contact and the opportunity view surfaces them, so "on the opportunity card" is not a separate write target. But a note **can** carry `relations: [{objectKey:'appointment', recordId}]`, which is exactly the shape GHL writes when someone adds a note from the appointment page (Sebastián's pre-call note on Aura carries it). So it is ONE note related to both, not a duplicate — and **no extra LLM tokens**: `buildReviProspectNote` is a pure string builder, so this is +1 GHL field, not another model call. **Why it matters beyond cosmetics:** a prospect with three calls was accumulating three REVI notes on one contact with nothing saying which call each described. New pure `nearestAppointmentToCall` (same 36h contract as `matchRecordingToCall`, its mirror image) resolves the call → appointment, skipping rows with no writable event (`opp:` synthetics, pre-cutover nulls). **Live dry run, 5 scored calls in window: 2 bound, 3 correctly fell back to contact-only** — and the fallback is right, not a gap: Santi/Andrés/Ruben's appointment rows now point at FUTURE calls (reschedule drift — GHL reuses the row), so binding a past call's read to an upcoming appointment would be actively misleading. Alberto García is the win the feature was for: 2 appointments on record, correctly bound to the Aug 18 one rather than the Aug 24 booking. Contact-only remains the honest default when no past appointment exists.

- **2026-08-19 — REVI-notes BACKFILL enabled as a knob, not a separate script.** Ron asked to run the backfill alongside today's 2 PM live run. **Measured first, and the headline number was wrong:** of 127 older scored calls, only **27** would have posted — **100 were skipped for having no `ghl_contact_id`** (8 no prospect row, 92 prospect-exists-but-null). The portal only backfilled that column for GHL-native rows, so pre-cutover prospects look contactless even though GHL knows them. Probe confirmed `GET /contacts/search/duplicate?email=` resolves them (2 of 3 samples). Skipped calls skew OLD — Apr 13 / May 17 / Jun 22 / Jul 47 / Aug 1 — so a full backfill is mostly 3-4 month old cold prospects. **Built:** `REVI_NOTES_LOOKBACK_DAYS` (default 7) makes a backfill a WIDER RUN OF THE SAME PATH rather than a second script that can drift; `REVI_NOTES_MAX_PER_RUN` (default 40) + a 250 ms throttle keep a wide run off GHL's rate limiter, and since the `revi-note:{fathom_recording_id}` marker makes it resumable, a capped run simply drains over following days. Rows are now sorted NEWEST FIRST so a capped backfill lands the most useful calls first instead of starting in April. `ghlFindContactByEmail` fallback recovers the 92 — and permanently improves the DAILY job too (today's normal run had already skipped one call for a missing prospect row). Appointment lookup now tolerates a missing prospect row (contact-only note). Verdict line reports what the cap deferred.

- **2026-08-19 — Production liveness verified end-to-end (Railway + Socket Mode), and the unclaimed-lead sweep was found crying wolf for 22 days.** A prior session had left Railway liveness marked "not verified" after `curl` to `ng-agent-production.up.railway.app` returned 403. **That 403 was this sandbox's own proxy, not the container** — `*.up.railway.app` is not reachable from a Claude Code session, while `slack.com` and the Railway GraphQL API both are. **Check liveness through the Railway MCP, never curl.** Live state: deployment `6edd0ef1` SUCCESS, commit `da2b607` (#82), booted 17:06 UTC, health server on 8080, 10 dynamic + ~21 total crons registered, `Make health: checked 4 [PROD] scenario(s), 0 down` at 17:10 and 17:20, and an inbound GHL webhook parsed end-to-end at 17:13. **Socket Mode is genuinely attached, not merely token-valid** — three independent signals: `slack.start()` is awaited before `NeuroGrowth PM Agent is running.` prints and a failure `process.exit(1)`s, so that log line IS the connection proof; `attachSocketWatchdog`'s `[socket] disconnected` never appears post-boot; `apps.connections.open` → `ok:true` and `auth.test` → `max_ai_-_ng` / `U0AMMFVDUUV`. **Note for future log reading:** only a handful of crons print `Registered static cron` — ~17 more register silently at module load (including both stale-lead jobs), so counting boot log lines undercounts what is actually running. **Lead-claim health, 30 days:** 745 leads posted, **742 claimed (99.6%)** — 639 by ✋ reaction, 119 `ghl_direct`. The claim flow is not the problem; **latency is**: median time-to-claim 144 min, and **347 of 639 reaction claims (54%) took over 2 hours** on paid FB lead-form traffic. **The "GHL lead received but setter not resolved" log is NOT a dropped lead** — an empty `assignedTo` routes to the ✋ claim path by design ([index.js](../index.js) `handleGHLWebhook`), which is why 99.6% still land. **The one genuinely stale row was a false positive:** SANTIAGO LONDONO U, flagged unclaimed for 542h, is a real lead (FB lead form, +57 mobile, self-booked with Jose Carranza Jul 28) that was *finished* — GHL carries `no-fit` + `cancelled`, so no ✋ was ever needed. Fixed in `fix/stale-lead-sweep-terminal-tags`: the sweep now drops leads whose contact carries a terminal disposition (won-deal / no-fit / generic-lost / activation-done / call-showed, verified against the live location tag list), fails SAFE toward noise on any GHL error, caps at 50 lookups oldest-first with the overflow still reported, and keeps `cancelled` NON-terminal on its own. `STALE_LEAD_TERMINAL_TAGS` overrides without a deploy. `test/stale-lead-sweep.test.js` 18 checks, mutation-verified. **One DB row deleted by hand:** `lead_posts` `ca9c6bd5` "David  Test" (`david.mckinney@neurogrowth.io`, our own domain) — a genuine test row. Santiago's row was deliberately KEPT: `lead_posts` is the authoritative source for lead volume in the weekly recap and standups, so deleting a real paid-FB lead would quietly understate volume and destroy its attribution record.
- **2026-08-19 — Won-handoff notes: the closing call is NOT the appointment the outcome points at (fix on top of #69).** Ron's correction: "sometimes the prospect does not close the deal right there in the call, but later on follow up — however, we need the context of the call anyways." The 36h appointment join (`matchRecordingToCall`, correct for PROVING attendance) is the wrong contract for fetching call CONTEXT: dash attaches a won outcome to the prospect's LATEST appointment, so the appointment's date is not evidence of when the closing call happened. Live proof: **Fernando Corella won 2026-08-06 against an appointment dated 2026-08-12, real call 2026-08-04** — the 36h join missed it by 8 days and would have discarded a real summary permanently. Replaced with `findClosingCallRecording` + pure `pickClosingCall`: scope by prospect email, take the NEWEST scored call at or before the win (60d lookback, +2d forward pad so a close logged hours before the call is scored still matches, while a post-sale onboarding/quicksync call can never masquerade as the closing call). Same rule now covers same-day closes, follow-up closes weeks later, AND synthetic `opp:` rows — `findOppRowRecording` and its "ambiguity skips" rule deleted (multi-call sales are normal; the LAST call before the win is the answer, not a reason to skip). Note header now carries the RECORDING's date, not the appointment's (a header dated Aug 12 over an Aug 4 call is a lie to the reader). **Second bug fixed: dry-run was mutating state** — the `gave_up`/`no_recording` markers were written regardless of mode, so a dry run would have sealed 3 deals out of the first live sweep permanently (terminal marker → skip). All marker writes on the no-recording path are now gated behind `!dryRun`. Verified against live REVI: window resolves 4 of 6 wons (Fernando + Luis Guillermo + Eric Pacheco + Josstinne); Claudio Andrade and Johan Solis genuinely have no scored call. Tests: 24 checks incl. the Fernando case, multi-call recency, and the post-sale-call exclusion.
- **2026-08-19 — Max now follows Slack threads everywhere; he was only ever thread-aware when tagged.** Ron replied inside the thread on Max's "Outcome-logging escalation" DM post (15 prospects, Aug 5–14) asking "is this still true? are these still pending?" — Max answered **at channel level**, about the unrelated 4-calls conversation running in the main DM. **Root cause: the DM handler never read `message.thread_ts` at all.** It never fetched the thread (so the escalation list was invisible — Max answered from `loadHistory()`, the flat last-10 DM history) and it replied with bare `say(reply)`, which posts to the DM root. The `#ng-pm-agent` channel handler replied *in*-thread but was equally blind to thread content. Only `app_mention` read a thread, and only on an explicit tag. **Shipped:** one shared path — `buildThreadContext()` plus the pure helpers `isMaxMessage` / `isMaxThread` / `formatThreadTranscript` / `shouldAnswerChannelMessage` / `claimEvent` — replacing the 65-line block that lived inline in `app_mention`; all three handlers now call the same code. **DM handler** reads the thread it was asked in and answers **in that thread** (roster-reject, rate-limit, error replies and file/voice notes included); top-level DMs stay top-level so no thread is started that nobody asked for. **Channel handler widened** past `#ng-pm-agent` to thread replies in any channel — public, private, group DM — but only threads **Max is already in** (he posted the root or replied earlier); top-level chatter elsewhere is still ignored, since answering everything in every channel he is a member of is a different product. That gate runs **before** `checkApproval` and the roster rejection, so neither fires in a channel he stays out of. **Two renderer bugs fixed in passing:** senders were printed as `user:U0AM…` for everyone except the tagger — who was labelled "Ron" regardless of who it actually was — so Max could not tell teammates apart in a thread; they now resolve through `getMemberContext()`, as does `<@Uxxx>` markup inside message text. And the thread **root** now gets 1200 chars where replies get 300 — the root is normally the report being asked about, and cutting it at 300 is part of why Max could not answer "are these still pending". **`claimEvent`** (60s TTL) is the new dedupe: an @mention inside a thread now matches both `app_mention` and `slack.message`, so whichever lands first answers. **Learning side effects stay tied to an explicit tag** (`tagged: true`, app_mention only): `extractAndSaveReportLesson` and `extractClientContext` are LLM calls, and firing them on every thread reply everywhere would multiply cost and manufacture "lessons" out of ordinary follow-up questions — today's behaviour is preserved exactly. **System prompt rule 6 — THE THREAD IS THE QUESTION:** a `THREAD CONTEXT` block outranks the flat conversation history; if the thread does not hold the answer Max says so rather than substituting the other conversation. `test/thread-context.test.js` (35 checks, slice-compiled) needs no CI edit — PR #76 replaced the by-name test list with a `test/*.test.js` glob while this branch was open, so the rebase dropped this branch's ci.yml change entirely. **Watch:** confirm **`message.mpim`** is subscribed in the Slack app config (api.slack.com/apps/A0AMJFZDY2X → Event Subscriptions) — group DMs stay dark without it, the identical silent gap that hid `message.groups` until 2026-05-13. One `conversations.replies` call now fires per thread reply in every channel Max is in; if that ever hits Slack rate limits the gate needs a cheaper pre-check.
- **2026-08-19 — The alert fan-out quality check was reporting a false positive hourly, to a team channel (#81, on top of #86).** Ron flagged two things about the 09:00/10:00 posts: Aura Bonilla was reported as having no alert card while her card sat in `#ng-new-client-alerts` posted the same minute, and the 10:00 post carried nothing but a skip line. **Cause 1 — the header filter matched nothing.** `GMAIL_ALERT_HEADERS` held the rendered glyphs (`🔔🔔 NEW FLYWHEEL AI CUSTOMER 🔔🔔`); `conversations.history` returns the message SOURCE, where Slack stores emoji as `:bell::bell:`. So `text.includes(header)` was false for every card in every route from the moment the check shipped: the content contract examined ZERO cards while reporting "0 bad card(s)" (green because it never looked), and `seenCards` stayed empty so the divergence half called every new customer uncovered. Aura was not a near miss — she was the first customer created after the check went live. **#78's email matcher was correct and did not fix it** (name components are genuinely reversed between `client_dashboards` and the card) — it had nothing to match against; the 11:00 CR run on that deployed fix still logged `1 customer(s) with no alert`, and that second data point is the only reason the real cause was found. **Two sessions found this in parallel** — #86 landed the emoji-free header list first; #81 rebased onto it rather than forcing a duplicate, and adds what #86 does not cover: the header must OPEN the first line with `:shortcodes:`/glyphs stripped (these channels carry human traffic ABOUT these alerts — "did the CLOSER REQUIRED alert ever fire?" would otherwise be graded as a malformed card), and a name that is punctuation only (the live 2026-08-12 CLOSER REQUIRED card rendered its name as `,` — non-empty, so the blank check passed it). **Cause 2 — a standing blind spot was firing as an hourly alarm.** A skipped channel counted as something to say, and `#ng-newclient-onboarding-tracking` was unreadable on every run (Max was not a member; Ron invited him 10:41 CR), so the check posted identical hourly messages whose whole content was "a channel could not be read" — remediation being a Slack invite, not a deploy. These go to `#ng-fullfillment-ops` via `postMakeHealthAlert`, so the whole fulfillment team was reading them. A run that finds NOTHING WRONG (no bad card, no uncovered customer, only skips) now reports once a day in the 09:00 CR run; a skip is still never green and still rides along with any run that DID find something, at any hour. Suppression is **stateless (clock-based) on purpose** — an in-memory "already said this today" memo resets on deploy, and 2026-08-19 shipped six deploys inside forty minutes. `nowMs` is injectable so tests drive both sides of the window without mocking the global clock. Also isolated the test sections in `test/gmail-alert-quality.test.js`: one throwing assertion used to cancel every later section, so CI reported the survivors as absent rather than failed — the first mutation run on this matcher read as "changed nothing" when it had in fact killed the section above it. **Verified live:** 13:00 CR run on the merged build logged `0 bad card(s), 0 customer(s) with no alert, 0 check(s) skipped` with nothing posted, and the 24h window covered Aura's card plus two lifecycle cards, so the contract actually examined them. 63 checks in that file; mutation-verified both ways. Fixtures throughout are now copied off `conversations.history`, not off the rendered screen — the old ones were faithful to what Make SENDS rather than what this code READS, which is why 47 green checks agreed with the bug.

- **2026-08-19 (evening) — First live appointment-status sweep wrote 0 of 6 (GHL 401 blink, not a scope gap); all outcome-loop GHL calls now go through `ghlFetch`.** The 2 PM REVI-notes run was a clean success — `✅ ALL GREEN — candidates 43, posted 40, no-contact 3`, 5 deferred by the cap — and Aura's note landed with the appointment relation from PR #84 working exactly as designed (bound to both her contact and appointment `4BFxyeQFgg6wCr3jYf1O`). The 3 PM sweep was not: `⚠️ ISSUES — considered 40, showed 0, write failures 6`, every one `401 The token is not authorized for this scope`. **Initially mis-diagnosed as a missing GHL token scope and reported to Ron as such — that was wrong.** The disproof took one request: the identical PUT, same token (`pit-583b1100…`, and `ghl-mcp` reads the very same `GHL_API_KEY` from `.env`, so there is no second key), same appointment, returned **200 an hour later**. It is GHL's transient auth blink — the exact phenomenon another session had already characterised from 95 strike sweeps in PR #70 ("401s hit 19 distinct contacts and 18 of them 401'd exactly once… load-independent… clears by itself") and hardened against in `ghlFetch`. **The outcome loop simply never used that wrapper:** every GHL call it added was raw `fetch`. Routed 11 call sites through `ghlFetch` (appointment status read+write, bulk calendar read, REVI note POST, contact-by-email, opportunity search/read/move) — `ghlMoveOpportunityStage` was already routed by the strike-mover work, so nothing to do there. Each site now carries a label for the retry log. **Also added the alert that was missing:** the sweep reported 6 failures to a console line nobody reads. It now DMs Ron when `failed >= 3` OR when it wrote nothing despite failures — thresholded so a single absorbed blink stays quiet. Aura's Aug 14 appointment was set to `showed` by hand during the diagnosis (correct value — REVI recorded 51 min).

- **2026-08-19 — Won-handoff notes ARMED LIVE, and the dry run that wrote state.** Closes the arc started by #69/#72. `WON_HANDOFF_MODE=live` set by Ron in the Railway UI (ng-pm-MAX) — **Claude cannot set Railway variables at all**: the permission classifier blocks the whole operation class, not just secret-looking values (same wall as `PORTAL_WRITER_DATABASE_URL` on 2026-08-14; don't retry it, hand the var to Ron). dash [PR #41](https://github.com/neurogrowth-cr/dash.neurogrowth.io/pull/41) merged (RPC migration record + read-only `closing_call_summary` block in `ProspectDetailDrawer`). **Incident worth keeping:** the first dry run fired 2026-08-19 15:00Z on pre-#72 code and wrote 3 terminal `gave_up` markers to agent_knowledge (Fernando Corella, Claudio Andrade, Johan Solis) — a DRY RUN mutated state, and because `gave_up` is terminal it would have sealed those deals out of the first live sweep permanently. Markers deleted by hand; #72 gates every marker write on `!dryRun`. Rule: if a mode is called dry-run, gate EVERY write behind it, not just the obvious one. **Live coverage as armed:** 6 wons in the 30d window, 4 resolve to a scored REVI call (Fernando Corella → Aug 4, Luis Guillermo Rodríguez → Jul 29, Eric Pacheco → Jul 27, Josstinne Montaño → Jul 22); Claudio Andrade and Johan Solis have no scored call at all and will report as gave-up, not failures. **Acceptance step deliberately skipped:** armed without any human reading a generated summary for TONE — the 15:00Z dry run printed 3 but the container rotated before the logs could be pulled, and Ron chose to arm anyway. First live run 2026-08-19 17:00 CR / 23:00Z; verification SQL + rollback (flip var → delete appended text by hand → delete `won-handoff:{appt}` markers → fix prompt) live in `~/automations/ops/recipes/won-handoff-notes.md`, now Status: live.
